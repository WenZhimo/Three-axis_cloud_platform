# 术语与命名规范

输入来源：

- 知识点/知识点总表.md
- 知识点/教学顺序.md
- 教材设计/教材章节目录.md

使用约束：

- 教材正文、图表、实践任务和思考题统一使用本文件术语。
- 正式术语必须来自已登记知识点、项目文件名、函数名、变量名或配置项。
- 不以俗称替代正式知识点名称。

---

# 正式知识点名称

以下名称必须保持一致：

- STM32F103RCTx芯片平台
- STM32CubeIDE工程配置
- 链接脚本与内存布局
- HAL驱动模块裁剪
- Makefile构建产物
- STM32 HAL驱动库
- CMSIS寄存器与内核访问
- Cortex-M3内核启动
- 系统时钟树
- HAL MSP初始化机制
- GPIO输出与复用
- AFIO重映射与SWD调试
- NVIC中断配置
- SysTick系统节拍
- DWT周期计数器
- Newlib系统调用适配
- UART串口调试输出
- 通用定时器PWM输出
- 高级定时器TIM8
- 基本定时器TIM6
- I2C主机通信
- USB FS设备
- USB Device中间件
- USB静态内存分配
- USB CDC虚拟串口
- USB描述符与唯一序列号
- 16位数据联合体解析
- 全局配置结构eepromConfig_t
- 枚举定义电机轴
- MPU6050寄存器配置
- MPU6050连续数据读取
- 三维矩阵乘法
- IMU安装方向矩阵
- 温度补偿标定
- 静态零偏估计
- 传感器物理量缩放
- 一阶低通滤波
- 加速度计重力校正
- 角度归一化
- 四元数姿态解算
- 欧拉角计算
- 互补滤波
- 传感器数据结构sensors_t
- 500Hz实时控制循环
- PID控制
- PID微分项滤波
- 目标角斜坡逼近
- 输出限幅与速率限制
- 积分抗饱和
- 机械角与电角转换
- 正弦查表快速三角函数
- 多定时器PWM相位同步
- 三相正弦PWM电机驱动
- 三轴电机硬件映射
- 自动跟随/回中算法
- 三轴云台控制架构
- 10Hz低频任务
- 运行状态门控
- NaN与Inf防护
- PID调参仿真工具
- ST-LINK与SWD调试配置
- 电机硬件诊断

---

# 项目文件命名

文件名必须保持项目原名：

- Three-axis_cloud_platformV2.ioc
- .cproject
- .mxproject
- .project
- STM32F103RCTX_FLASH.ld
- Three-axis_cloud_platformV2 Debug.launch
- Core/Src/main.c
- Core/Src/stm32f1xx_it.c
- Core/Src/stm32f1xx_hal_msp.c
- Core/Src/tim.c
- Core/Src/i2c.c
- Core/Src/usart.c
- Core/Src/gpio.c
- Core/Src/syscalls.c
- Core/Src/sysmem.c
- Drivers/CustomDrivers/Src/mpu6050.c
- Drivers/CustomDrivers/Src/mpu6050Calibration.c
- Drivers/CustomDrivers/Src/drv_pwmMotors.c
- Drivers/SRC/Src/MargAHRS.c
- Drivers/SRC/Src/pid.c
- Drivers/SRC/Src/computeMotorCommands.c
- Drivers/SRC/Src/firstOrderFilter.c
- Drivers/SRC/Src/evvgcCF.c
- Drivers/SRC/Src/fastTrig.c
- Drivers/SRC/Src/config.c
- Drivers/BGC/bgc32.h
- USB_DEVICE/App/usb_device.c
- USB_DEVICE/App/usbd_cdc_if.c
- USB_DEVICE/App/usbd_desc.c
- USB_DEVICE/Target/usbd_conf.c
- tools/pid_tuning_sim.py

---

# 函数、变量和配置项命名

函数名按项目源码原样书写：

- SystemClock_Config()
- HAL_Init()
- MX_GPIO_Init()
- MX_TIM3_Init()
- MX_TIM2_Init()
- MX_TIM4_Init()
- MX_I2C2_Init()
- MX_TIM8_Init()
- MX_USART3_UART_Init()
- MX_USB_DEVICE_Init()
- MX_TIM6_Init()
- MPU6050_Init()
- MPU6050_Read_And_Process()
- MargAHRSupdate()
- computeMotorCommands()
- PWM_Motor_Init()
- PWM_Motor_SetAngle()
- initPID()
- initFirstOrderFilter()
- orientIMU()

变量名和结构名按项目源码原样书写：

- frame_500Hz
- last_print_tick
- sensors_t
- eepromConfig_t
- MotorAxis_t
- sensors.margAttitude500Hz
- return_state_count_roll

配置项按项目文件原样书写：

- Mcu.CPN
- Mcu.Family
- Mcu.Package
- ProjectManager.TargetToolchain
- ProjectManager.FirmwarePackage
- USE_HAL_DRIVER
- STM32F103xE
- USE_RTOS

---

# 运行路径术语

以下术语用于区分“代码存在”“参与构建”和“进入当前运行路径”：

- 当前主线：指当前仓库证据能够串起的实际运行流程，可用于描述章节级或系统级数据流。
- 当前 500Hz 实时控制循环：指 `frame_500Hz -> 主循环消费 -> 传感器读取/姿态更新/控制输出` 这一周期路径。
- 当前主控制路径：指 `computeMotorCommands()` 及其下游 `PWM_Motor_SetAngle()`、TIM CCR 写入相关路径。
- 通信支线：指已初始化但不承担当前主控制闭环的通信能力，例如 USB CDC。
- 备用方案：指项目中存在、可作为替代思路分析，但当前没有进入主线的完整方案，例如 `evvgcCF.c` 互补滤波。
- 保留函数：指已经定义但当前没有调用证据的局部函数，例如 `calculateAccConfidence()`。
- 待接入边界：指函数、配置字段或常量看起来有设计意图，但当前没有形成调用或数据流证据，例如 `autoPan()` 与 AutoPan 配置字段。
- 诊断工具：指用于调试或点测的函数，不因存在定义而自动成为当前主线，例如 `PWM_Motor_Hardware_Diagnosis()`。

使用要求：

- 分析总系统时优先使用“当前主线”。
- 分析 `computeMotorCommands()` 到电机输出时使用“当前主控制路径”。
- 分析 USB CDC 等通信能力时使用“通信支线”。
- 分析未接入替代算法时使用“备用方案”，不要写成当前主线。
- 分析未调用的局部辅助函数时使用“保留函数”。
- 分析有设计意图但未形成调用证据的接口或字段时使用“待接入边界”。

---

# 阅读层次术语

Book Refinement 阶段统一使用以下四类阅读层次：

- 【必须掌握】：后续章节继续学习所必需的核心概念、项目主线和关键结论。
- 【工程深化】：工程实现、调试经验、设计权衡、性能影响和维护建议。
- 【拓展阅读】：帮助读者建立更完整知识体系，但不影响当前主线学习的补充材料。
- 【证据与验证】：源码证据、构建产物、官方资料、实验记录、证据边界和待验证项。

使用要求：

- 不得把证据审计内容写成所有读者都必须连续阅读的主线。
- 不得把拓展阅读写成后续章节必须依赖的前置知识。
- 每章可通过导读、表格、段落标签或章节末索引体现四层结构，不强制新增正式标题。

---

# 待验证分类术语

`【待验证】` 后续应尽量明确类别：

- 【硬件验证待完成】：需要电机、电流、温升、机械限位、实物方向或示波器等仓库外证据。
- 【软件验证待完成】：需要运行日志、断点、变量观测、串口输出或调试会话。
- 【参数验证待完成】：需要 PID 参数、限幅、速率限制、滤波参数或仿真参数的对比记录。
- 【构建验证待完成】：需要重新构建、构建日志、`.map`、`.list`、ELF 或板上镜像新鲜度证明。
- 【官方资料待确认】：需要 ST 官方手册、数据手册、应用笔记或权威资料复核。
- 【实验待完成】：需要设计并执行可复现实验，当前只有实验方案或观察入口。

使用要求：

- 只有在不破坏行文流畅的情况下细分；短句中仍可保留 `【待验证】`。
- 章节末或证据表中应优先使用细分类别，便于后续集中处理。
- 不得用“待验证”掩盖已能从源码或构建产物证明的静态事实。

---

# 交叉引用术语

重复知识点治理时统一使用以下表达：

- “完整讲解见第XX章……”
- “本节只做最小回顾。”
- “本章不重复展开……”
- “本节关注项目中的使用边界。”

使用要求：

- 一个知识点只保留一个完整讲解位置。
- 其它章节引用时，必须说明本章关注的差异、边界或应用层级。
- 不使用“前文说过”这类模糊引用，应尽量指向具体章节名。

---

# 禁用或需谨慎使用的称呼

以下称呼不得替代正式名称：

- “板子”不得替代 STM32F103RCTx芯片平台，除非上下文明确指硬件整体且有证据。
- “定时器”不得替代通用定时器PWM输出、高级定时器TIM8、基本定时器TIM6。
- “姿态算法”不得替代四元数姿态解算或互补滤波。
- “电机驱动”不得替代三相正弦PWM电机驱动或三轴电机硬件映射。
- “串口”不得替代 UART串口调试输出。
- “USB通信”不得替代 USB FS设备、USB Device中间件或 USB CDC虚拟串口。

---

# 未纳入边界术语

以下内容只能出现在【待验证】或“未纳入正式体系”说明中：

- SPI
- CAN
- ADC
- 业务 DMA
- RTOS
- 原理图/PCB

使用要求：

- 不得作为正式知识点标题。
- 不得作为正式章节主线。
- 不得作为实践任务的必要前置。
- 如后续发现项目证据，必须先回到知识库补充正式记录。
