# 项目主线分析

输入来源：

- 知识点/知识点总表.md
- 知识点/知识图谱.md

约束：

- 只记录项目中的数据流、控制流、通信流和层次结构。
- 不编写教程正文，不解释知识点内容。
- 流程中的函数名、变量名只作为项目证据路径节点，不作为正式知识点新增。
- 严格知识前置关系以 `知识点/知识依赖图.md` 与 `知识点/教学顺序.md` 为准。

---

# 数据流

## IMU采集数据流

MPU6050寄存器配置
↓
I2C主机通信
↓
MPU6050连续数据读取
↓
16位数据联合体解析
↓
三维矩阵乘法
↓
IMU安装方向矩阵
↓
温度补偿标定
↓
静态零偏估计
↓
传感器物理量缩放
↓
传感器数据结构sensors_t

项目证据：

- Drivers/CustomDrivers/Inc/mpu6050.h
- Drivers/CustomDrivers/Src/mpu6050.c
- Drivers/CustomDrivers/Src/mpu6050Calibration.c
- Core/Src/main.c
- Drivers/BGC/bgc32.h

## 姿态解算数据流

传感器数据结构sensors_t
↓
加速度计重力校正
↓
四元数姿态解算
↓
欧拉角计算
↓
sensors.margAttitude500Hz
↓
角度归一化
↓
PID控制

项目证据：

- Drivers/SRC/Src/MargAHRS.c
- Drivers/SRC/Src/evvgcCF.c
- Drivers/BGC/bgc32.h
- Core/Src/main.c

## 电机输出数据流

PID控制
↓
输出限幅与速率限制
↓
机械角与电角转换
↓
正弦查表快速三角函数
↓
三相正弦PWM电机驱动
↓
通用定时器PWM输出
↓
三轴电机硬件映射

项目证据：

- Drivers/SRC/Src/computeMotorCommands.c
- Drivers/SRC/Src/pid.c
- Drivers/SRC/Src/fastTrig.c
- Drivers/CustomDrivers/Src/drv_pwmMotors.c
- Core/Src/tim.c

---

# 控制流

## 启动控制流

Cortex-M3内核启动
↓
SystemInit()
↓
main()
↓
HAL_Init()
↓
SystemClock_Config()
↓
MX_GPIO_Init()
↓
MX_TIM3_Init()
↓
MX_TIM2_Init()
↓
MX_TIM4_Init()
↓
MX_I2C2_Init()
↓
MX_TIM8_Init()
↓
MX_USART3_UART_Init()
↓
MX_USB_DEVICE_Init()
↓
MX_TIM6_Init()
↓
init_eepromConfig()
↓
PWM_Motor_Init()
↓
initFirstOrderFilter()
↓
initPID()
↓
orientIMU()
↓
MPU6050_Init()

项目证据：

- Core/Startup/startup_stm32f103rctx.s
- Core/Src/main.c
- Core/Src/gpio.c
- Core/Src/tim.c
- Core/Src/i2c.c
- Core/Src/usart.c
- USB_DEVICE/App/usb_device.c
- Drivers/SRC/Src/config.c
- Drivers/CustomDrivers/Src/drv_pwmMotors.c
- Drivers/SRC/Src/firstOrderFilter.c
- Drivers/SRC/Src/pid.c
- Drivers/CustomDrivers/Src/mpu6050.c

## 500Hz实时控制流

SysTick系统节拍
↓
SysTick_Handler()
↓
frameCounter
↓
frame_500Hz
↓
main循环
↓
micros()
↓
MPU6050_Read_And_Process()
↓
MargAHRSupdate()
↓
运行状态门控
↓
computeMotorCommands()
↓
PWM_Motor_SetAngle()

项目证据：

- Core/Src/stm32f1xx_it.c
- Core/Src/main.c
- Drivers/CustomDrivers/Src/mpu6050.c
- Drivers/SRC/Src/MargAHRS.c
- Drivers/SRC/Src/computeMotorCommands.c
- Drivers/CustomDrivers/Src/drv_pwmMotors.c

## 10Hz低频控制流

SysTick系统节拍
↓
HAL_GetTick()
↓
last_print_tick
↓
状态计数检查
↓
UART串口调试输出

项目证据：

- Core/Src/main.c
- Core/Src/usart.c

---

# 通信流

## I2C传感器通信流

STM32 HAL驱动库
↓
HAL MSP初始化机制
↓
I2C主机通信
↓
MPU6050寄存器配置
↓
MPU6050连续数据读取

项目证据：

- Core/Src/i2c.c
- Drivers/CustomDrivers/Src/mpu6050.c
- Drivers/CustomDrivers/Inc/mpu6050.h

## UART调试通信流

STM32 HAL驱动库
↓
HAL MSP初始化机制
↓
UART串口调试输出
↓
Newlib系统调用适配
↓
printf()

项目证据：

- Core/Src/usart.c
- Core/Src/syscalls.c
- Core/Src/main.c

## USB CDC通信流

USB FS设备
↓
USB Device中间件
↓
USB静态内存分配
↓
USB CDC虚拟串口
↓
USB描述符与唯一序列号

项目证据：

- USB_DEVICE/App/usb_device.c
- USB_DEVICE/App/usbd_cdc_if.c
- USB_DEVICE/App/usbd_desc.c
- USB_DEVICE/Target/usbd_conf.c
- Middlewares/ST/STM32_USB_Device_Library

---

# 系统层次结构

硬件与芯片层：

- STM32F103RCTx芯片平台
- Cortex-M3内核启动
- 链接脚本与内存布局
- 系统时钟树
- CMSIS寄存器与内核访问

外设配置层：

- GPIO输出与复用
- AFIO重映射与SWD调试
- SysTick系统节拍
- NVIC中断配置
- 通用定时器PWM输出
- 高级定时器TIM8
- 基本定时器TIM6
- 多定时器PWM相位同步
- I2C主机通信
- UART串口调试输出
- USB FS设备

驱动与中间件层：

- HAL驱动模块裁剪
- HAL MSP初始化机制
- STM32 HAL驱动库
- USB Device中间件
- USB静态内存分配
- MPU6050寄存器配置
- MPU6050连续数据读取
- 三相正弦PWM电机驱动
- 三轴电机硬件映射

数据结构与配置层：

- 16位数据联合体解析
- 传感器物理量缩放
- 全局配置结构eepromConfig_t
- 传感器数据结构sensors_t
- 枚举定义电机轴

算法与估计层：

- 温度补偿标定
- 静态零偏估计
- 三维矩阵乘法
- 角度归一化
- 正弦查表快速三角函数
- 一阶低通滤波
- 互补滤波
- 四元数姿态解算
- 加速度计重力校正
- 欧拉角计算
- PID控制
- 积分抗饱和
- PID微分项滤波

控制与应用层：

- 三轴云台控制架构
- 500Hz实时控制循环
- 10Hz低频任务
- 运行状态门控
- 输出限幅与速率限制
- 目标角斜坡逼近
- 机械角与电角转换
- 自动跟随/回中算法

工程与调试层：

- STM32CubeIDE工程配置
- Makefile构建产物
- ST-LINK与SWD调试配置
- DWT周期计数器
- NaN与Inf防护
- 电机硬件诊断
- PID调参仿真工具
- Newlib系统调用适配

---

# 项目主线更新结论

主线一：

MPU6050连续数据读取
↓
传感器物理量缩放
↓
四元数姿态解算
↓
PID控制
↓
三相正弦PWM电机驱动

主线二：

SysTick系统节拍
↓
500Hz实时控制循环
↓
运行状态门控
↓
computeMotorCommands()
↓
PWM_Motor_SetAngle()

主线三：

STM32CubeIDE工程配置
↓
HAL驱动模块裁剪
↓
Makefile构建产物
↓
ST-LINK与SWD调试配置
↓
电机硬件诊断

待验证：

- 当前总表未发现上位机协议解析或参数更新代码；通信流仅能确认 UART printf 输出和 USB CDC 接口初始化。
- 当前总表未发现原理图/PCB 文件；电机与传感器硬件连接只能追溯到 .ioc 引脚配置和代码中的通道映射。
