**模块分析进度记录**

| 模块 | 新发现知识点分类 | 项目证据 | 更新后知识点统计数量 |
| --- | --- | --- | --- |
| Core/CubeMX/启动与外设初始化 | STM32基础、时钟系统、GPIO、中断系统、定时器、I2C、UART、USB | Core/Src/main.c、Core/Src/tim.c、Core/Src/i2c.c、Core/Src/usart.c、Core/Src/gpio.c、Core/Src/stm32f1xx_it.c、Core/Startup/startup_stm32f103rctx.s、Three-axis_cloud_platformV2.ioc | 23 |
| Drivers/CustomDrivers | I2C、通信知识、姿态解算、数学基础、电机控制、调试方法 | Drivers/CustomDrivers/Src/mpu6050.c、Drivers/CustomDrivers/Src/mpu6050Calibration.c、Drivers/CustomDrivers/Src/drv_pwmMotors.c | 35 |
| Drivers/SRC/BGC算法层 | PID控制、滤波算法、姿态解算、数学基础、软件架构、数据结构、算法知识 | Drivers/SRC/Src/computeMotorCommands.c、Drivers/SRC/Src/pid.c、Drivers/SRC/Src/MargAHRS.c、Drivers/SRC/Src/firstOrderFilter.c、Drivers/SRC/Src/evvgcCF.c、Drivers/SRC/Src/fastTrig.c、Drivers/BGC/bgc32.h | 53 |
| USB_DEVICE/Middlewares | USB、中间件、软件架构 | USB_DEVICE/App/usb_device.c、USB_DEVICE/App/usbd_cdc_if.c、USB_DEVICE/App/usbd_desc.c、USB_DEVICE/Target/usbd_conf.c、Middlewares/ST/STM32_USB_Device_Library | 57 |
| 工程配置/构建/工具 | 工程管理、调试方法 | .cproject、.mxproject、.project、.settings、STM32F103RCTX_FLASH.ld、Three-axis_cloud_platformV2 Debug.launch、Debug/objects.list、Debug/sources.mk、tools/pid_tuning_sim.py | 62 |

**分析备注**

项目模块识别结果：项目包含 CubeMX/Core 初始化层、CustomDrivers 传感器与电机驱动层、Drivers/SRC 控制算法层、ST HAL/CMSIS 驱动层、USB Device 中间件层、STM32CubeIDE/GCC 工程与 Debug 构建产物。

递归搜索当前工作树未发现原理图、PCB、PDF、Markdown、Word 等硬件或说明文档文件；因此未收录独立硬件设计知识点。SPI、CAN、ADC 等外设在当前工程中没有业务配置或调用证据，未作为项目使用知识点收录。DMA 库文件存在并被链接，但未发现业务 DMA 通道配置或 HAL_DMA_Start 等业务调用，因此不作为“项目使用 DMA”知识点收录。RTOS 配置为 USE_RTOS 0U，调试配置中 RTOS proxy 未启用，未发现任务创建或调度器启动代码，因此不作为“项目使用 RTOS”知识点收录。

---

# STM32F103RCTx芯片平台

类别：
STM32基础

项目证据：
CubeMX 配置声明 Mcu.CPN=STM32F103RCT6、Mcu.Family=STM32F1、Mcu.Package=LQFP64；工程宏定义包含 STM32F103xE；链接脚本目标为 STM32F103RCTx。

相关文件：
Three-axis_cloud_platformV2.ioc
.cproject
STM32F103RCTX_FLASH.ld
Drivers/CMSIS/Device/ST/STM32F1xx/Include/stm32f103xe.h

相关函数：
SystemInit()
SystemClock_Config()

出现次数：
4

关联知识点：
* Cortex-M3内核
* CMSIS设备头文件
* 链接脚本

后续需要深入讲解：
是

说明：
项目目标 MCU 平台。

---

# Cortex-M3内核启动

类别：
STM32基础

项目证据：
启动文件设置 .cpu cortex-m3、Reset_Handler、向量表 g_pfnVectors，并在复位后调用 SystemInit、拷贝 .data、清零 .bss、调用 __libc_init_array 和 main。

相关文件：
Core/Startup/startup_stm32f103rctx.s
Core/Src/system_stm32f1xx.c
STM32F103RCTX_FLASH.ld
Core/Src/main.c

相关函数：
Reset_Handler
SystemInit()
main()
Default_Handler

出现次数：
3

关联知识点：
* 中断向量表
* 链接脚本
* C运行时初始化

后续需要深入讲解：
是

说明：
用于 MCU 上电到 main 函数执行的启动链路。

---

# 链接脚本与内存布局

类别：
工程管理

项目证据：
链接脚本定义 FLASH 0x08000000 长度 256K、RAM 0x20000000 长度 48K，设置 _estack、_Min_Heap_Size=0x200、_Min_Stack_Size=0x400，并放置 .isr_vector、.text、.rodata、.data、.bss、._user_heap_stack。

相关文件：
STM32F103RCTX_FLASH.ld
Core/Startup/startup_stm32f103rctx.s
Core/Src/sysmem.c

相关函数：
_sbrk()
Reset_Handler

出现次数：
3

关联知识点：
* Flash与RAM地址
* 堆栈规划
* 启动文件

后续需要深入讲解：
是

说明：
用于固件镜像和运行时内存分配。

---

# 系统时钟树

类别：
时钟系统

项目证据：
SystemClock_Config 使用 HSE、PLL Source HSE、PLL_MUL9，SYSCLK 选择 PLLCLK，AHB=72MHz，APB1=36MHz，APB2=72MHz，USB=48MHz；.ioc 中 RCC.SYSCLKFreq_VALUE=72000000、RCC.USBFreq_Value=48000000。

相关文件：
Core/Src/main.c
Three-axis_cloud_platformV2.ioc
Core/Src/system_stm32f1xx.c
Core/Inc/stm32f1xx_hal_conf.h
Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_rcc.c

相关函数：
SystemClock_Config()
HAL_RCC_OscConfig()
HAL_RCC_ClockConfig()
HAL_RCCEx_PeriphCLKConfig()
HAL_RCC_GetSysClockFreq()
SystemCoreClockUpdate()

出现次数：
4

关联知识点：
* HSE
* PLL
* APB定时器时钟
* USB时钟

后续需要深入讲解：
是

说明：
决定系统主频、定时器频率和 USB 48MHz 时钟来源。

---

# SysTick系统节拍

类别：
中断系统

项目证据：
HAL_Init 通过 HAL_InitTick 配置默认 1ms HAL 时基；HAL_RCC_ClockConfig 在系统时钟切换后重新适配 HAL tick；main 中再次调用 HAL_SYSTICK_Config(SystemCoreClock / 1000) 和 HAL_NVIC_SetPriority(SysTick_IRQn, 0, 0)；SysTick_Handler 调用 HAL_IncTick，并通过 frameCounter 产生 500Hz 控制标志。

相关文件：
Core/Src/main.c
Core/Src/stm32f1xx_it.c
Core/Inc/main.h
Three-axis_cloud_platformV2.ioc
Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal.c
Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_rcc.c
Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_cortex.c

相关函数：
HAL_InitTick()
HAL_RCC_ClockConfig()
HAL_SYSTICK_Config()
SysTick_Handler()
HAL_IncTick()
HAL_GetTick()
micros()

出现次数：
7

关联知识点：
* 1ms系统滴答
* 500Hz控制循环
* HAL时基

后续需要深入讲解：
是

说明：
用于系统时间、调度标志和 HAL 延时。

---

# DWT周期计数器

类别：
调试方法

项目证据：
main 中使能 CoreDebug->DEMCR、DWT->CYCCNT 和 DWT->CTRL；micros() 结合 SysTick->VAL、SysTick->LOAD 与 HAL_GetTick 计算微秒时间戳；DWT_Delay_us 使用 DWT->CYCCNT 实现微秒延时，并被 mpu6050Calibration.c 与 mpu6050.c 调用；core_cm3.h 提供 DWT、CoreDebug 和相关位掩码定义。

相关文件：
Core/Src/main.c
Drivers/CustomDrivers/Src/mpu6050Calibration.c
Drivers/CustomDrivers/Inc/mpu6050Calibration.h
Drivers/CustomDrivers/Src/mpu6050.c
Drivers/CMSIS/Include/core_cm3.h

相关函数：
micros()
DWT_Delay_us()

出现次数：
5

关联知识点：
* Cortex-M调试寄存器
* 微秒计时
* 性能测量

后续需要深入讲解：
是

说明：
用于高精度计时和性能分析。

---

# NVIC中断配置

类别：
中断系统

项目证据：
启动文件向量表登记 SysTick_Handler、USB_LP_CAN1_RX0_IRQHandler 和 TIM8_UP_IRQHandler；HAL_Init 配置 NVIC_PRIORITYGROUP_4；.ioc 启用 SysTick、TIM8_UP_IRQn、USB_LP_CAN1_RX0_IRQn；tim.c 配置 TIM8_UP_IRQn 优先级 1；usbd_conf.c 配置 USB_LP_CAN1_RX0_IRQn 优先级 0；stm32f1xx_it.c 实现对应中断处理函数。

相关文件：
Three-axis_cloud_platformV2.ioc
Core/Startup/startup_stm32f103rctx.s
Core/Src/tim.c
USB_DEVICE/Target/usbd_conf.c
Core/Src/stm32f1xx_it.c
Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal.c
Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_cortex.c

相关函数：
HAL_NVIC_SetPriorityGrouping()
HAL_NVIC_SetPriority()
HAL_NVIC_EnableIRQ()
SysTick_Handler()
TIM8_UP_IRQHandler()
USB_LP_CAN1_RX0_IRQHandler()

出现次数：
7

关联知识点：
* 中断优先级
* 外设中断服务函数
* HAL IRQ分发

后续需要深入讲解：
是

说明：
用于时间基准、TIM8 和 USB 中断。

---

# GPIO输出与复用

类别：
GPIO

项目证据：
gpio.c 使能 GPIOD/GPIOA/GPIOB/GPIOC 时钟，将 PB12 配置为 GPIO_MODE_OUTPUT_PP、GPIO_PULLUP；tim.c 将 PWM 引脚配置为 GPIO_MODE_AF_PP；i2c.c 将 PB10/PB11 配置为 GPIO_MODE_AF_OD；usart.c 将 PC10/PC11 配置为 USART3 TX/RX。

相关文件：
Core/Src/gpio.c
Core/Src/tim.c
Core/Src/i2c.c
Core/Src/usart.c
Three-axis_cloud_platformV2.ioc

相关函数：
MX_GPIO_Init()
HAL_GPIO_Init()
HAL_GPIO_WritePin()
HAL_TIM_MspPostInit()
HAL_I2C_MspInit()
HAL_UART_MspInit()

出现次数：
5

关联知识点：
* GPIO推挽输出
* GPIO开漏复用
* 定时器PWM复用
* USART复用

后续需要深入讲解：
是

说明：
用于 LED/控制引脚、PWM、I2C 和串口引脚配置。

---

# AFIO重映射与SWD调试

类别：
GPIO

项目证据：
HAL_MspInit 启用 AFIO 和 PWR 时钟，并调用 __HAL_AFIO_REMAP_SWJ_NOJTAG；usart.c 调用 __HAL_AFIO_REMAP_USART3_PARTIAL；.ioc 配置 PA13/PA14 为 Serial Wire。

相关文件：
Core/Src/stm32f1xx_hal_msp.c
Core/Src/usart.c
Three-axis_cloud_platformV2.ioc

相关函数：
HAL_MspInit()
__HAL_AFIO_REMAP_SWJ_NOJTAG()
__HAL_AFIO_REMAP_USART3_PARTIAL()

出现次数：
3

关联知识点：
* JTAG关闭保留SWD
* USART3部分重映射
* AFIO

后续需要深入讲解：
是

说明：
用于释放调试相关引脚并完成 USART3 引脚映射。

---

# 通用定时器PWM输出

类别：
定时器

项目证据：
TIM2、TIM3、TIM4 配置 PWM 模式，TIM2 CH2/CH3/CH4、TIM3 CH1/CH2/CH3/CH4、TIM4 CH3/CH4 输出；main.c 调用 PWM_Motor_Init；drv_pwmMotors.c 调用 HAL_TIM_PWM_Start 并写 TIMx->CCR；computeMotorCommands.c 调用 PWM_Motor_SetAngle 进入运行时 PWM 更新路径。

相关文件：
Core/Src/tim.c
Three-axis_cloud_platformV2.ioc
Drivers/CustomDrivers/Src/drv_pwmMotors.c
Drivers/CustomDrivers/Inc/drv_pwmMotors.h
Core/Src/main.c
Drivers/SRC/Src/computeMotorCommands.c

相关函数：
MX_TIM2_Init()
MX_TIM3_Init()
MX_TIM4_Init()
HAL_TIM_PWM_Init()
HAL_TIM_PWM_ConfigChannel()
HAL_TIM_PWM_Start()
PWM_Motor_SetAngle()
computeMotorCommands()

出现次数：
6

关联知识点：
* PWM模式1
* CCR占空比
* 定时器通道
* GPIO复用

后续需要深入讲解：
是

说明：
用于三轴电机三相 PWM 输出。

---

# 高级定时器TIM8

类别：
定时器

项目证据：
TIM8 配置 PWM CH1/CH2/CH3，Period=999、Prescaler=3，并启用 TIM8_UP_IRQn；main.c 调用 MX_TIM8_Init；stm32f1xx_it.c 中 TIM8_UP_IRQHandler 调用 HAL_TIM_IRQHandler。

相关文件：
Core/Src/tim.c
Core/Src/stm32f1xx_it.c
Three-axis_cloud_platformV2.ioc
Core/Src/main.c

相关函数：
MX_TIM8_Init()
HAL_TIMEx_ConfigBreakDeadTime()
TIM8_UP_IRQHandler()
HAL_TIM_IRQHandler()

出现次数：
3

关联知识点：
* 高级定时器
* Break/DeadTime配置
* 定时器更新中断

后续需要深入讲解：
是

说明：
项目已配置 TIM8 PWM 与更新中断。

---

# 基本定时器TIM6

类别：
定时器

项目证据：
TIM6 配置 Prescaler=35、Period=65535；main 中调用 MX_TIM6_Init 并用 __HAL_TIM_SET_COUNTER(&htim6, 0) 清零计数器。

相关文件：
Core/Src/tim.c
Core/Src/main.c
Three-axis_cloud_platformV2.ioc

相关函数：
MX_TIM6_Init()
__HAL_TIM_SET_COUNTER()

出现次数：
3

关联知识点：
* 基本定时器
* 计数器清零
* 微秒级测时

后续需要深入讲解：
是

说明：
用于辅助计时配置。

---

# 多定时器PWM相位同步

类别：
电机控制

项目证据：
Core/Src/tim.c 和 Three-axis_cloud_platformV2.ioc 将 TIM2/TIM3/TIM4 配置为相同 Prescaler=0、Period=3599 的 PWM 输出；PWM_Motor_Init 启动 TIM2/TIM3/TIM4 PWM 后关闭全局中断，并连续写 TIM2->CNT、TIM3->CNT、TIM4->CNT 为 0，以减小多个定时器的 PWM 相位错位。

相关文件：
Drivers/CustomDrivers/Src/drv_pwmMotors.c
Core/Src/tim.c
Three-axis_cloud_platformV2.ioc

相关函数：
PWM_Motor_Init()
__disable_irq()
__enable_irq()

出现次数：
1

关联知识点：
* 直接寄存器访问
* PWM同步
* 临界区

后续需要深入讲解：
是

说明：
用于三相电机多定时器输出相位一致。

---

# I2C主机通信

类别：
I2C

项目证据：
I2C2 配置 100kHz、7位地址、PB10/PB11；main.c 在 MPU6050 初始化和读取前调用 MX_I2C2_Init；mpu6050.h 定义 MPU6050_I2C_ADDR；mpu6050.c 使用 HAL_I2C_Mem_Write 和 HAL_I2C_Mem_Read 访问 MPU6050 寄存器。

相关文件：
Core/Src/i2c.c
Drivers/CustomDrivers/Src/mpu6050.c
Drivers/CustomDrivers/Inc/mpu6050.h
Three-axis_cloud_platformV2.ioc
Core/Src/main.c

相关函数：
MX_I2C2_Init()
HAL_I2C_Init()
MPU_Write_Reg()
MPU_Read_Reg()
HAL_I2C_Mem_Write()
HAL_I2C_Mem_Read()
MPU6050_Init()
MPU6050_Read_And_Process()

出现次数：
5

关联知识点：
* I2C时序
* 7位地址
* 寄存器读写
* 开漏引脚

后续需要深入讲解：
是

说明：
用于 MPU6050 传感器通信。

---

# MPU6050寄存器配置

类别：
通信知识

项目证据：
mpu6050.h 定义 WHO_AM_I、PWR_MGMT_1、SMPLRT_DIV、CONFIG、GYRO_CONFIG、ACCEL_CONFIG 和数据寄存器地址；MPU6050_Init 读取 WHO_AM_I=0x68，并写入采样率、DLPF、陀螺仪量程和加速度计量程。

相关文件：
Drivers/CustomDrivers/Inc/mpu6050.h
Drivers/CustomDrivers/Src/mpu6050.c
Core/Src/main.c

相关函数：
MPU6050_Init()
MPU_Write_Reg()
MPU_Read_Reg()

出现次数：
3

关联知识点：
* I2C
* 传感器寄存器表
* DLPF
* 量程设置

后续需要深入讲解：
是

说明：
用于 IMU 初始化和数据采集。

---

# MPU6050连续数据读取

类别：
通信知识

项目证据：
Core/Src/stm32f1xx_it.c 在 SysTick_Handler 中按 COUNT_500HZ 置位 frame_500Hz；Core/Src/main.c 在 500Hz 主循环中调用 MPU6050_Read_And_Process；MPU6050_Read_And_Process 从 REG_ACCEL_XOUT_H 连续读取 MPU_DATA_LENGTH=14 字节，解析加速度、温度和陀螺仪数据。

相关文件：
Drivers/CustomDrivers/Src/mpu6050.c
Drivers/CustomDrivers/Inc/mpu6050.h
Core/Src/main.c
Core/Src/stm32f1xx_it.c
Core/Inc/main.h
Drivers/SRC/Src/evvgcCF.c

相关函数：
MPU6050_Read_And_Process()
MPU_Read_Reg()

出现次数：
4

关联知识点：
* I2C突发读取
* 大小端解析
* 原始传感器数据

后续需要深入讲解：
是

说明：
用于 500Hz 控制循环中的 IMU 数据输入。

---

# 16位数据联合体解析

类别：
数据结构

项目证据：
mpu6050.h 定义 Int16_Converter_t union，用 bytes[0]/bytes[1] 拼接 int16_t value；bgc32.h 定义 int16andUint8_t、int32andUint8_t、uint16andUint8_t。

相关文件：
Drivers/CustomDrivers/Inc/mpu6050.h
Drivers/CustomDrivers/Src/mpu6050.c
Drivers/BGC/bgc32.h

相关函数：
MPU6050_Read_And_Process()

出现次数：
3

关联知识点：
* union
* 字节序
* 传感器原始值

后续需要深入讲解：
是

说明：
用于从 I2C 字节缓冲区恢复有符号传感器数据。

---

# 传感器物理量缩放

类别：
数学基础

项目证据：
mpu6050.h 定义 ACCEL_SCALE_FACTOR、GYRO_SCALE_FACTOR 和灵敏度常量；main.c 将 rawAccel/rawGyro 减偏置后乘缩放因子得到 sensors.accel500Hz 与 sensors.gyro500Hz。

相关文件：
Drivers/CustomDrivers/Inc/mpu6050.h
Core/Src/main.c
Drivers/CustomDrivers/Src/mpu6050.c

相关函数：
MPU6050_Get_Physical_Data()
main()

出现次数：
3

关联知识点：
* LSB到物理单位转换
* 加速度单位
* 角速度单位

后续需要深入讲解：
是

说明：
用于将 MPU6050 原始数据转换为控制算法输入。

---

# 温度补偿标定

类别：
滤波算法

项目证据：
mpu6050Calibration.c 采集冷机/热机两阶段数据，根据 tempDiff 计算 accelTCBiasSlope/Intercept 与 gyroTCBiasSlope/Intercept；MPU6050_ComputeTemError 用温度线性模型计算偏置。

相关文件：
Drivers/CustomDrivers/Src/mpu6050Calibration.c
Drivers/CustomDrivers/Src/mpu6050.c
Drivers/CustomDrivers/Inc/mpu6050.h
Drivers/SRC/Src/config.c

相关函数：
mpu6050Calibration()
MPU6050_ComputeTemError()
DWT_Delay_us()

出现次数：
4

关联知识点：
* 线性拟合
* 传感器偏置
* 温度漂移

后续需要深入讲解：
是

说明：
用于补偿 IMU 温漂造成的零偏变化。

---

# 静态零偏估计

类别：
滤波算法

项目证据：
MPU6050_ComputeStaticError 采样 5000 次，累加加速度和陀螺仪数据，计算 accelRTBias、gyroRTBias 和 accelOneG。

相关文件：
Drivers/CustomDrivers/Src/mpu6050.c
Drivers/CustomDrivers/Inc/mpu6050.h

相关函数：
MPU6050_ComputeStaticError()
MPU6050_Read_And_Process()
MPU6050_ComputeTemError()

出现次数：
2

关联知识点：
* 均值估计
* 零偏
* 重力模长

后续需要深入讲解：
是

说明：
用于启动阶段传感器静态误差估计。

---

# IMU安装方向矩阵

类别：
姿态解算

项目证据：
mpu6050.c 定义 orientationMatrix[9]，orientIMU 根据 imuOrientation 填充 3x3 矩阵；Core/Src/main.c 调用 orientIMU；config.c 设置默认 imuOrientation；MPU6050_Read_And_Process 调用 matrixMultiply 旋转加速度与陀螺仪数据。

相关文件：
Drivers/CustomDrivers/Src/mpu6050.c
Drivers/CustomDrivers/Inc/mpu6050.h
Drivers/SRC/Src/config.c
Core/Src/main.c

相关函数：
orientIMU()
matrixMultiply()
MPU6050_Read_And_Process()

出现次数：
3

关联知识点：
* 三维坐标变换
* 矩阵乘法
* IMU安装方向

后续需要深入讲解：
是

说明：
用于把传感器坐标转换到控制坐标。

---

# 三维矩阵乘法

类别：
数学基础

项目证据：
matrixMultiply 实现 matrixC = matrixA * matrixB，用于 3x3 方向矩阵乘 3x1 传感器向量。

相关文件：
Drivers/CustomDrivers/Src/mpu6050.c
Drivers/CustomDrivers/Inc/mpu6050.h

相关函数：
matrixMultiply()
MPU6050_Read_And_Process()

出现次数：
2

关联知识点：
* 坐标变换
* IMU方向矩阵

后续需要深入讲解：
是

说明：
用于传感器数据坐标旋转。

---

# UART串口调试输出

类别：
UART

项目证据：
USART3 配置 115200、8N1、TX/RX、PC10/PC11；.ioc 记录 PC10/PC11 和 USART3 异步模式；usart.c 通过 fputc 和 _write 将 printf 重定向到 HAL_UART_Transmit；.cproject 与 Debug/makefile 启用 newlib-nano 浮点 printf/scanf；main.c、mpu6050.c 和 mpu6050Calibration.c 存在 printf 调试输出。

相关文件：
Core/Src/usart.c
Core/Src/main.c
Three-axis_cloud_platformV2.ioc
.cproject
Debug/makefile
Drivers/CustomDrivers/Src/mpu6050.c
Drivers/CustomDrivers/Src/mpu6050Calibration.c

相关函数：
MX_USART3_UART_Init()
HAL_UART_Init()
HAL_UART_Transmit()
fputc()
_write()
printf()

出现次数：
7

关联知识点：
* 串口参数
* printf重定向
* 调试日志

后续需要深入讲解：
是

说明：
用于初始化提示和运行数据输出。

---

# USB FS设备

类别：
USB

项目证据：
.ioc 配置 USB_DEVICE CDC_FS，PA11/PA12 为 USB_DM/USB_DP，USB 时钟 48MHz；usb_device.c 初始化 USBD，usbd_conf.c 初始化 PCD USB FS 并配置 PMA 端点。

相关文件：
Three-axis_cloud_platformV2.ioc
USB_DEVICE/App/usb_device.c
USB_DEVICE/Target/usbd_conf.c
USB_DEVICE/App/usbd_desc.c

相关函数：
MX_USB_DEVICE_Init()
USBD_Init()
USBD_Start()
USBD_LL_Init()
HAL_PCD_Init()
HAL_PCDEx_PMAConfig()

出现次数：
4

关联知识点：
* USB全速设备
* PCD驱动
* USB端点PMA
* USB时钟

后续需要深入讲解：
是

说明：
用于项目 USB Device 功能。

---

# USB CDC虚拟串口

类别：
USB

项目证据：
usb_device.c 注册 USBD_CDC 和 USBD_Interface_fops_FS；usbd_cdc_if.c 实现 CDC_Init_FS、CDC_Receive_FS、CDC_Transmit_FS；USB 描述符中产品字符串为 STM32 Virtual ComPort。

相关文件：
USB_DEVICE/App/usb_device.c
USB_DEVICE/App/usbd_cdc_if.c
USB_DEVICE/App/usbd_desc.c
Middlewares/ST/STM32_USB_Device_Library/Class/CDC/Src/usbd_cdc.c

相关函数：
USBD_RegisterClass()
USBD_CDC_RegisterInterface()
CDC_Init_FS()
CDC_Receive_FS()
CDC_Transmit_FS()

出现次数：
4

关联知识点：
* USB CDC类
* 虚拟串口
* USB描述符

后续需要深入讲解：
是

说明：
用于 USB 虚拟串口通信接口。

---

# USB描述符与唯一序列号

类别：
USB

项目证据：
usbd_desc.c 定义 VID、PID、Manufacturer、Product、Configuration、Interface 字符串，并通过 DEVICE_ID1/2/3 生成序列号字符串。

相关文件：
USB_DEVICE/App/usbd_desc.c
USB_DEVICE/App/usbd_desc.h

相关函数：
USBD_FS_DeviceDescriptor()
USBD_FS_ProductStrDescriptor()
USBD_FS_SerialStrDescriptor()
Get_SerialNum()
IntToUnicode()

出现次数：
2

关联知识点：
* USB设备描述符
* 字符串描述符
* STM32唯一ID

后续需要深入讲解：
是

说明：
用于主机枚举 USB 设备。

---

# USB静态内存分配

类别：
软件架构

项目证据：
usbd_conf.h 将 USBD_malloc 映射到 USBD_static_malloc；usbd_conf.c 使用静态数组 mem 分配 USBD_CDC_HandleTypeDef，不实现动态释放。

相关文件：
USB_DEVICE/Target/usbd_conf.h
USB_DEVICE/Target/usbd_conf.c

相关函数：
USBD_static_malloc()
USBD_static_free()

出现次数：
2

关联知识点：
* 嵌入式内存管理
* USB中间件
* 静态分配

后续需要深入讲解：
是

说明：
用于 USB Device 中间件运行时对象分配。

---

# HAL驱动模块裁剪

类别：
软件架构

项目证据：
stm32f1xx_hal_conf.h 启用 HAL_GPIO_MODULE_ENABLED、HAL_I2C_MODULE_ENABLED、HAL_PCD_MODULE_ENABLED、HAL_TIM_MODULE_ENABLED、HAL_UART_MODULE_ENABLED、HAL_CORTEX_MODULE_ENABLED、HAL_DMA_MODULE_ENABLED、HAL_FLASH_MODULE_ENABLED、HAL_EXTI_MODULE_ENABLED、HAL_PWR_MODULE_ENABLED、HAL_RCC_MODULE_ENABLED，并注释 ADC/CAN/SPI/RTOS 等模块；.mxproject 记录 CubeMX 生成和曾使用的 HAL/USB 库文件列表。

相关文件：
Core/Inc/stm32f1xx_hal_conf.h
.cproject
.mxproject
Debug/objects.list

相关函数：
无

出现次数：
4

关联知识点：
* HAL配置
* 条件编译
* 工程裁剪

后续需要深入讲解：
是

说明：
用于控制编译进项目的 HAL 外设模块。

---

# HAL MSP初始化机制

类别：
软件架构

项目证据：
工程实现 HAL_MspInit、HAL_TIM_Base_MspInit、HAL_TIM_PWM_MspInit、HAL_I2C_MspInit、HAL_UART_MspInit、HAL_PCD_MspInit，为 HAL 外设初始化提供时钟、GPIO、中断配置。

相关文件：
Core/Src/stm32f1xx_hal_msp.c
Core/Src/tim.c
Core/Src/i2c.c
Core/Src/usart.c
USB_DEVICE/Target/usbd_conf.c

相关函数：
HAL_MspInit()
HAL_TIM_Base_MspInit()
HAL_TIM_PWM_MspInit()
HAL_I2C_MspInit()
HAL_UART_MspInit()
HAL_PCD_MspInit()

出现次数：
5

关联知识点：
* HAL回调
* 外设底层初始化
* 时钟使能

后续需要深入讲解：
是

说明：
用于连接 CubeMX 外设配置和底层硬件资源。

---

# CMSIS寄存器与内核访问

类别：
STM32基础

项目证据：
代码直接访问 SysTick->VAL、SysTick->LOAD、CoreDebug->DEMCR、DWT->CYCCNT、DWT->CTRL、SCB->SCR、TIMx->CNT、TIMx->CCR；Cortex-M3 内核访问由 Drivers/CMSIS/Include/core_cm3.h 提供，STM32F103xE 设备外设寄存器访问由 Drivers/CMSIS/Device/ST/STM32F1xx/Include/stm32f103xe.h 提供。

相关文件：
Core/Src/main.c
Drivers/CustomDrivers/Src/mpu6050Calibration.c
Drivers/CustomDrivers/Src/drv_pwmMotors.c
USB_DEVICE/Target/usbd_conf.c
Drivers/CMSIS/Include/core_cm3.h
Drivers/CMSIS/Device/ST/STM32F1xx/Include/stm32f103xe.h

相关函数：
micros()
DWT_Delay_us()
PWM_Motor_Init()
PWM_Motor_SetAngle()
HAL_PCD_SuspendCallback()

出现次数：
5

关联知识点：
* CMSIS
* 直接寄存器访问
* Cortex-M内核寄存器

后续需要深入讲解：
是

说明：
用于绕过 HAL 完成计时、PWM 更新和低功耗相关配置。

---

# Newlib系统调用适配

类别：
工程管理

项目证据：
syscalls.c 提供弱 _read、_write、_close、_fstat、_isatty、_lseek 等低层系统调用入口；sysmem.c 通过 _sbrk 连接 newlib 堆分配与链接脚本中的堆栈边界；usart.c 提供 GCC 下的强 _write 串口输出实现；.cproject 与 Debug/makefile 启用 newlib-nano 浮点 printf/scanf。

相关文件：
Core/Src/syscalls.c
Core/Src/sysmem.c
Core/Src/usart.c
.cproject
Debug/makefile

相关函数：
_read()
_write()
_sbrk()
_fstat()
_isatty()

出现次数：
5

关联知识点：
* newlib
* printf支持
* 裸机系统调用

后续需要深入讲解：
是

说明：
用于 C 标准库在裸机环境下运行。

---

# 三轴云台控制架构

类别：
软件架构

项目证据：
main 初始化配置、PWM、滤波、PID、IMU 和 AHRS；SysTick 置位 frame_500Hz，500Hz 循环读取传感器、缩放物理量、调用 MargAHRSupdate、执行启动延时门控并调用 computeMotorCommands；控制函数按 ROLL/PITCH/YAW 分轴计算电角和电机指令，最终调用 PWM_Motor_SetAngle。

相关文件：
Core/Inc/main.h
Core/Src/stm32f1xx_it.c
Core/Src/main.c
Drivers/CustomDrivers/Src/mpu6050.c
Drivers/SRC/Src/MargAHRS.c
Drivers/SRC/Src/computeMotorCommands.c
Drivers/SRC/Inc/computeMotorCommands.h
Drivers/BGC/bgc32.h
Drivers/CustomDrivers/Src/drv_pwmMotors.c

相关函数：
main()
MPU6050_Read_And_Process()
MargAHRSupdate()
computeMotorCommands()
PWM_Motor_SetAngle()

出现次数：
4

关联知识点：
* 实时控制循环
* 姿态解算
* PID控制
* 电机PWM输出

后续需要深入讲解：
是

说明：
用于贯穿传感器输入、姿态估计、控制计算和执行器输出。

---

# 500Hz实时控制循环

类别：
软件架构

项目证据：
main.h 定义 FRAME_COUNT=1000、COUNT_500HZ=2、frame_500Hz 和 dt500Hz 声明；SysTick_Handler 每 1ms 增加 frameCounter，并在 frameCounter % COUNT_500HZ == 0 时设置 frame_500Hz；main 循环在 frame_500Hz 时执行 IMU 读取、AHRS 和电机控制；TIM6 在 main 中完成初始化和计数器清零，但当前没有作为 500Hz 触发源的证据。

相关文件：
Core/Inc/main.h
Core/Src/stm32f1xx_it.c
Core/Src/main.c
Core/Src/tim.c

相关函数：
SysTick_Handler()
main()
MX_TIM6_Init()
MPU6050_Read_And_Process()
MargAHRSupdate()
computeMotorCommands()

出现次数：
3

关联知识点：
* SysTick
* 实时调度
* 控制周期dt

后续需要深入讲解：
是

说明：
用于主控制任务周期触发。

---

# 10Hz低频任务

类别：
软件架构

项目证据：
main 循环用 HAL_GetTick() - last_print_tick >= 100 触发 100ms 任务，处理 return_state_count、使能 roll，并 printf 输出状态。

相关文件：
Core/Src/main.c

相关函数：
main()
HAL_GetTick()
printf()

出现次数：
1

关联知识点：
* 非实时任务
* 串口打印
* 状态检查

后续需要深入讲解：
否

说明：
用于低频状态检查和调试输出。

---

# PID控制

类别：
PID控制

项目证据：
pid.h 定义 PIDdata_t、ANGULAR、D_ERROR 等 PID 接口类型；pid.c 实现 initPID、updatePID、P/I/D 输出计算；config.c 设置 ROLL/PITCH/YAW 默认 PID 参数；main.c 在调试初始化阶段覆盖部分 PID 参数并在 AHRS 收敛后清空 PID 状态；computeMotorCommands.c 在轴使能和状态分支满足时调用 updatePID。

相关文件：
Drivers/SRC/Src/pid.c
Drivers/SRC/Inc/pid.h
Drivers/SRC/Src/config.c
Core/Src/main.c
Drivers/SRC/Src/computeMotorCommands.c

相关函数：
initPID()
updatePID()
computeMotorCommands()

出现次数：
5

关联知识点：
* 三轴控制
* 误差计算
* D项滤波
* 积分限幅

后续需要深入讲解：
是

说明：
用于云台三轴姿态控制。

---

# 积分抗饱和

类别：
PID控制

项目证据：
pid.c 在 iHold 为 false 时更新 iTerm，并限制到 [-10, 10]；computeMotorCommands.c 根据误差条件设置 holdIntegrators 或 rollHoldIntegrators；main 在 AHRS 收敛后调用 zeroPIDintegralError。

相关文件：
Drivers/SRC/Src/pid.c
Drivers/SRC/Src/computeMotorCommands.c
Core/Src/main.c

相关函数：
updatePID()
zeroPIDintegralError()
setPIDintegralError()
computeMotorCommands()

出现次数：
3

关联知识点：
* PID积分项
* 输出限幅
* 启动收敛

后续需要深入讲解：
是

说明：
用于防止控制误差大时积分累计失控。

---

# PID微分项滤波

类别：
PID控制

项目证据：
pid.c 定义 F_CUT=20Hz，rc=1/(TWO_PI*F_CUT)，updatePID 对 dTerm 使用一阶低通并计算三点平均 dAverage；tools/pid_tuning_sim.py 用 RC_D_FILTER 匹配固件 D 项滤波。

相关文件：
Drivers/SRC/Src/pid.c
tools/pid_tuning_sim.py

相关函数：
initPID()
updatePID()
update_pid()

出现次数：
2

关联知识点：
* 一阶低通滤波
* 微分噪声
* PID仿真

后续需要深入讲解：
是

说明：
用于平滑 PID 微分项。

---

# 角度归一化

类别：
数学基础

项目证据：
computeMotorCommands.c 实现 wrapToPif，将角度限制到 [-PI, PI]；pid.c 实现 standardRadianFormat；MargAHRS.c 内也定义 wrapToPif；tools/pid_tuning_sim.py 实现 wrap_pi。

相关文件：
Drivers/SRC/Src/computeMotorCommands.c
Drivers/SRC/Src/pid.c
Drivers/SRC/Src/MargAHRS.c
tools/pid_tuning_sim.py

相关函数：
wrapToPif()
standardRadianFormat()
wrap_pi()

出现次数：
4

关联知识点：
* 弧度制
* 环形角误差
* PID角度控制

后续需要深入讲解：
是

说明：
用于避免角度跨越正负 PI 时误差异常。

---

# 机械角与电角转换

类别：
电机控制

项目证据：
computeMotorCommands.c 定义 mechanical2electricalDegrees 和 electrical2mechanicalDegrees，并在三轴控制中使用机械角乘换算系数得到电角；config.c 设置 roll/pitch/yawMotorPoles，但当前未发现 computeMotorCommands.c 读取这些配置项参与换算；drv_pwmMotors.c 定义 POLE_PAIRS，但当前主输出链路未见使用该常量。

相关文件：
Drivers/SRC/Src/computeMotorCommands.c
Drivers/SRC/Inc/computeMotorCommands.h
Drivers/SRC/Src/config.c
Drivers/CustomDrivers/Src/drv_pwmMotors.c

相关函数：
computeMotorCommands()
PWM_Motor_SetAngle()

出现次数：
4

关联知识点：
* 电机极对数
* 三相PWM
* 云台角度控制

后续需要深入讲解：
是

说明：
用于把云台姿态误差转换为电机定子电角控制量。

---

# 输出限幅与速率限制

类别：
电机控制

项目证据：
computeMotorCommands.c 使用 clampf、ROLL/PITCH/YAW_CMD_LIMIT_RAD、outputRate、pidCmdPrev 和 eepromConfig.rateLimit 限制 PID 输出和变化速度；config.c 设置 rateLimit=45.0f*D2R；tools 脚本也模拟 cmd_limit 与 rate_limit。

相关文件：
Drivers/SRC/Src/computeMotorCommands.c
Drivers/SRC/Src/config.c
tools/pid_tuning_sim.py

相关函数：
clampf()
moveTowardsf()
moveTowardsAnglef()
computeMotorCommands()
run_simulation()

出现次数：
3

关联知识点：
* PID输出保护
* 斜率限制
* 电机安全

后续需要深入讲解：
是

说明：
用于限制电机指令突变和过大补偿。

---

# 目标角斜坡逼近

类别：
电机控制

项目证据：
computeMotorCommands.c 实现 moveTowardsf 与 moveTowardsAnglef；当前实际调用 moveTowardsAnglef 的路径集中在 Roll 轴，使用 ROLL_SLEW_RAD_S 和 ROLL_TARGET_SLEW_RAD_S 平滑 rollTargetSlew。PITCH_TARGET_SLEW_RAD_S 与 YAW_TARGET_SLEW_RAD_S 当前仅见定义，未发现参与当前 Pitch/Yaw 控制路径的调用证据。

相关文件：
Drivers/SRC/Src/computeMotorCommands.c

相关函数：
moveTowardsf()
moveTowardsAnglef()
computeMotorCommands()

出现次数：
1

关联知识点：
* 速率限制
* 角度归一化
* 平滑控制

后续需要深入讲解：
是

说明：
用于目标姿态逐步过渡。

---

# 三相正弦PWM电机驱动

类别：
电机控制

项目证据：
drv_pwmMotors.c 生成 SIN_TABLE_SIZE=720 的 sinTable，PWM_Motor_SetAngle 根据电角计算 A/B/C 三相相差 120 度的 CCR 值，并写入 TIM2/TIM3/TIM4 对应通道；Core/Src/tim.c 配置这些通道为 PWM1 模式并设置统一周期。

相关文件：
Drivers/CustomDrivers/Src/drv_pwmMotors.c
Drivers/CustomDrivers/Inc/drv_pwmMotors.h
Core/Src/tim.c

相关函数：
GenerateSinTable()
PWM_Motor_Init()
PWM_Motor_SetAngle()

出现次数：
2

关联知识点：
* PWM输出
* 正弦查表
* 三相电机
* 电角

后续需要深入讲解：
是

说明：
用于云台电机三相驱动输出。

---

# 三轴电机硬件映射

类别：
电机控制

项目证据：
PWM_Motor_SetAngle 将 MOTOR_PITCH 映射到 TIM3 CH2/CH3/CH4，将 MOTOR_ROLL 映射到 TIM3 CH1、TIM2 CH4、TIM2 CH3，将 MOTOR_YAW 映射到 TIM4 CH4、TIM2 CH2、TIM4 CH3。

相关文件：
Drivers/CustomDrivers/Src/drv_pwmMotors.c
Core/Src/tim.c
Three-axis_cloud_platformV2.ioc

相关函数：
PWM_Motor_SetAngle()
PWM_Motor_Init()

出现次数：
3

关联知识点：
* 定时器通道
* GPIO复用
* 三相PWM

后续需要深入讲解：
是

说明：
用于确定三轴电机相线对应的 PWM 通道。

---

# 电机硬件诊断

类别：
调试方法

项目证据：
PWM_Motor_Hardware_Diagnosis 每 1000ms 切换 step，关闭所有 CCR 后只给单相写固定 duty，用于验证底层引脚和驱动管。

相关文件：
Drivers/CustomDrivers/Src/drv_pwmMotors.c

相关函数：
PWM_Motor_Hardware_Diagnosis()
HAL_GetTick()

出现次数：
1

关联知识点：
* PWM通道测试
* 硬件排查
* 定时调试

后续需要深入讲解：
否

说明：
用于绕过控制算法验证输出硬件。

---

# 四元数姿态解算

类别：
姿态解算

项目证据：
MargAHRS.c 定义 q0/q1/q2/q3 及 q0q0 等乘积，MargAHRSinit 用 atan2f、cosf、sinf 初始化四元数，MargAHRSupdate 根据陀螺仪积分更新四元数并归一化。

相关文件：
Drivers/SRC/Src/MargAHRS.c
Drivers/SRC/Inc/MargAHRS.h
Core/Src/main.c

相关函数：
MargAHRSinit()
MargAHRSupdate()

出现次数：
3

关联知识点：
* 陀螺仪积分
* 四元数归一化
* 欧拉角转换

后续需要深入讲解：
是

说明：
用于计算 roll、pitch、yaw 姿态角。

---

# 加速度计重力校正

类别：
姿态解算

项目证据：
MargAHRSupdate 归一化加速度向量，计算 vx/vy/vz 重力方向估计，利用 exAcc/eyAcc/ezAcc 对 gx/gy/gz 作比例修正。

相关文件：
Drivers/SRC/Src/MargAHRS.c
Core/Src/main.c
Drivers/SRC/Src/config.c

相关函数：
MargAHRSupdate()
calculateAccConfidence()

出现次数：
3

关联知识点：
* 传感器融合
* 重力向量
* 加速度置信度

后续需要深入讲解：
是

说明：
用于抑制陀螺仪积分漂移。

---

# 欧拉角计算

类别：
数学基础

项目证据：
MargAHRSupdate 使用 atan2f 和 asinf 从四元数乘积计算 ROLL/PITCH/YAW；evvgcCF.c 使用 atan2f 从加速度计算 roll/pitch 初始角。

相关文件：
Drivers/SRC/Src/MargAHRS.c
Drivers/SRC/Src/evvgcCF.c

相关函数：
MargAHRSupdate()
initOrientation()
getOrientation()

出现次数：
2

关联知识点：
* 三角函数
* 四元数
* 姿态角

后续需要深入讲解：
是

说明：
用于把传感器融合状态转为控制使用的姿态角。

---

# 互补滤波

类别：
滤波算法

项目证据：
evvgcCF.c 中 getOrientation 使用加速度角低通 smoothAcc，并用 orient[PITCH] = gyro积分 + 0.0002f*(smoothAcc[PITCH]-orient[PITCH]) 的形式融合陀螺仪和加速度计；Debug/objects.list 显示 evvgcCF.o 参与构建，但 Core/Src/main.c 当前主循环调用 MargAHRSupdate，未调用 getOrientation。

相关文件：
Drivers/SRC/Src/evvgcCF.c
Drivers/SRC/Inc/evvgcCF.h
Drivers/BGC/bgc32.h
Core/Src/main.c
Debug/objects.list

相关函数：
initOrientation()
getOrientation()

出现次数：
2

关联知识点：
* 陀螺仪积分
* 加速度角
* 一阶滤波

后续需要深入讲解：
是

说明：
作为项目中保留的姿态计算方案。

---

# 一阶低通滤波

类别：
滤波算法

项目证据：
firstOrderFilter.c 根据 eepromConfig 的 tau 计算 gx1/gx2/gx3，firstOrderFilter 根据 previousInput/previousOutput 输出滤波值；config.c 设置 accelX/Y/Z500HzLowPassTau 和姿态/速率指令低通时间常数；main.c 当前调用 initFirstOrderFilter 初始化滤波器状态。

相关文件：
Drivers/SRC/Src/firstOrderFilter.c
Drivers/SRC/Inc/firstOrderFilter.h
Drivers/SRC/Src/config.c
Core/Src/main.c

相关函数：
initFirstOrderFilter()
firstOrderFilter()
init_eepromConfig()

出现次数：
3

关联知识点：
* 时间常数
* 传感器平滑
* 数字滤波

后续需要深入讲解：
是

说明：
用于加速度和指令滤波配置。

---

# 正弦查表快速三角函数

类别：
数学基础

项目证据：
fastTrig.c 定义 sinDataI16[SINARRAYSIZE]，initSinArray 生成 1024 点正弦表，fastSin 用索引近似 sin；drv_pwmMotors.c 也生成 720 点正弦表用于 PWM。

相关文件：
Drivers/SRC/Src/fastTrig.c
Drivers/SRC/Inc/fastTrig.h
Drivers/CustomDrivers/Src/drv_pwmMotors.c

相关函数：
initSinArray()
fastSin()
GenerateSinTable()

出现次数：
3

关联知识点：
* 查表法
* 正弦函数
* 定点缩放

后续需要深入讲解：
是

说明：
用于减少运行时三角函数计算或生成三相 PWM 表。

---

# NaN与Inf防护

类别：
调试方法

项目证据：
computeMotorCommands.c、pid.c、MargAHRS.c 多处使用 isnan/isinf 检查 dt、角度、PID 输出、四元数归一化输入，并在异常时回退到安全值。

相关文件：
Drivers/SRC/Src/computeMotorCommands.c
Drivers/SRC/Src/pid.c
Drivers/SRC/Src/MargAHRS.c
tools/pid_tuning_sim.py

相关函数：
computeMotorCommands()
updatePID()
MargAHRSupdate()
run_and_plot()

出现次数：
4

关联知识点：
* 浮点数异常
* 控制安全
* 调试保护

后续需要深入讲解：
是

说明：
用于防止异常浮点值进入电机控制链路。

---

# 运行状态门控

类别：
软件架构

项目证据：
main 启动后先禁用 pitch/roll/yaw，等待 startup_delay_counter 达到 1000 后清 PID 状态并启用 pitch/yaw；return_state_count 和 return_state_count_roll 控制 roll/pitch 相关状态切换。

相关文件：
Core/Src/main.c
Drivers/SRC/Src/computeMotorCommands.c
Drivers/SRC/Inc/computeMotorCommands.h

相关函数：
main()
zeroPIDintegralError()
zeroPIDstates()
computeMotorCommands()

出现次数：
3

关联知识点：
* AHRS收敛
* PID状态清零
* 控制使能

后续需要深入讲解：
是

说明：
用于避免开机姿态未收敛时直接驱动电机。

---

# 全局配置结构eepromConfig_t

类别：
数据结构

项目证据：
mpu6050.h 定义 eepromConfig_t，包含温度补偿、PID、功率、使能、IMU方向、电机极对、速率限制、角度限位、滤波时间常数等字段；config.c 初始化默认值。

相关文件：
Drivers/CustomDrivers/Inc/mpu6050.h
Drivers/SRC/Src/config.c
Core/Src/main.c

相关函数：
init_eepromConfig()
main()
computeMotorCommands()
MPU6050_ComputeTemError()

出现次数：
3

关联知识点：
* 结构体
* 参数集中管理
* PID配置

后续需要深入讲解：
是

说明：
用于集中保存云台控制参数。

---

# 传感器数据结构sensors_t

类别：
数据结构

项目证据：
bgc32.h 定义 sensors_t，包含 accel500Hz、evvgcCFAttitude500Hz、margAttitude500Hz、gyro500Hz、mag10Hz；main.c 定义全局 sensors 并在控制循环中写入 accel500Hz/gyro500Hz；MargAHRS.c 写入 margAttitude500Hz；evvgcCF.c 写入 evvgcCFAttitude500Hz；computeMotorCommands.c 读取 margAttitude500Hz 作为当前控制输入。

相关文件：
Drivers/BGC/bgc32.h
Core/Src/main.c
Drivers/SRC/Src/MargAHRS.c
Drivers/SRC/Src/evvgcCF.c
Drivers/SRC/Src/computeMotorCommands.c

相关函数：
main()
MargAHRSupdate()
getOrientation()

出现次数：
4

关联知识点：
* 结构体
* 全局状态
* 500Hz数据流

后续需要深入讲解：
是

说明：
用于承载传感器和姿态算法的运行数据。

---

# 枚举定义电机轴

类别：
数据结构

项目证据：
drv_pwmMotors.h 定义 MotorAxis_t 枚举，包含 MOTOR_ROLL、MOTOR_PITCH、MOTOR_YAW；PWM_Motor_SetAngle 根据枚举选择输出通道。

相关文件：
Drivers/CustomDrivers/Inc/drv_pwmMotors.h
Drivers/CustomDrivers/Src/drv_pwmMotors.c

相关函数：
PWM_Motor_SetAngle()
computeMotorCommands()

出现次数：
2

关联知识点：
* enum
* 三轴电机映射
* API参数设计

后续需要深入讲解：
否

说明：
用于区分三轴电机输出目标。

---

# 自动跟随/回中算法

类别：
算法知识

项目证据：
computeMotorCommands.c 定义 autoPan、YAP_DEADBAND、MOTORPOS2SETPNT、AUTOPANSMOOTH 等偏航自动跟随相关代码，但当前主控制路径未发现 autoPan 调用；当前实际运行证据主要是 return_state、return_state_roll、return_state_count、return_state_count_roll 与 main.c 10Hz 任务配合完成 Pitch/Roll 回中和使能切换。

相关文件：
Drivers/SRC/Src/computeMotorCommands.c
Core/Src/main.c

相关函数：
autoPan()
computeMotorCommands()

出现次数：
2

关联知识点：
* 死区
* 平滑滤波
* Yaw控制

后续需要深入讲解：
是

说明：
用于偏航自动跟随或回中控制逻辑。

---

# PID调参仿真工具

类别：
调试方法

项目证据：
tools/pid_tuning_sim.py 使用 tkinter 和 matplotlib 构建三轴 PID 仿真界面，解析固件中的 PID 默认值、输出限幅、rateLimit，并模拟二阶对象、阶跃和扰动。

相关文件：
tools/pid_tuning_sim.py

相关函数：
parse_firmware_defaults()
update_pid()
run_simulation()
PIDSimulatorApp.run_and_plot()

出现次数：
1

关联知识点：
* PID仿真
* Python GUI
* 参数解析
* 二阶系统模型

后续需要深入讲解：
是

说明：
用于离线调试 PID 参数。

---

# STM32CubeIDE工程配置

类别：
工程管理

项目证据：
.cproject 配置 MCU ARM GCC、Debug/Release、USE_HAL_DRIVER、STM32F103xE、include paths、链接脚本、Debug 使用 -g3 和 float printf/scanf；.mxproject 记录 CubeMX 生成文件、历史库文件、HeaderPath 和 CDefines；.project 声明 Eclipse 工程；.settings 保存语言和编码设置。

相关文件：
.cproject
.mxproject
.project
.settings/language.settings.xml
.settings/org.eclipse.cdt.core.prefs
.settings/org.eclipse.core.resources.prefs
.settings/stm32cubeide.project.prefs

相关函数：
无

出现次数：
7

关联知识点：
* STM32CubeIDE
* ARM GCC
* Include Path
* 编译宏

后续需要深入讲解：
是

说明：
用于 IDE 构建和源码索引。

---

# Makefile构建产物

类别：
工程管理

项目证据：
Debug/sources.mk 列出参与构建的源码目录；Debug/objects.list 列出所有目标文件，包括 Core、CustomDrivers、Drivers/SRC、HAL、USB 中间件；Debug 目录包含 elf、map、list、o、d、su、cyclo 文件。

相关文件：
Debug/sources.mk
Debug/objects.list
Debug/makefile
Debug/Three-axis_cloud_platformV2.map
Debug/Three-axis_cloud_platformV2.list

相关函数：
无

出现次数：
5

关联知识点：
* GNU Make
* 目标文件
* map文件
* 栈使用/复杂度产物

后续需要深入讲解：
是

说明：
用于确认实际参与编译的模块和构建输出。

---

# ST-LINK与SWD调试配置

类别：
调试方法

项目证据：
Three-axis_cloud_platformV2 Debug.launch 配置 ST-LINK、swd_mode=true、target remote、stopAt=main、verify_flash_download=true、reset_strategy=connect_under_reset、PROGRAM_NAME=Debug/Three-axis_cloud_platformV2.elf。

相关文件：
Three-axis_cloud_platformV2 Debug.launch
Three-axis_cloud_platformV2.ioc

相关函数：
无

出现次数：
2

关联知识点：
* SWD
* ST-LINK
* GDB远程调试
* Flash下载验证

后续需要深入讲解：
是

说明：
用于固件下载和在线调试。

---

# USB Device中间件

类别：
中间件

项目证据：
Middlewares/ST/STM32_USB_Device_Library/Core 提供 usbd_core、usbd_ctlreq、usbd_ioreq；Class/CDC 提供 usbd_cdc；Debug/objects.list 显示这些对象参与链接。

相关文件：
Middlewares/ST/STM32_USB_Device_Library/Core/Src/usbd_core.c
Middlewares/ST/STM32_USB_Device_Library/Core/Src/usbd_ctlreq.c
Middlewares/ST/STM32_USB_Device_Library/Core/Src/usbd_ioreq.c
Middlewares/ST/STM32_USB_Device_Library/Class/CDC/Src/usbd_cdc.c
Debug/objects.list

相关函数：
USBD_Init()
USBD_RegisterClass()
USBD_LL_SetupStage()
USBD_CDC_Init()

出现次数：
5

关联知识点：
* USB协议栈
* CDC类驱动
* 中间件分层

后续需要深入讲解：
是

说明：
用于 USB CDC 功能的协议栈实现。

---

# STM32 HAL驱动库

类别：
驱动层

项目证据：
Drivers/STM32F1xx_HAL_Driver/Src 中 GPIO、I2C、TIM、UART、PCD、RCC、DMA、CORTEX、FLASH、PWR、EXTI 等源文件存在；Debug/objects.list 显示对应 .o 参与链接；Core/Inc/stm32f1xx_hal_conf.h 启用对应模块。

相关文件：
Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_gpio.c
Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_i2c.c
Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_tim.c
Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_uart.c
Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_pcd.c
Debug/objects.list

相关函数：
HAL_Init()
HAL_GPIO_Init()
HAL_I2C_Init()
HAL_TIM_PWM_Init()
HAL_UART_Init()
HAL_PCD_Init()

出现次数：
6

关联知识点：
* HAL API
* 外设句柄
* MSP回调

后续需要深入讲解：
是

说明：
用于项目各外设驱动。

---
