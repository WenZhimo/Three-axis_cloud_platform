# 第22章 滤波入口与重力校正

> 导航：上一章：[第21章_传感器标定零偏与物理量缩放](第21章_传感器标定零偏与物理量缩放.md) ｜ 下一章：[第23章_姿态表示与姿态解算](第23章_姿态表示与姿态解算.md)

## 1. 本章目标

第21章已经把方向处理后的 `rawAccel/rawGyro` 转换为 `sensors.accel500Hz` 和 `sensors.gyro500Hz`。进入姿态解算之前，项目还出现了两类容易混在一起的处理：一类是 `一阶低通滤波`，另一类是 `加速度计重力校正`。

本章目标是：

- 理解一阶低通滤波的基本目的：抑制快速变化和测量噪声。
- 定位 `firstOrderFilter.c` 中滤波器参数初始化和滤波公式。
- 明确当前主线只调用 `initFirstOrderFilter()`，没有在 500Hz 加速度处理路径中调用 `firstOrderFilter()`。
- 理解 `加速度计重力校正` 的最小原理：用加速度测得的重力方向修正陀螺仪积分漂移。
- 定位 `MargAHRSupdate()` 中加速度归一化、重力方向估计和 `KpAcc` 比例修正。
- 区分 MPU6050 内部 DLPF、项目软件一阶低通和 AHRS 重力反馈三条不同路径。

本章知识链为：

`知识点总表` 中的 `一阶低通滤波`、`加速度计重力校正`
-> `知识依赖图` 中 `一阶低通滤波` 依赖 `全局配置结构eepromConfig_t` 和 `传感器物理量缩放`，`加速度计重力校正` 依赖 `传感器物理量缩放` 和 `静态零偏估计`
-> `学习优先级` 中 P2 项目关键知识
-> `教学顺序` 第37、38项
-> `教材章节` 第22章。

本章阅读分层：

| 阅读层次 | 建议范围 | 适合读者 |
|---|---|---|
| 【必须掌握】 | 第1节到第5节，第13节总结 | 需要区分一阶低通、硬件DLPF和加速度计重力校正主线的读者 |
| 【工程深化】 | 第6节到第8.9节，第9节调试方法 | 需要维护滤波初始化、AHRS加速度输入、归一化和 `KpAcc` 修正路径的读者 |
| 【拓展阅读】 | 第4.4节，第7.2节，第8.4.1节，第10节到第12节 | 需要进一步理解数学库开销、软件浮点、硬件DLPF边界和主线调用差异的读者 |
| 【证据与验证】 | 第7.1节到第7.2节、第8.1节到第8.10节、第9节、章节尾部固定检查，以及所有 `【待验证】` 项 | 需要审查滤波器初始化、软件滤波主线调用边界、MPU6050硬件DLPF、AHRS加速度输入、数学库/软件浮点、构建产物分层、运行时滤波证据或准备姿态/噪声实测的读者 |

如果只是沿姿态解算前处理主线学习，可以先抓住“缩放后的传感器量 -> 当前主线未见软件一阶低通调用 -> AHRS内部重力反馈修正”这条链；排查滤波是否生效或实时预算时，再回到构建证据、数学库和调试方法小节。

## 2. 前置知识

本章正式前置知识包括：

- `全局配置结构eepromConfig_t`
- `传感器物理量缩放`
- `静态零偏估计`

第23章会正式讲 `四元数姿态解算`，因此本章不会推导完整四元数更新公式。本章只讲到一个必要边界：`MargAHRSupdate()` 内部已有当前姿态状态，并能由该状态计算“当前估计的重力方向”；加速度计提供“当前测得的重力方向”。二者之间的误差用于修正陀螺仪输入。

## 3. 问题背景

传感器进入姿态解算前有两个现实问题。

第一，原始传感器数据即使已经缩放，也会有噪声。如果直接把每一帧的快速抖动交给控制算法，后续姿态和 PID 都可能出现不必要的高频变化。一阶低通滤波用于让信号变化更平滑。

第二，陀螺仪适合短时间积分，但有漂移。加速度计能观测重力方向，但容易受线性加速度影响。云台静止或缓慢运动时，加速度向量主要反映重力方向，因此可以用它对陀螺仪积分方向做修正。

当前项目的实际情况比较微妙：

- `firstOrderFilter.c` 中确实实现了一阶低通滤波器。
- `Core/Src/main.c` 也在启动阶段调用了 `initFirstOrderFilter()`。
- 但当前 500Hz 主循环没有调用 `firstOrderFilter()` 处理 `sensors.accel500Hz`。
- `Core/Src/main.c` 调用 `MPU6050_Init(..., DLPF_CFG_260HZ)`，硬件 DLPF 配置属于 MPU6050 初始化路径。
- 当前真正进入主线的是 `MargAHRSupdate()` 里的加速度重力校正。

因此教材必须避免两种误写：不能把“滤波器模块存在”写成“当前加速度数据已经经过该软件滤波器”；也不能把“软件滤波未调用”写成“传感器链路完全没有任何滤波配置”。

还要注意源码注释和真实调用之间的边界。`Core/Src/main.c` 在调用
`MPU6050_Read_And_Process()` 前有“初步处理（如滤波）”的注释，但注释本身不是调用证据。
当前应以函数调用关系为准：这一行调用的是 MPU6050 读取与方向矩阵处理路径，不是
`firstOrderFilter()`。

## 4. 核心概念

### 4.1 一阶低通滤波

一阶低通滤波器的作用是保留较慢变化，削弱较快变化。它常用于传感器数据平滑、指令平滑和微分项噪声抑制。

项目中的滤波器状态结构是：

```c
typedef struct firstOrderFilterData
{
    float gx1;
    float gx2;
    float gx3;
    float previousInput;
    float previousOutput;
} firstOrderFilterData_t;
```

其中：

- `gx1/gx2/gx3` 是滤波系数。
- `previousInput` 是上一帧输入。
- `previousOutput` 是上一帧输出。

### 4.2 时间常数tau

`Drivers/SRC/Src/config.c` 中设置了多个滤波时间常数，例如：

```c
eepromConfig.accelX500HzLowPassTau = 0.1f;
eepromConfig.accelY500HzLowPassTau = 0.1f;
eepromConfig.accelZ500HzLowPassTau = 0.1f;
```

`tau` 越大，滤波越平滑，但响应越慢；`tau` 越小，响应越快，但噪声抑制更弱。

连续时间的一阶低通滤波器常写成：

```text
H(s) = 1 / (tau * s + 1)
```

其中 `s` 是拉普拉斯变量，`tau` 是时间常数。它表达的是：输入中的低频成分更容易通过，
高频成分会被削弱。

项目中的 `firstOrderFilter.c` 不是直接运行连续时间公式，而是把它离散化为可在 500Hz
采样循环中执行的差分方程。当前系数形式对应 Tustin，也叫双线性变换的一阶低通写法：

```text
a = 2 * tau * fs

y[n] = 1 / (1 + a) * x[n]
     + 1 / (1 + a) * x[n-1]
     - (1 - a) / (1 + a) * y[n-1]
```

和项目变量对应关系为：

```text
gx1 = 1 / (1 + a)
gx2 = 1 / (1 + a)
gx3 = (1 - a) / (1 + a)
```

所以 `firstOrderFilter()` 中的 `- gx3 * previousOutput` 不是额外多出来的一项，
而是差分方程反馈项的符号约定。

### 4.3 加速度计重力校正

加速度计在只受重力影响时，测得的是重力方向。姿态算法内部也能根据当前姿态状态估计一个重力方向。

如果这两个方向不一致，说明仅靠陀螺仪积分得到的姿态方向正在偏离重力观测。项目用叉乘形式计算误差：

```text
exAcc, eyAcc, ezAcc
```

然后把误差乘以 `eepromConfig.KpAcc`，加回陀螺仪角速度：

```c
gx += exAcc * eepromConfig.KpAcc;
gy += eyAcc * eepromConfig.KpAcc;
gz += ezAcc * eepromConfig.KpAcc;
```

这一步不是低通滤波，而是姿态估计中的反馈修正。

### 4.4 三种容易混淆的处理

本章标题里有“滤波入口”，但当前项目里至少有三种容易混用的概念：

| 名称 | 发生位置 | 当前主线证据 | 作用边界 |
| --- | --- | --- | --- |
| MPU6050 DLPF | 传感器内部 `REG_CONFIG` | `MPU6050_Init(..., DLPF_CFG_260HZ)` | 影响芯片输出的数据路径 |
| 软件一阶低通 | `firstOrderFilter()` | 仅初始化，当前未处理 `sensors.accel500Hz` | 平滑软件变量 |
| 加速度重力校正 | `MargAHRSupdate()` | 500Hz 主循环实际调用 | 用方向误差修正陀螺仪输入 |

这三者不能互相替代。硬件 DLPF 是否达到预期带宽，需要寄存器写入成功、手册参数和运行证据；软件一阶低通是否进入主线，需要函数调用证据；重力校正是否改善姿态，需要姿态日志或外部基准【待验证】。

## 5. 工作原理

当前项目中，第22章相关路径分为启动初始化和 500Hz 运行两部分。

启动阶段：

1. `Core/Src/main.c` 调用 `init_eepromConfig(true)`。
2. `Drivers/SRC/Src/config.c` 设置加速度滤波、指令滤波和姿态指令滤波的时间常数。
3. `Core/Src/main.c` 调用 `initFirstOrderFilter()`。
4. `initFirstOrderFilter()` 根据时间常数计算 `firstOrderFilters[]` 中多个滤波器的系数和初值。
5. `Core/Src/main.c` 调用 `MPU6050_Init(ACCEL_FS_4G, GYRO_FS_500DPS, DLPF_CFG_260HZ)`。
6. `MPU6050_Init()` 把第三个实参写入 `REG_CONFIG`，这是 MPU6050 内部 DLPF 配置路径。

500Hz 运行阶段：

1. `Core/Src/main.c` 调用 `MPU6050_Read_And_Process()`。
2. `Core/Src/main.c` 把原始整数缩放为 `sensors.accel500Hz` 和 `sensors.gyro500Hz`。
3. 当前代码没有调用 `firstOrderFilter()` 处理这些加速度值。
4. `Core/Src/main.c` 调用 `MargAHRSupdate()`。
5. `MargAHRSupdate()` 对输入加速度向量求模并归一化。
6. `MargAHRSupdate()` 根据当前姿态状态计算估计重力方向 `vx/vy/vz`。
7. `MargAHRSupdate()` 用加速度观测方向与估计方向计算误差 `exAcc/eyAcc/ezAcc`。
8. 误差乘以 `KpAcc` 后修正 `gx/gy/gz`。

本章只讲第5至第8步的重力校正，不展开后续四元数积分和欧拉角输出。

## 6. STM32实现机制

本章处理完全发生在 CPU 软件层，没有新增 STM32 外设。

严格说，软件一阶低通和 AHRS 重力校正在 STM32 CPU 上执行；MPU6050 DLPF 则发生在传感器芯片内部。STM32 只通过 I2C 在初始化阶段写入 `REG_CONFIG`，并不能在后续 500Hz 循环里证明 DLPF 的实时效果。

一阶低通滤波器使用普通浮点运算。STM32F103RCTx 是 Cortex-M3 内核，没有硬件 FPU，因此这些浮点运算由编译器和运行库生成软件浮点代码。当前项目仍采用这种实现，说明滤波和 AHRS 代码是软件算法层，不依赖特殊外设。

一阶滤波器的系数初始化发生在系统启动阶段，调用顺序位于 `Core/Src/main.c`：

```c
init_eepromConfig(true);
PWM_Motor_Init();
initFirstOrderFilter();
initPID();
```

加速度重力校正发生在 500Hz 主循环内。`MargAHRSupdate()` 的输入 `dt500Hz` 来自第21章之后的实时循环时间计算，它决定后续姿态更新步长。本章不展开实时循环调度，后面第26章会专门讲。

从性能角度看，如果 `firstOrderFilter()` 被调用，一次调用主要包含 3 次浮点乘法、2 次浮点加减
和 2 次状态写回，计算量较小。真正需要重点关注的是 `MargAHRSupdate()`，因为它包含 `sqrt`、
倒数、归一化、叉乘、四元数更新和四元数归一化等软件浮点计算。仓库中 `Core/Src/main.c`
已经维护 `executionTime500Hz`，因此是否满足 500Hz 实时预算，应以 DWT 或运行日志记录为准；
没有实测数据时只能写成【待验证】。

源码里还有一个性能审查细节：加速度模长使用 `sqrt(...)`，四元数模长使用 `sqrtf(...)`。
在 C 语言中 `sqrt` 是 double 版本，可能引入 float 到 double 再回到 float 的转换成本；
当前 `.list/.map` 已能确认这里确实出现 `__aeabi_f2d -> sqrt -> __aeabi_d2f` 链路，但真实耗时
仍取决于运行路径、库实现和编译选项，需要 DWT 或运行日志实测【待验证】。

## 7. 项目中的应用

本章涉及的项目证据文件包括：

- `Drivers/SRC/Src/firstOrderFilter.c`
- `Drivers/SRC/Inc/firstOrderFilter.h`
- `Drivers/SRC/Src/config.c`
- `Drivers/SRC/Src/MargAHRS.c`
- `Drivers/SRC/Inc/MargAHRS.h`
- `Core/Src/main.c`

`firstOrderFilter.c` 定义 `firstOrderFilters[]`、`initFirstOrderFilter()` 和 `firstOrderFilter()`。

`config.c` 设置滤波时间常数、`KpAcc` 和 `KiAcc`。

`mpu6050.h` 定义 `DLPF_CFG_260HZ`、`DLPF_CFG_184HZ` 等硬件 DLPF 宏。

`mpu6050.c` 中 `MPU6050_Init()` 把 `dlpf` 实参写入 `REG_CONFIG`。

`Core/Src/main.c` 调用 `initFirstOrderFilter()` 和 `MargAHRSupdate()`，但当前没有调用 `firstOrderFilter()`。

`Core/Src/main.c` 调用 `MPU6050_Init()` 时直接传入 `DLPF_CFG_260HZ`，没有使用 `eepromConfig.dlpfSetting`。

`MargAHRS.c` 中的 `MargAHRSupdate()` 是当前主线加速度重力校正的实际入口。

### 7.1 构建产物证据边界

除源码调用关系外，当前 Debug 构建产物还能进一步约束本章结论。

`Debug/Three-axis_cloud_platformV2.map` 中可以看到 `LOAD ./Drivers/SRC/Src/firstOrderFilter.o`，说明一阶滤波源码对象参与了链接；同一文件中 `initFirstOrderFilter` 位于 `0x08004f44`，`MargAHRSupdate` 位于 `0x08003938`，`firstOrderFilters` 位于 `.bss` 的 `0x20000e00`，`accelOneG` 位于 `.data` 的 `0x20000004`。这些证据可以证明滤波器系数初始化函数、AHRS 更新入口、滤波器状态数组和静态标定得到的 `accelOneG` 符号都进入了当前 Debug 构建。

但 `.map` 里也能看到一个反向边界：`.text.firstOrderFilter` 和 `.text.calculateAccConfidence` 均出现在 `0x00000000` 的输入段，而不是带有 Flash 最终地址的输出段。因此，构建产物支持的说法应写成“函数源码存在并参与对象文件/调试信息，但当前没有被保留为最终可执行主线路径”，不能写成当前 500Hz 加速度主线已经调用了 `firstOrderFilter()` 或 `calculateAccConfidence()`。

`Debug/Three-axis_cloud_platformV2.list` 与源码可以互相印证两件事：一是 `main()` 启动阶段调用 `initFirstOrderFilter()`，反汇编中也能看到对 `firstOrderFilters[ACCEL_X/Y/Z_500HZ_LOWPASS]` 以及部分 pitch 指向滤波槽位的系数和历史值初始化；二是 500Hz 主循环调用 `MargAHRSupdate(...)` 时直接传入 `sensors.gyro500Hz[]` 与 `sensors.accel500Hz[]`，没有在该调用前插入 `firstOrderFilter()` 的返回值。

#### 7.1.1 `.list`中的源码行与机器指令要分开读

这一点还可以再往下拆一层。`.list` 文件不是纯反汇编，它会把源码行、注释行和机器指令交织在一起显示。对本章来说，这意味着“看见某个滤波器索引或赋值文本”不等于“看见它已经编译成最终可执行指令”。

以 `initFirstOrderFilter()` 为例：

- `ACCEL_X/Y/Z_500HZ_LOWPASS` 的系数和初值在 `.list` 中都有带地址的指令段，说明这三个槽位确实进入了当前 Debug 镜像。
- `PITCH_RATE_POINTING_50HZ_LOWPASS` 和 `PITCH_ATT_POINTING_50HZ_LOWPASS` 也有带地址的指令段，说明它们同样被编译成了可执行初始化路径。
- `ROLL_RATE_POINTING_50HZ_LOWPASS`、`YAW_RATE_POINTING_50HZ_LOWPASS`、`ROLL_ATT_POINTING_50HZ_LOWPASS` 和 `YAW_ATT_POINTING_50HZ_LOWPASS` 的相关源码行在 `.list` 中可以出现，但它们在当前文件里只保留为被注释或被跳过的文本，没有对应的最终可执行指令地址；这只能说明源码文本存在，不能说明这些槽位已经被初始化为有效滤波器。

这个区分很重要，因为教材里很容易把“`.list` 中能搜到某个名字”误读成“运行主线真的走了那段代码”。对当前项目来说，只有带地址的行才能支撑“进入最终镜像并生成指令”的结论；纯文本注释或被注释掉的赋值，只能证明源码曾经写过类似路径，不能证明它现在仍在主线里执行【待验证】。

`.su/.cyclo` 文件还能补充静态栈用量和圈复杂度边界：

- `initFirstOrderFilter()`：静态栈 24 字节，圈复杂度 1。
- `firstOrderFilter()`：静态栈 32 字节，圈复杂度 1。
- `MargAHRSupdate()`：静态栈 80 字节，圈复杂度 33。
- `calculateAccConfidence()`：静态栈 24 字节，圈复杂度 1。
- `main()`：静态栈 56 字节，圈复杂度 7。

这些数字要和 `.map/.list` 分层使用。`initFirstOrderFilter()` 和 `MargAHRSupdate()` 既有最终地址、
调用跳转和静态分析条目，可以支撑“初始化路径”和“重力校正入口”已经进入当前 Debug 构建。
`firstOrderFilter()` 与 `calculateAccConfidence()` 虽然也有 `.su/.cyclo` 条目，但 `.map` 显示它们位于
`0x00000000` 输入段，没有最终 Flash 地址；因此只能说明编译器分析过这些源函数，不能说明当前 500Hz
主线实际调用了软件一阶低通或加速度置信度函数。`MargAHRSupdate()` 的圈复杂度 33 也只提示分支复杂度较高，
不能直接推出某次运行一定进入了 `norm > 0.1f` 的加速度校正分支。

因此，本章基于构建产物能确认的是“初始化路径进入构建、AHRS 重力校正入口进入构建、当前主线加速度输入未见软件一阶低通调用证据”。构建产物不能证明 MPU6050 内部 DLPF 的真实带宽效果、I2C 配置一定成功、强振动下姿态一定改善、滤波函数运行时断点一定命中，也不能替代 DWT 计时、串口日志、姿态基准或硬件实测；`.su/.cyclo` 也不能替代运行时栈水位、真实耗时或具体分支覆盖记录。这些结论缺少证据时仍保持【待验证】。

### 7.2 数学库与软件浮点证据边界

`MargAHRSupdate()` 的工程代价不能只从源码行数判断。当前 Debug 构建产物还能证明以下数学库和软件浮点链路：

- `.map` 显示 `./Drivers/SRC/Src/MargAHRS.o` 引入了 `asinf`、`atan2f`、`sqrtf`、`cosf` 和 `__aeabi_fcmpun`。
- `.map` 中这些 `libm.a`、`libgcc.a` 路径位于 `thumb/v7-m/nofp` 目录，说明当前构建链接的是面向 Cortex-M3 无硬件浮点路径的库实现。
- `.list` 中 `norm = sqrt(ax*ax + ay*ay + az*az)` 对应先调用 `__aeabi_f2d`，再调用 `sqrt`，最后调用 `__aeabi_d2f`；这印证了源码中使用 double 版 `sqrt` 会带来 float/double 转换链路。
- `.list` 中四元数归一化 `1.0f / sqrtf(norm_q)` 对应 `sqrtf` 和 `__aeabi_fdiv`；欧拉角输出还会触发 `atan2f` 与 `asinf` 相关路径。

这些证据可以支撑两个教学结论。第一，`MargAHRSupdate()` 比普通一阶低通更值得做 500Hz 性能审查，因为它不仅有乘加和分支，还会拉入数学库函数和软件浮点辅助函数。第二，把 `sqrt` 改成 `sqrtf`、减少欧拉角输出频率或拆分姿态更新与输出，都是可以讨论的优化方向，但本章不能直接写成“已经优化”或“必然超时”。是否超出实时预算仍要看 `executionTime500Hz`、DWT 计时或示波/日志证据，缺少实测时保持【待验证】。

## 8. 代码分析

### 8.1 firstOrderFilters数组

`Drivers/SRC/Src/firstOrderFilter.c` 定义：

```c
firstOrderFilterData_t firstOrderFilters[NUMBER_OF_FIRST_ORDER_FILTERS];
```

`Drivers/SRC/Inc/firstOrderFilter.h` 中 `NUMBER_OF_FIRST_ORDER_FILTERS` 为 9，并定义了多个索引：

```c
ACCEL_X_500HZ_LOWPASS
ACCEL_Y_500HZ_LOWPASS
ACCEL_Z_500HZ_LOWPASS
PITCH_RATE_POINTING_50HZ_LOWPASS
PITCH_ATT_POINTING_50HZ_LOWPASS
```

部分 Roll/Yaw 指令滤波初始化代码在当前文件中被注释掉。教材不能把所有 9 个滤波器都说成当前已经完整初始化并进入主线。

更细地看，`NUMBER_OF_FIRST_ORDER_FILTERS = 9` 只是数组容量和索引编号，并不等于 9 个滤波器都已经具有有效系数。当前可执行初始化路径可以分成三类：

| 索引 | 当前初始化状态 | 默认 `tau` 来源 | 教学边界 |
| --- | --- | --- | --- |
| `ACCEL_X/Y/Z_500HZ_LOWPASS` | 已初始化 | `0.1s` | 系数可手算，但当前未进入 500Hz 加速度主线 |
| `PITCH_RATE_POINTING_50HZ_LOWPASS` | 已初始化 | `0.0s` | `a=0`，`gx1=gx2=gx3=1`，基本不体现低通平滑 |
| `PITCH_ATT_POINTING_50HZ_LOWPASS` | 已初始化 | `0.25s` | 只说明滤波槽位被初始化，不证明结果进入控制主线 |
| `ROLL_RATE/YAW_RATE/ROLL_ATT/YAW_ATT` | 初始化代码被注释 | 配置字段存在 | 全局数组槽位会因静态存储期先清零，但这不是有效滤波器系数 |

因此，调试时不能只看“数组有 9 个元素”。必须同时看具体索引的初始化代码是否处于未注释状态、对应 `tau` 是否为 0、以及该索引是否真的被传给 `firstOrderFilter()`。

### 8.2 initFirstOrderFilter的系数计算

以 X 轴加速度滤波器为例：

```c
a = 2.0f * eepromConfig.accelX500HzLowPassTau * 500.0f;
gx1 = 1.0f / (1.0f + a);
gx2 = 1.0f / (1.0f + a);
gx3 = (1.0f - a) / (1.0f + a);
```

这里的 `500.0f` 对应 500Hz 采样链路。`accelX500HzLowPassTau` 来自 `config.c` 默认配置。

代入当前默认值 `tau = 0.1s` 和 `fs = 500Hz`：

```text
a = 2 * 0.1 * 500 = 100

gx1 = 1 / (1 + 100) = 1 / 101 ~= 0.00990099
gx2 = 1 / (1 + 100) = 1 / 101 ~= 0.00990099
gx3 = (1 - 100) / (1 + 100) = -99 / 101 ~= -0.980198
```

因为运行公式是：

```c
output = gx1 * input + gx2 * previousInput - gx3 * previousOutput;
```

所以在这个默认配置下，上一帧输出的实际系数是 `-gx3 ~= +0.980198`。这意味着该滤波器
一旦被调用，会强烈依赖历史输出，信号会比较平滑，但阶跃响应也会更慢。这里仍然只是
滤波器系数初始化结论，不能推出当前 500Hz 加速度主线已经使用该输出。

Z 轴滤波器的初值是：

```c
previousInput  = -9.8065f;
previousOutput = -9.8065f;
```

这个初值假设 Z 轴初始接近一个重力加速度方向。但当前第20章和第21章已经指出，项目的实际安装方向、Z 轴正负约定和缩放链路存在需要结合仓库外实测继续验证的边界。因此这里不能单独证明硬件静止时 Z 轴一定为 `-9.8065f`，只能说明滤波器初值这样设置。

### 8.3 firstOrderFilter公式

`firstOrderFilter()` 的输出公式是：

```c
output = gx1 * input + gx2 * previousInput - gx3 * previousOutput;
```

随后更新历史状态：

```c
previousInput = input;
previousOutput = output;
```

这是一种离散一阶滤波实现。它依赖上一帧输入和上一帧输出，所以调试时不能只看当前输入；必须同时观察 `previousInput/previousOutput`。

如果用第8.2节的默认 X 轴系数做一次手算，假设：

```text
input = 9.8065
previousInput = 0
previousOutput = 0
```

则第一帧输出为：

```text
output = 0.00990099 * 9.8065
       + 0.00990099 * 0
       - (-0.980198) * 0
       ~= 0.09709
```

这说明一阶滤波器若从零状态接收一个接近 1g 的阶跃输入，第一帧不会立刻跳到
`9.8065`。它会逐步靠近输入值。调试时要把这个现象和“传感器本身读数错误”区分开。
但再次强调：当前加速度主线没有调用 `firstOrderFilter()`，所以这个手算用于理解函数，
不能作为当前 AHRS 输入已经变平滑的运行证据。

### 8.4 当前主线未调用firstOrderFilter

全工程搜索当前调用点，能看到：

- `Core/Src/main.c` 调用 `initFirstOrderFilter()`。
- `Drivers/SRC/Src/firstOrderFilter.c` 定义 `firstOrderFilter()`。

但当前 500Hz 主循环没有调用：

```c
firstOrderFilter(...)
```

因此，本章对 `一阶低通滤波` 的结论必须谨慎：

- 项目存在通用一阶低通滤波器。
- 项目启动时初始化了滤波器系数。
- 当前主线的加速度输入没有经过 `firstOrderFilter()`。
- 第28章会再讲 PID D 项内部的独立一阶低通形式，那不是这里这个 `firstOrderFilter()` 函数的直接调用。

### 8.4.1 MPU6050硬件DLPF边界

第18章已经分析过 MPU6050 初始化。这里需要把它和软件一阶低通再分开一次。

`Core/Src/main.c` 当前调用：

```c
MPU6050_Init(ACCEL_FS_4G, GYRO_FS_500DPS, DLPF_CFG_260HZ);
```

`Drivers/CustomDrivers/Src/mpu6050.c` 中把第三个实参写入 `REG_CONFIG`：

```c
status = MPU_Write_Reg(REG_CONFIG, dlpf);
```

所以当前项目可以证明“初始化路径尝试配置 MPU6050 内部 DLPF”。但这不是 `firstOrderFilter()`，也不是 `MargAHRSupdate()` 的加速度重力校正。

还要注意，`Drivers/SRC/Src/config.c` 中存在：

```c
eepromConfig.dlpfSetting = DLPF_CFG_184HZ;
```

但当前 `main.c` 没有把 `eepromConfig.dlpfSetting` 作为 `MPU6050_Init()` 的第三个实参，而是硬编码传入 `DLPF_CFG_260HZ`。
因此，不能把配置对象里的 `DLPF_CFG_184HZ` 写成当前真实硬件 DLPF 设置。

本章只记录这条边界：软件一阶低通未进入 500Hz 加速度主线，不等于 MPU6050 初始化没有 DLPF 配置；硬件 DLPF 配置存在，也不等于软件滤波器输出已经进入 AHRS。

#### 8.4.2 DLPF配置字段、实参与寄存器写入边界

硬件 DLPF 还要再拆成四个层级，否则容易把“配置结构里有字段”和“芯片真实已经按该带宽工作”混成一件事。

| 层级 | 当前证据 | 能说明什么 | 不能说明什么 |
|---|---|---|---|
| 配置对象字段 | `config.c` 写 `eepromConfig.dlpfSetting = DLPF_CFG_184HZ` | 项目配置结构预留了 DLPF 字段，默认值是 `184Hz` 编号 | 当前 `MPU6050_Init()` 一定使用该字段 |
| 初始化实参 | `main.c` 调用 `MPU6050_Init(..., DLPF_CFG_260HZ)` | 当前主线传入的是 `DLPF_CFG_260HZ`，而不是 `eepromConfig.dlpfSetting` | I2C 写寄存器一定成功 |
| 寄存器写入路径 | `mpu6050.c` 执行 `MPU_Write_Reg(REG_CONFIG, dlpf)` | 初始化函数会尝试把第三个实参写入 `REG_CONFIG` | MPU6050 内部真实滤波带宽已经通过读回或波形验证 |
| 构建产物 | `.list` 显示 `main()` 调用 `MPU6050_Init(..., DLPF_CFG_260HZ)`，并显示 `MPU_Write_Reg(REG_CONFIG, dlpf)` 路径 | 当前 Debug 构建包含这条初始化写入路径 | 现场硬件一定收到该写入，或 DLPF 对噪声/延迟的实际效果已达预期 |

因此，本章可以写“当前源码和 Debug 构建证明主线尝试用 `DLPF_CFG_260HZ` 配置 `REG_CONFIG`”。
但不能写“`eepromConfig.dlpfSetting = DLPF_CFG_184HZ` 已经生效”，也不能写“传感器输出已经被实测验证为 260Hz DLPF 效果”。
若要闭合硬件 DLPF 结论，至少还需要 `REG_CONFIG` 读回、I2C 写入状态记录、频响或噪声对比记录；缺少这些证据时保持【待验证】。

### 8.5 MargAHRSupdate的加速度输入

`Core/Src/main.c` 调用：

```c
MargAHRSupdate(sensors.gyro500Hz[ROLL],
               sensors.gyro500Hz[PITCH],
               sensors.gyro500Hz[YAW],
               sensors.accel500Hz[XAXIS],
               sensors.accel500Hz[YAXIS],
               sensors.accel500Hz[ZAXIS],
               0, 0, 0, false, dt500Hz);
```

这说明当前 `MargAHRSupdate()` 使用的是第21章缩放后的加速度和陀螺仪。磁力计输入均为 0，`magDataUpdate` 为 `false`，所以本章不讲磁力计修正。

#### 8.5.1 AHRS输入来源的最小核对表

`MargAHRSupdate()` 的实参应逐项看来源，而不是只看函数名：

| 实参组 | 当前来源 | 能证明什么 | 不能证明什么 |
|---|---|---|---|
| `gx/gy/gz` | `sensors.gyro500Hz[]`，来自第21章的 `(rawGyro - gyroRTBias - gyroTCBias) * GYRO_SCALE_FACTOR` | AHRS 当前使用缩放到 `rad/s` 目标层的陀螺仪输入 | 不能证明零偏估计质量或陀螺仪漂移已经被实测消除 |
| `ax/ay/az` | `sensors.accel500Hz[]`，来自第21章的 `(rawAccel - accelTCBias) * ACCEL_SCALE_FACTOR` | AHRS 当前使用缩放到 `m/s^2` 目标层的加速度输入 | 不能证明加速度已经经过软件 `firstOrderFilter()`，也不能证明线性加速度可忽略 |
| `mx/my/mz` | `main.c` 固定传入 `0, 0, 0` | 当前调用点没有提供磁力计观测 | 不能把本路径写成九轴磁航向修正 |
| `magDataUpdate` | `main.c` 固定传入 `false` | 当前主线不声明本帧有新磁力计数据 | 不能证明磁力计分支已被运行覆盖 |
| `dt` | `dt500Hz` | 后续四元数积分使用本帧软件时间步长 | 不能证明 MPU6050 样本时间戳与该步长严格一致 |

这张表把第21章的缩放链路和第23章的姿态积分入口接起来：第22章能证明 AHRS 收到哪些软件变量，不能证明这些变量已经经过所有可能的滤波、置信度调权或硬件同步验证。

### 8.6 加速度归一化

`MargAHRSupdate()` 首先计算加速度模长：

```c
norm = sqrt(ax*ax + ay*ay + az*az);
```

若 `norm > 0.1f`，则执行：

```c
normR = 1.0f / norm;
ax *= normR;
ay *= normR;
az *= normR;
```

这一步把加速度向量变成单位方向向量。换句话说，重力校正关心的是方向，而不是加速度大小本身。

如果 `norm <= 0.1f`，当前函数会跳过加速度归一化和重力误差修正，但后续四元数仍会用陀螺仪
输入继续更新。结合第19章的读失败清零路径看，读失败或异常小的加速度向量可能让本帧退化为
“仅陀螺更新”。这能避免除以接近零的模长，但不能把该帧标记为有效重力观测。

这个 `0.1f` 是一个二值门控，而不是连续置信度。当前逻辑可以写成：

```text
if ||a|| <= 0.1:
    不做加速度重力校正
else:
    a_meas = a / ||a||
    用固定 KpAcc 做方向误差修正
```

一旦 `norm > 0.1f`，加速度模长本身就被归一化消掉了。也就是说，`norm = 9.8`、`norm = 12` 或 `norm = 20` 在进入方向校正后都会变成单位向量，当前主线没有根据“偏离 1g 的程度”自动降低 `KpAcc`。这不是数学错误，而是当前实现的工程取舍：它用硬阈值避免除零，却没有实现连续加速度可信度调权。

### 8.7 估计重力方向与误差

`MargAHRSupdate()` 根据当前姿态状态计算估计重力方向：

```c
vx = 2.0f * (q1q3 - q0q2);
vy = 2.0f * (q0q1 + q2q3);
vz = q0q0 - q1q1 - q2q2 + q3q3;
```

随后计算误差：

```c
exAcc = vy * az - vz * ay;
eyAcc = vz * ax - vx * az;
ezAcc = vx * ay - vy * ax;
```

这组式子可以理解为“估计重力方向”和“测得加速度方向”之间的方向误差。完整姿态数学推导留到第23章，本章只需要知道：误差越大，说明当前姿态状态与加速度观测越不一致。

用向量写法可以更清楚：

```text
a_meas = [ax, ay, az] / ||[ax, ay, az]||
v_est  = [vx, vy, vz]
e      = v_est x a_meas
```

叉乘展开后就是：

```text
exAcc = vy * az - vz * ay
eyAcc = vz * ax - vx * az
ezAcc = vx * ay - vy * ax
```

如果 `v_est` 和 `a_meas` 方向相同，叉乘结果接近零，说明加速度观测不需要明显修正
当前姿态。如果二者存在夹角，叉乘方向给出修正轴，叉乘模长随夹角增大而增大。
这也是 Mahony 类非线性互补滤波中常见的重力参考方向误差构造方式。

还可以把这一步拆成一个更小的数学单元。`a_meas` 经过归一化后是单位向量；
`v_est` 来自上一时刻单位四元数对应的估计重力方向，在四元数归一化正常的前提下也可视作单位向量。
若二者夹角为 `theta`，则：

```text
|e| = |v_est x a_meas|
    = |v_est| * |a_meas| * sin(theta)
    ~= sin(theta)
```

因此 `exAcc/eyAcc/ezAcc` 不是加速度大小误差，而是方向误差。夹角很小时，
`sin(theta) ~= theta`，误差向量可以近似表示姿态方向的小角度偏差；夹角接近 90 度时，
误差模长接近 1。这个结论只描述方向误差的几何含义，不能直接证明当前姿态输出已经准确，
因为真实运行还受到陀螺零偏、采样周期、线性加速度和四元数积分误差影响。

这里还有一个工程边界：归一化后的加速度只保留方向，不保留模长异常信息。当云台快速平移、
强振动或受到冲击时，加速度方向可能不再主要代表重力方向。当前 `MargAHRSupdate()` 没有
调用 `calculateAccConfidence()` 动态降低 `KpAcc`，所以强线性加速度下的修正可靠性需要
结合实测日志标记为【待验证】。

### 8.8 KpAcc比例修正

误差被乘以 `eepromConfig.KpAcc` 后加到陀螺仪输入：

```c
gx += exAcc * eepromConfig.KpAcc;
gy += eyAcc * eepromConfig.KpAcc;
gz += ezAcc * eepromConfig.KpAcc;
```

`config.c` 中默认：

```c
eepromConfig.KpAcc = 0.98f;
eepromConfig.KiAcc = 0.02f;
```

但当前 `MargAHRSupdate()` 只使用 `KpAcc`。文件中虽然定义了 `exAccInt/eyAccInt/ezAccInt` 和 `kiAcc` 相关变量，但当前主线没有用 `KiAcc` 做加速度积分校正。不能把 `KiAcc` 写成当前已生效参数。

这里需要避免把 `KpAcc` 误讲成一阶低通滤波系数。`exAcc/eyAcc/ezAcc` 是单位向量叉乘得到的方向误差，
本身没有加速度单位；`gx/gy/gz` 是传入四元数积分的角速度量。当前代码把
`exAcc * KpAcc` 直接加到 `gx`，所以在这个实现语义下，`KpAcc` 更像“每单位方向误差产生多少角速度修正”的比例增益。
如果把弧度看作无量纲量，它的有效量纲可理解为 `1/s`；从调试读数角度看，也可以记成
“rad/s per unit direction error”。

以默认 `KpAcc = 0.98f` 做数量级估计：当 `v_est` 与 `a_meas` 夹角约 90 度时，
`|e| ~= 1`，加到陀螺输入的修正向量模长量级约为 `0.98 rad/s`。这只是由源码公式得到的
教学估算，不等同于真实运行中每一轴都会出现 `0.98 rad/s` 的固定修正；实际三轴分量取决于
误差轴方向、当前四元数状态和输入数据。

这个设计有一个可以教学化的权衡：

- 只使用 `KpAcc`，实现简单，反馈路径短，不会因为错误加速度观测持续积分而形成额外偏置。
- 不使用 `KiAcc`，意味着当前主线没有通过加速度误差在线估计并抵消陀螺零偏的积分环节。
- 如果未来启用 `KiAcc`，必须同时处理积分限幅、加速度可信度和运动状态判断，否则线性加速度
  可能被误当成重力误差并累积到陀螺修正中。

因此本章把当前方案称为“加速度比例重力校正”更准确；把它写成完整 PI 型 Mahony 修正会
超过当前仓库证据。

### 8.9 calculateAccConfidence当前未进入主线

`MargAHRS.c` 中存在：

```c
void calculateAccConfidence(float accMag)
```

它会使用 `accelOneG` 计算加速度置信度。但当前全工程没有发现 `calculateAccConfidence()` 的调用点。

第21章留下的 `accelOneG` 边界在这里得到进一步澄清：`accelOneG` 确实被 `MargAHRS.c` 中的保留函数使用，但该函数当前没有进入 `MargAHRSupdate()` 主线。

更细地看，`calculateAccConfidence()` 试图构造的是“连续置信度”：

```c
accMag /= accelOneG;
accMag  = HardFilter(accMagP, accMag);
accConfidence = constrain(1.0f - (accConfidenceDecay * sqrt(fabs(accMag - 1.0f))),
                          0.0f, 1.0f);
```

它和 `norm > 0.1f` 的差别很大：

| 机制 | 当前是否进入主线 | 判断依据 | 对 `KpAcc` 的影响 |
|---|---|---|---|
| `norm > 0.1f` | 是 | 加速度模长是否大于低阈值 | 只决定是否执行重力校正 |
| `calculateAccConfidence()` | 否 | 加速度模长相对 `accelOneG` 偏离多少 | 理论上可用于连续降低修正强度，但当前没有接入 |

再结合第21章：静态估计后的 `accelOneG` 是 5000 次 LSB 累加模长，而当前 `MargAHRSupdate()` 中的 `norm` 来自 `sensors.accel500Hz`，单位目标接近 `m/s^2`。因此即使未来想接入 `calculateAccConfidence()`，也不能只在 `MargAHRSupdate()` 里直接调用；必须先统一 `accMag` 和 `accelOneG` 的单位层级，并明确 `accConfidence` 如何参与 `KpAcc` 或误差项。

### 8.10 本节证据边界

本节只根据当前仓库说明文件、函数、宏、变量和调用关系。运行时频率、外部硬件表现、主机侧现象、传感器方向、电机响应或真实控制效果仍需调试记录、日志或仓库外实测证据；缺少证据时保持【待验证】。

## 9. 调试方法

本节按统一调试结构组织：现象 -> 可能原因 -> 定位方法 -> 验证步骤 -> 解决方案 -> 经验总结。对第22章而言，核心是把一阶低通初始化、软件滤波是否进入主线、MPU6050 硬件 DLPF、`MargAHRSupdate()` 输入、加速度归一化、重力误差修正和加速度可信度边界分开验证。

### 9.1 现象与可能原因

常见现象包括滤波器系数已经初始化但加速度看起来仍未滤波、`firstOrderFilter()` 断点不命中、硬件 DLPF 与软件一阶低通被混淆、`MargAHRSupdate()` 输入异常、`norm <= 0.1f` 导致重力修正不执行、强振动下姿态修正不可靠，或 `calculateAccConfidence()` 被误认为当前主线已经接入。可能原因包括主循环未调用 `firstOrderFilter()`、`eepromConfig.dlpfSetting` 未接入 `MPU6050_Init()` 实参、前序缩放或读取失败导致 AHRS 输入无效、当前加速度可信度只有硬阈值门控，或缺少姿态日志/外部基准验证。

### 9.2 定位方法：滤波器初始化、主线调用和硬件 DLPF

第一步，确认滤波器初始化。

- 在 `Core/Src/main.c` 的 `initFirstOrderFilter()` 调用处设置断点。
- 单步进入 `Drivers/SRC/Src/firstOrderFilter.c`。
- 观察 `firstOrderFilters[ACCEL_X_500HZ_LOWPASS].gx1/gx2/gx3`。
- 对照 `eepromConfig.accelX500HzLowPassTau = 0.1f` 计算系数。

第二步，确认当前是否调用滤波函数。

- 在 `firstOrderFilter()` 函数入口设置断点。
- 运行主循环，如果断点没有命中，记录“当前 500Hz 主线未调用该函数”。
- 不要仅凭 `initFirstOrderFilter()` 已运行就判定加速度已滤波。

第三步，确认 MPU6050 DLPF 配置路径。

- 在 `Core/Src/main.c` 中确认 `MPU6050_Init()` 的第三个实参。
- 在 `MPU6050_Init()` 中确认 `REG_CONFIG` 写入使用的是 `dlpf` 实参。
- 对照 `config.c` 中的 `eepromConfig.dlpfSetting`，记录它当前没有接入该实参。
- 不能把硬件 DLPF 配置写成软件 `firstOrderFilter()` 已启用。

### 9.3 验证步骤：AHRS输入和加速度归一化

第四步，观察 `MargAHRSupdate()` 输入。

- 在 `Core/Src/main.c` 调用 `MargAHRSupdate()` 前设置断点。
- 记录 `sensors.accel500Hz` 和 `sensors.gyro500Hz`。
- 确认输入来自第21章缩放链路。

第五步，观察加速度归一化。

- 在 `MargAHRSupdate()` 中观察 `norm`。
- 如果 `norm <= 0.1f`，重力校正分支不会执行。
- 如果 `norm > 0.1f`，观察归一化后的 `ax/ay/az`。
- 记录 `norm` 偏离约 `9.8065 m/s^2` 的程度，但不要把这种偏离写成当前主线会自动降低 `KpAcc`；
  当前主线只做 `norm > 0.1f` 二值门控。
- 若前序 I2C 读取失败并导致原始量被清零，应把本帧记录为“重力观测无效或待验证”，
  不能把姿态继续更新误读为加速度修正仍然有效。

### 9.4 验证步骤：重力误差和修正量

第六步，观察重力误差和修正量。

- 观察 `vx/vy/vz`、`exAcc/eyAcc/ezAcc`。
- 观察 `gx/gy/gz` 在 `KpAcc` 修正前后的变化。
- 计算 `dot = vx * ax + vy * ay + vz * az`，在调试记录中把它限制到 `[-1, 1]` 后估算
  `theta = acos(dot)`，再比较 `sqrt(exAcc^2 + eyAcc^2 + ezAcc^2)` 与 `sin(theta)` 是否同量级。
- 用 `KpAcc * sqrt(exAcc^2 + eyAcc^2 + ezAcc^2)` 估算本帧加速度方向误差带来的角速度修正量级，
  再和 `gx/gy/gz` 修正前后的差值对照。
- 如果 `KpAcc` 改为 0，理论上这一步对陀螺仪输入不再产生加速度修正。

### 9.5 解决方案：四类结论分离

第七步，分离四类结论。

- “系数已经初始化”只需要 `initFirstOrderFilter()` 和 `gx1/gx2/gx3` 证据。
- “函数输出符合预期”需要单步进入 `firstOrderFilter()` 或写临时测试调用。
- “滤波结果进入主线”必须证明 `firstOrderFilter()` 的返回值被传入后续 AHRS 或控制链路。
- “硬件 DLPF 已配置”必须证明 `MPU6050_Init()` 实参和 `REG_CONFIG` 写入路径。

这四类结论不能互相替代。当前仓库可以稳定支持第一类和第四类结论；
第二类可以通过调试临时验证；第三类在当前 500Hz 加速度路径中没有调用证据。
即便讨论第一类“系数已经初始化”，也要落实到具体索引：部分索引当前被注释掉，部分索引虽然初始化但默认 `tau=0`，不能笼统写成“9 个滤波器均已有效初始化”。

### 9.6 解决方案：调试记录

- 记录滤波器初始化调用、配置 `tau`、系数 `gx1/gx2/gx3` 和当前主线调用搜索结果。
- 对 9 个滤波器索引，应分别记录“已初始化 / 注释未初始化 / tau 为 0”的状态，避免把数组容量当成有效滤波器数量。
- 对重力校正，应记录 AHRS 输入、加速度模长、归一化结果、误差项和修正前后陀螺仪值。
- 对加速度可信度，应分开记录“当前硬阈值已生效”和“`calculateAccConfidence()` 连续置信度未接入”。
- 若 `firstOrderFilter()` 未命中断点，应写成“初始化存在、主线调用未确认”，不能写成已滤波。
- 加速度修正效果、噪声改善和姿态稳定性需要运行日志或外部姿态基准，缺失时标记为【待验证】。
- 对硬件 DLPF，应记录 `DLPF_CFG_260HZ`、`REG_CONFIG` 写入和 `eepromConfig.dlpfSetting` 未接入的边界。

### 9.7 经验总结

第22章的调试结论应按“滤波器系数初始化 -> 软件滤波主线调用 -> 硬件 DLPF 配置 -> AHRS 输入 -> 加速度方向误差 -> 陀螺仪修正”逐层收敛。当前仓库能证明 `initFirstOrderFilter()` 和 `MargAHRSupdate()` 进入主线，也能证明 `firstOrderFilter()` 与 `calculateAccConfidence()` 没有当前主线调用证据；但噪声改善、姿态稳定性、强振动下重力观测可信度和硬件 DLPF 实际带宽仍需要运行日志、DWT 或外部姿态基准验证。缺少证据时，不把“初始化存在”写成“滤波已经生效”。

## 10. 常见问题

问题一：当前项目是否已经对加速度使用 `firstOrderFilter()`。

当前没有发现主循环调用 `firstOrderFilter()` 的证据。项目初始化了滤波器参数，但当前加速度主线直接把 `sensors.accel500Hz` 传给 `MargAHRSupdate()`。

问题二：`initFirstOrderFilter()` 有意义吗。

有意义，它说明项目保留了通用一阶滤波器配置和状态初始化。
后续如果恢复或新增调用，就可以直接使用这些滤波参数。
但当前教材必须区分“初始化存在”和“运行时使用”。
初始化滤波器不等于当前加速度或姿态指令已经经过该函数滤波。

问题二补充：源码注释写了“如滤波”，是否可以证明已经滤波。

不能。注释可以提示作者意图，但教材审查要以调用关系为准。当前 500Hz 主线调用
`MPU6050_Read_And_Process()` 后直接缩放并传入 `MargAHRSupdate()`，没有调用
`firstOrderFilter()`。

问题三：`KpAcc` 和 `KiAcc` 是否都生效。

当前 `MargAHRSupdate()` 只使用 `KpAcc` 修正 `gx/gy/gz`。`KiAcc` 在配置中存在，但当前加速度积分校正没有进入主线。

问题四：`calculateAccConfidence()` 是否影响当前姿态更新。

当前没有调用证据。它使用 `accelOneG` 计算置信度，但不在当前 `MargAHRSupdate()` 主线中。

问题五：加速度归一化是不是物理量缩放。

不是。物理量缩放已经在第21章完成。这里的归一化是把加速度向量变为单位方向向量，用于比较重力方向。

问题六：本章是否已经讲完姿态解算。

没有。本章只讲加速度重力校正入口。四元数积分、归一化和欧拉角输出在第23章展开。

问题七：加速度重力校正是不是等同于一阶滤波。

不是。
一阶滤波是对信号做平滑，通常关注噪声和响应速度的折中。
本章的重力校正是把加速度方向与四元数估计的重力方向比较，
再用误差修正陀螺仪积分。
二者都能改善姿态估计，但作用对象和数学含义不同。

问题八：当前没有调用 `firstOrderFilter()`，是否表示项目完全没有滤波。

不能这样写。
当前 500Hz 加速度软件路径没有调用 `firstOrderFilter()`，这是软件滤波主线结论。
但 MPU6050 初始化时传入了 `DLPF_CFG_260HZ`，并在 `MPU6050_Init()` 中写入 `REG_CONFIG`。
所以更准确的说法是：软件一阶低通没有进入当前加速度主线；传感器内部 DLPF 配置属于另一路硬件初始化证据。

问题九：`eepromConfig.dlpfSetting = DLPF_CFG_184HZ` 是否是当前硬件 DLPF 设置。

当前不能这么说。
`main.c` 调用 `MPU6050_Init()` 时直接传入 `DLPF_CFG_260HZ`，没有把 `eepromConfig.dlpfSetting` 传进去。
因此当前真实初始化实参以 `main.c` 为准，配置对象字段只能说明存在一个尚未接入当前调用路径的配置值。

问题十：`KpAcc = 0.98f` 是否表示类似“一阶滤波 0.98 系数”？

不能这样理解。当前代码中 `KpAcc` 乘的是单位向量叉乘得到的方向误差，然后直接加到陀螺角速度输入。
它描述的是重力方向误差对角速度修正的比例强度，而不是对输入信号做历史加权平均的一阶低通系数。
如果把它误当成低通系数，就会把“信号平滑”和“姿态误差反馈”两类不同机制混在一起。

问题十一：加速度模长明显偏离 1g 时，当前 AHRS 会自动降低修正权重吗。

当前主线没有这个证据。`MargAHRSupdate()` 只检查 `norm > 0.1f`。
一旦通过阈值，它会把加速度向量归一化，并用固定 `KpAcc` 做方向误差修正。
`calculateAccConfidence()` 看起来是为连续置信度准备的保留函数，但当前没有调用点，也没有看到 `accConfidence` 参与 `KpAcc` 或 `exAcc/eyAcc/ezAcc` 的计算。
因此强线性加速度、振动或冲击下的加速度修正可靠性，必须通过运行日志和外部姿态基准继续验证。

## 11. 实践任务

开始任务前，先回到本章第8节定位一阶滤波器、当前主线调用边界、加速度归一化和重力误差修正；第9节提供滤波与重力校正调试顺序。

任务一：验证滤波器系数。

用 `eepromConfig.accelX500HzLowPassTau = 0.1f` 和采样频率 500Hz，计算 `a = 2 * tau * 500`，再计算 `gx1/gx2/gx3`，与调试器中的值比较。
验收依据是系数表包含手算值、调试器值、初始化证据和 500Hz 主线调用边界。

任务二：确认 `firstOrderFilter()` 当前调用情况。

在 `firstOrderFilter()` 入口设置断点，运行 500Hz 主循环，记录断点是否命中，并解释这个结果对“当前加速度是否经过一阶低通滤波”的含义。
验收依据是调用记录表包含搜索结果、断点命中结果、主线结论和禁止外推结论。

任务三：观察加速度重力校正。

在 `MargAHRSupdate()` 中观察 `norm`、`ax/ay/az`、`vx/vy/vz`、`exAcc/eyAcc/ezAcc`，记录一次完整校正过程。
验收依据是校正记录表分列归一化加速度、估计重力方向、误差向量、`dot`、`theta`、`|e|` 和非滤波结论。

任务四：验证 `KpAcc` 影响。

通过调试器把 `eepromConfig.KpAcc` 临时设为 0，观察 `gx/gy/gz` 在误差计算后是否仍被加速度误差修正。
调试后恢复原值，不把该临时观察写入源码。
验收依据是对比表包含 `KpAcc=0`、原值、`exAcc/eyAcc/ezAcc`、`KpAcc * |e|` 和 `gx/gy/gz` 变化。

任务五：记录保留函数边界。

查找 `calculateAccConfidence()` 的调用点，记录当前是否进入主线，并说明为什么不能把它作为当前加速度重力校正的一部分讲解。
验收依据是保留函数表包含函数定义、调用搜索结果、当前主线结论和待接入边界。

任务五补充：验证硬阈值与连续置信度的区别。

在 `MargAHRSupdate()` 中记录多帧 `norm`、是否进入 `norm > 0.1f` 分支、`KpAcc` 实际值和 `gx/gy/gz` 修正量。
同时搜索 `accConfidence` 是否参与 `KpAcc` 或误差项计算。
验收依据是表格能说明当前主线只有低模长硬阈值，没有按偏离 1g 程度连续降低加速度修正权重。

任务六：手算一次滤波器阶跃响应。

使用 `input = 9.8065`、`previousInput = 0`、`previousOutput = 0` 和第8.2节默认系数，
计算第一帧 `output`。然后说明这个数值只能验证滤波器差分方程，不能证明当前 AHRS
输入已经经过滤波。
验收依据是计算过程同时列出输入、历史状态、系数、输出和证据边界。

任务七：区分硬件 DLPF 与软件一阶低通。

画出三条路径：`MPU6050_Init() -> REG_CONFIG`、`initFirstOrderFilter() -> firstOrderFilters[]`、`MargAHRSupdate() -> KpAcc`。
分别写出每条路径的发生位置、当前调用证据和不能外推的结论。
验收依据是表格明确写出 `DLPF_CFG_260HZ` 是硬件初始化实参，`firstOrderFilter()` 当前未进入 500Hz 加速度主线。

实践边界：

当前任务优先形成表格、链路图、搜索记录和计算过程。涉及 IDE 现场、构建日志、断点数值、外部波形、主机侧结果或硬件响应时，若没有截图、日志或仓库外实测证据，结论保持【待验证】。

## 12. 思考题

1. 为什么只调用 `initFirstOrderFilter()` 不能说明传感器数据已经被滤波。
2. 一阶低通滤波的时间常数增大，会给噪声和响应速度带来什么变化。
3. 为什么加速度重力校正要先把加速度向量归一化。
4. 如果云台受到明显线性加速度，加速度计重力校正可能产生什么风险。
5. 当前 `KiAcc` 没有进入主线，这对长期漂移修正意味着什么。
6. 为什么第22章只讲重力校正入口，而不提前推导完整四元数姿态解算。
7. 如何区分“滤波器参数已初始化”、“滤波函数被调用”和“滤波结果进入姿态主线”？
8. 为什么软件 `firstOrderFilter()` 未调用，不能推出 MPU6050 内部 DLPF 没有配置？
9. 为什么 `eepromConfig.dlpfSetting` 存在，仍然不能直接当作当前硬件 DLPF 实参？
10. 为什么 `KpAcc` 不能按一阶低通滤波系数理解？如何用 `|e| ~= sin(theta)` 估算修正量级？
11. 为什么 `norm > 0.1f` 是硬阈值，而不是加速度可信度的连续调权？
12. 如果未来要启用 `calculateAccConfidence()`，为什么必须先统一 `accelOneG` 与 `norm` 的单位层级？

## 13. 本章总结

本章完成了第21章传感器输入到第23章姿态解算之间的过渡。

当前项目中：

- `firstOrderFilter.c` 实现了通用一阶低通滤波器。
- `Core/Src/main.c` 启动阶段调用 `initFirstOrderFilter()`。
- 当前 500Hz 主循环没有调用 `firstOrderFilter()` 处理加速度输入。
- `main.c` 中“如滤波”的注释不能替代 `firstOrderFilter()` 调用证据。
- MPU6050 初始化路径写入了 `REG_CONFIG`，当前 `main.c` 实参是 `DLPF_CFG_260HZ`。
- `eepromConfig.dlpfSetting = DLPF_CFG_184HZ` 当前没有接入 `MPU6050_Init()` 实参。
- 当前源码和 Debug 构建只能证明主线尝试用 `DLPF_CFG_260HZ` 写 `REG_CONFIG`；
  是否写入成功、是否读回一致、硬件 DLPF 效果是否符合预期仍需运行或硬件证据。
- 当前默认 `tau = 0.1s`、`fs = 500Hz` 时，一阶低通系数为 `gx1 = gx2 ~= 0.00990099`、
  `gx3 ~= -0.980198`，上一帧输出的实际系数约为 `+0.980198`。
- `NUMBER_OF_FIRST_ORDER_FILTERS = 9` 不代表 9 个滤波器都已有效初始化：当前 Roll/Yaw 相关部分初始化代码被注释，`PITCH_RATE_POINTING_50HZ_LOWPASS` 虽被初始化但默认 `tau=0`。
- `MargAHRSupdate()` 是当前主线加速度重力校正的实际入口。
- `MargAHRSupdate()` 对加速度向量归一化，计算估计重力方向 `vx/vy/vz`，再计算 `exAcc/eyAcc/ezAcc` 修正陀螺仪输入。
- 当加速度模长 `norm <= 0.1f` 时，重力修正分支跳过，后续姿态更新退化为陀螺仪主导。
- `exAcc/eyAcc/ezAcc` 可以写成 `v_est x a_meas`，即估计重力方向与测得加速度方向的叉乘误差。
- 在单位向量前提下，叉乘误差模长满足 `|e| ~= sin(theta)`，它描述方向夹角而不是加速度模长误差。
- `KpAcc` 当前是方向误差到角速度修正的比例增益，不是一阶低通滤波系数；默认值 `0.98f`
  对应的最大修正量级可用 `KpAcc * |e|` 做调试估算。
- 当前只使用 `KpAcc` 做比例修正，`KiAcc` 未进入加速度积分校正主线。
- `calculateAccConfidence()` 存在，但当前没有调用证据。
- `MargAHRSupdate()` 对加速度可信度只使用 `norm > 0.1f` 硬阈值，没有连续降低 `KpAcc` 的证据。
- `calculateAccConfidence()` 依赖 `accelOneG`，但当前没有看到它参与主线重力校正。
- `.map/.list/.su/.cyclo` 的构建产物结论统一回到第7.1节到第7.2节判断：它们能证明滤波初始化、AHRS 重力校正入口、数学库与软件浮点辅助路径、静态栈和圈复杂度条目进入某次 Debug 构建；其中静态分析条目不能替代最终链接地址或主线调用证据，也不能替代 DWT 实测耗时、运行时栈水位或具体分支覆盖记录。

目标回扣：

- 本章已经把一阶低通滤波和加速度计重力校正分开，避免把两个概念混成同一条主线。
- 本章已经补充 MPU6050 内部 DLPF、软件一阶低通和 AHRS 重力反馈三者的边界。
- 本章已经确认当前 500Hz 加速度输入没有进入 `firstOrderFilter()`，实际姿态修正入口在 `MargAHRSupdate()`。
- 本章已经说明当前加速度可信度是硬阈值门控，不是连续调权。
- 本章只闭合姿态解算前的输入修正目标，四元数更新和欧拉角输出留给第23章。
- 本章不把 `.su/.cyclo` 中的 `firstOrderFilter()` 或 `calculateAccConfidence()` 条目写成当前 500Hz 主线执行证据。
- 本章不把数学库符号进入构建写成真实运行耗时结论；实时余量仍需 DWT 或运行日志证明。

本章保留以下边界：

- 不把 `firstOrderFilter()` 写成当前加速度主线已启用滤波。
- 不把 `firstOrderFilters[]` 的 9 个槽位写成 9 个滤波器都已有效初始化并进入主线。
- 不把 `eepromConfig.dlpfSetting` 写成当前硬件 DLPF 实参。
- 不把 `REG_CONFIG` 写入路径写成硬件 DLPF 已经通过读回、频响或噪声实测验证。
- 不把硬件 DLPF 配置写成软件一阶低通已经进入 AHRS。
- 不展开四元数姿态解算，只讲加速度重力校正为第23章提供的输入修正。
- 不把 `.su/.cyclo` 静态分析结果写成运行时栈水位、真实耗时或分支覆盖证明。

下一章将进入 `姿态表示与姿态解算`，正式分析四元数状态、陀螺仪积分、归一化和欧拉角输出。

本章新增参考资料：

- R. Mahony, T. Hamel, J.-M. Pflimlin, `Nonlinear Complementary Filters on the Special Orthogonal Group`。
- A. V. Oppenheim, R. W. Schafer, `Discrete-Time Signal Processing` 中双线性变换相关内容。
- TDK/InvenSense `MPU-6000 and MPU-6050 Register Map and Descriptions, Revision 4.2` 中 `CONFIG` / `DLPF_CFG` 相关内容。
- 本仓库 `Drivers/SRC/Src/firstOrderFilter.c`。
- 本仓库 `Drivers/SRC/Src/MargAHRS.c`。
- 本仓库 `Drivers/SRC/Src/config.c`。
- 本仓库 `Drivers/CustomDrivers/Src/mpu6050.c`。
- 本仓库 `Drivers/CustomDrivers/Inc/mpu6050.h`。
- 本仓库 `Core/Src/main.c`。
- 本仓库 `Debug/Three-axis_cloud_platformV2.map` 和 `Debug/Three-axis_cloud_platformV2.list`。
- 本仓库 `Debug/Drivers/SRC/Src/firstOrderFilter.su` 和 `Debug/Drivers/SRC/Src/firstOrderFilter.cyclo`。
- 本仓库 `Debug/Drivers/SRC/Src/MargAHRS.su` 和 `Debug/Drivers/SRC/Src/MargAHRS.cyclo`。
- 本仓库 `Debug/Core/Src/main.su` 和 `Debug/Core/Src/main.cyclo`。

### 章节尾部固定检查

知识链路：

`知识点总表`
-> `知识依赖图`
-> `学习优先级`
-> `教学顺序`
-> `教材章节`

项目证据：

- `Drivers/SRC/Src/firstOrderFilter.c`
- `Drivers/SRC/Inc/firstOrderFilter.h`
- `Drivers/SRC/Src/MargAHRS.c`
- `Drivers/CustomDrivers/Src/mpu6050.c`
- `Drivers/CustomDrivers/Inc/mpu6050.h`
- `Core/Src/main.c`
- `Debug/Three-axis_cloud_platformV2.map`
- `Debug/Three-axis_cloud_platformV2.list`
- `Debug/Drivers/SRC/Src/firstOrderFilter.su`
- `Debug/Drivers/SRC/Src/firstOrderFilter.cyclo`
- `Debug/Drivers/SRC/Src/MargAHRS.su`
- `Debug/Drivers/SRC/Src/MargAHRS.cyclo`
- `Debug/Core/Src/main.su`
- `Debug/Core/Src/main.cyclo`

质量自检：

- P0 事实错误：通过
- P1 依赖断层：通过
- P2 逻辑连贯：通过
- P3 项目证据：通过
- P4 原理展开：通过
- P5 调试实践：通过
- P6 表达统一：通过

---
> 导航：上一章：[第21章_传感器标定零偏与物理量缩放](第21章_传感器标定零偏与物理量缩放.md) ｜ 下一章：[第23章_姿态表示与姿态解算](第23章_姿态表示与姿态解算.md)
