# 第03章 启动流程与内存布局

> 导航：上一章：[第02章_STM32CubeIDE工程结构](第02章_STM32CubeIDE工程结构.md) ｜ 下一章：[第04章_HAL工程裁剪与构建产物](第04章_HAL工程裁剪与构建产物.md)

## 1. 本章目标

- 建立复位、启动文件、链接脚本和 `main()` 入口之间的结构关系。
- 看懂链接脚本如何描述 FLASH、RAM、栈、堆和关键段位置。
- 看懂 `Reset_Handler` 如何调用 `SystemInit()`、初始化 `.data`、清零 `.bss`、调用 C 运行时初始化并进入 `main()`。
- 为后续 Newlib 适配、堆栈检查、中断系统和运行时初始化建立前置基础。

本章阅读分层：

| 阅读层次 | 建议范围 | 适合读者 |
|---|---|---|
| 【必须掌握】 | 第1节到第5节，第13节总结 | 需要理解复位后 CPU 如何经向量表、`Reset_Handler`、`.data/.bss` 初始化进入 `main()` 的读者 |
| 【工程深化】 | 第6节到第8节，第9节调试方法 | 需要维护链接脚本、启动文件、`SystemInit()`、`_sbrk()` 堆边界和 `main()` 入口关系的读者 |
| 【拓展阅读】 | 第8.1.1节到第8.2节，第8节中的堆栈边界说明，第10节常见问题 | 需要进一步理解 `KEEP(*(.isr_vector))`、VMA/LMA、`_Min_Heap_Size`、`_Min_Stack_Size` 和 C 运行时初始化边界的读者 |
| 【证据与验证】 | 第8节、第9节、章节尾部固定检查，以及所有 `【待验证】` 项 | 需要审查链接脚本入口、内存区域、栈/堆/段边界符号、复位入口、`SystemInit()`、`main()`、`.map/.list` 构建产物、PC/SP 读数、断点命中、内存窗口、下载日志和运行现场证据的读者 |

如果只是沿启动主线学习，可以先抓住“向量表给出初始栈顶和复位入口 -> `Reset_Handler` 准备 `.data/.bss` 和 C 运行时 -> 进入 `main()` -> 后续 HAL、时钟和外设初始化才开始”这条链；排查堆栈、段布局或真实启动异常时，再回到链接脚本、构建产物和调试方法小节。

本章速查：

| 查找目标 | 优先阅读 | 避免重复展开 |
|---|---|---|
| 复位后如何从向量表进入 `main()` | 第4节到第5节、第8.3节到第8.5节、第13节 | 平台与工程入口回到第01章、第02章 |
| 链接脚本、FLASH/RAM、栈堆和段边界符号 | 第6节、第8.1节到第8.2节 | 不把链接脚本预留空间写成运行时堆栈余量 |
| `.data/.bss` 初始化、C 运行时和 `SystemInit()` 边界 | 第8.2.1节到第8.4节、第10节 | 最终系统时钟配置继续到第06章 |
| `.map/.list`、PC/SP、断点和内存窗口验证 | 第8.6节到第9.6节、章节尾部固定检查 | 构建产物不能替代下载日志、断点命中或运行现场 |

## 2. 前置知识

- STM32F103RCTx芯片平台
- STM32CubeIDE工程配置
- 链接脚本与内存布局

这些前置主要对应第01章、第02章和第03章。

本章内部顺序为：先讲 `链接脚本与内存布局`，再讲 `Cortex-M3内核启动`。因为启动文件使用的 `_estack`、`_sidata`、`_sdata`、`_edata`、`_sbss` 和 `_ebss` 都来自链接脚本。

## 3. 问题背景

前两章已经确认项目目标平台和 STM32CubeIDE 工程配置。现在要回答一个更靠近运行时的问题：芯片复位后，程序是怎样从一片 FLASH 镜像走到 `main()` 的？

在普通 C 程序里，读者常常从 `main()` 开始看代码。但在 STM32 项目中，`main()` 不是 CPU 执行的第一条项目代码。复位后，CPU 先依赖向量表取得初始栈顶和复位入口，再进入启动文件。启动文件完成最小运行环境准备后，才会跳转到 `main()`。

本项目中，这条链路由四个文件共同证明：

- `STM32F103RCTX_FLASH.ld` 定义入口、内存区域和段布局。
- `Core/Startup/startup_stm32f103rctx.s` 定义向量表和 `Reset_Handler`。
- `Core/Src/system_stm32f1xx.c` 提供 `SystemInit()`。
- `Core/Src/sysmem.c` 提供 Newlib 堆增长入口 `_sbrk()`。
- `Core/Src/main.c` 提供应用入口 `main()`。

本章只讲启动链路和内存布局，不展开系统时钟树、外设初始化和业务循环。

## 4. 核心概念

- 链接脚本与内存布局：描述程序各段放入 FLASH/RAM 的规则，并向启动文件提供边界符号。
- Cortex-M3内核启动：复位后由向量表、初始栈顶、复位入口和启动代码共同完成的上电入口过程。
- 向量表：存放初始栈顶、复位入口和异常/中断入口地址的表。
- `Reset_Handler`：复位后进入的启动函数。
- `_estack`：链接脚本计算出的 RAM 最高地址，用作初始栈顶。
- `.data`：需要从 FLASH 拷贝到 RAM 的已初始化数据段；它同时具有 FLASH 中的加载地址和 RAM 中的运行地址。
- `.bss`：启动时需要清零的未初始化数据段。
- 加载地址与运行地址：加载地址是镜像烧录后初值所在的位置，运行地址是 CPU 读写变量时使用的位置。本项目 `.data` 的加载地址来自 FLASH，运行地址位于 RAM。
- C 运行时初始化：进入 `main()` 前执行库级初始化的步骤，本项目启动文件调用 `__libc_init_array`。
- `_end`：链接脚本在 `._user_heap_stack` 起点处导出的符号，通常表示静态 RAM 段结束后、Newlib 堆可开始增长的位置。
- `_Min_Heap_Size`：链接阶段要求至少预留的堆检查空间，本项目为 `0x200` 字节；它不是 `_sbrk()` 的运行时最大堆容量。
- `_Min_Stack_Size`：链接阶段要求至少预留的 MSP 栈检查空间，本项目为 `0x400` 字节；`sysmem.c` 还用它计算堆增长上限。
- `_sbrk()`：Newlib 的堆增长入口，`malloc()` 等库函数可能通过它从裸机工程申请堆空间。
- 运行时栈余量：程序实际运行后 MSP 栈还剩多少空间，必须结合水位、调试器或异常现场验证，不能只由链接脚本推出。

这些概念都来自第03章的两个正式知识点，不新增教材结构外知识点。

## 5. 工作原理

STM32 程序从复位到 `main()` 可以拆成两条互相咬合的线。

第一条线是链接线：

1. 链接脚本声明入口为 `Reset_Handler`。
2. 链接脚本定义 RAM 起始地址、RAM 长度、FLASH 起始地址和 FLASH 长度。
3. 链接脚本把 `.isr_vector`、`.text`、`.rodata` 放入 FLASH。
4. 链接脚本把 `.data` 的运行位置放入 RAM，同时记录其加载位置来自 FLASH。
5. 链接脚本把 `.bss` 放入 RAM，并导出起止符号。
6. 链接脚本预留堆和栈检查区域。

第二条线是启动线：

1. CPU 通过向量表取得 `_estack` 和 `Reset_Handler`。
2. `Reset_Handler` 先调用 `SystemInit()`。
3. `Reset_Handler` 使用 `_sidata`、`_sdata` 和 `_edata` 把 `.data` 初值从 FLASH 拷贝到 RAM。
4. `Reset_Handler` 使用 `_sbss` 和 `_ebss` 将 `.bss` 清零。
5. `Reset_Handler` 调用 `__libc_init_array`。
6. `Reset_Handler` 调用 `main()`。

这就是为什么链接脚本和启动文件必须一起读：链接脚本定义“数据在哪里”，启动文件执行“启动时怎么搬、怎么清、往哪里跳”。

这里还要补一个常被跳过的细节：`__libc_init_array` 不是“项目业务初始化函数”，而是 C 运行时初始化入口。当前链接脚本保留了 `.preinit_array`、`.init_array` 和 `.fini_array` 段，当前 Debug `map` 中 `.preinit_array` 为空、`.init_array` 只有 `crtbegin.o` 提供的 4 字节条目，`startup_stm32f103rctx.s` 则在清零 `.bss` 后显式调用 `__libc_init_array`。这说明本项目把 C 运行时初始化链路接进了启动流程，但当前证据还不能把它写成“项目已经定义了用户构造函数”或“业务对象已在此阶段自动完成初始化”。

这两条线之间还有一个容易被忽略的边界：`.data` 不是“全部在 FLASH”或“全部在 RAM”。它的初值必须随固件镜像存放在 FLASH 中，否则掉电后初值会丢失；但全局变量运行时需要被 CPU 修改，所以运行位置必须在 RAM 中。链接脚本用 `_sidata = LOADADDR(.data)` 记录 FLASH 初值位置，用 `.data >RAM AT> FLASH` 表示运行地址在 RAM、加载地址在 FLASH。启动文件再用 `_sidata`、`_sdata`、`_edata` 把这段初值搬到 RAM。这个动作完成后，C 代码才可以像普通变量一样读写这些全局初值。

`.bss` 的逻辑相反。未初始化全局变量不需要在 FLASH 镜像里保存一份全 0 数据，否则会浪费 FLASH 空间。链接脚本只需要给 `.bss` 分配 RAM 地址范围，启动文件在进入 `main()` 前把 `_sbss` 到 `_ebss` 清零。这样既满足 C 语言“静态存储期对象默认初始化为 0”的语义，又避免把大量 0 写进固件镜像。

## 6. STM32实现机制

本项目的 STM32 实现机制可以从三个层面确认。

第一层是内存区域：

- `STM32F103RCTX_FLASH.ld` 中 RAM 起始地址为 `0x20000000`，长度为 `48K`。
- `STM32F103RCTX_FLASH.ld` 中 FLASH 起始地址为 `0x8000000`，长度为 `256K`。
- `_estack = ORIGIN(RAM) + LENGTH(RAM)`，也就是把 RAM 末端作为初始栈顶。
- `_Min_Heap_Size = 0x200`，`_Min_Stack_Size = 0x400`，用于保留最小堆栈检查空间。

把 RAM 边界代入可得：`_estack = 0x20000000 + 48 * 1024 = 0x2000C000`。这不是一个“栈变量”，而是向量表第一项写入的初始栈指针值。CPU 复位后读取该值作为 MSP 的初始值，然后才读取第二项 `Reset_Handler` 作为复位入口。

第二层是段布局：

- `.isr_vector` 被保留在 FLASH，用于放置启动向量表。
- `.text` 和 `.rodata` 放在 FLASH，用于代码和只读数据。
- `.data` 运行在 RAM，但初始值来自 FLASH。
- `.bss` 运行在 RAM，启动时由启动文件清零。
- `._user_heap_stack` 用于检查 RAM 中是否还能容纳预留堆和栈。

这里的 `._user_heap_stack` 更像链接阶段的空间占位和越界检查，不等于运行时已经把所有栈空间“写满”或“固定占用”。实际栈从 `_estack` 附近向低地址增长，实际堆通常从 `_end` 之后向高地址增长；两者是否会碰撞，需要结合 map 文件、运行路径、调试器栈深度观察和动态分配行为继续验证。

还要注意，链接脚本中的 `_Min_Heap_Size` 和 `_Min_Stack_Size` 在运行期并不扮演完全相同的角色。`._user_heap_stack` 段把 `_end`、`_Min_Heap_Size` 和 `_Min_Stack_Size` 连在一起，目的是让链接器检查静态 RAM 使用后至少还能留下这两段空间。可是 `Core/Src/sysmem.c` 的 `_sbrk()` 只用 `_end` 作为堆起点，用 `_estack - _Min_Stack_Size` 作为最大堆边界；它没有把 `_Min_Heap_Size` 当作堆上限。因此 `_Min_Heap_Size = 0x200` 应理解为链接期最低保留检查值，而不是“本项目运行时最多只能 malloc 512 字节”。相反，`_Min_Stack_Size = 0x400` 同时出现在链接期检查和 `_sbrk()` 堆边界中，但它仍然只是预留下限，不是运行期真实栈峰值证明。

第三层是启动代码：

- `startup_stm32f103rctx.s` 声明 `.cpu cortex-m3`。
- `g_pfnVectors` 的前两项分别是 `_estack` 和 `Reset_Handler`。
- `Reset_Handler` 调用 `SystemInit()`。
- `Reset_Handler` 执行 `.data` 拷贝和 `.bss` 清零。
- `Reset_Handler` 调用 `__libc_init_array` 后进入 `main()`。
- `Default_Handler` 作为弱定义的默认处理入口，未被项目强定义替换的处理函数会落到默认入口。

当前 `Debug/Three-axis_cloud_platformV2.list` 还能从构建结果侧佐证段地址：`.isr_vector` 位于 `0x08000000`，`.data` 的运行地址位于 `0x20000000`、加载地址位于 `0x08015b70`，`.bss` 从 `0x20000370` 开始。构建产物属于当前 Debug 构建的结果证据，能用于交叉验证，但不应反过来替代链接脚本和源码作为配置来源。

注意：启动文件中的设备向量表包含芯片级入口，并不表示每个入口都已经成为本项目业务主线。正式功能分析必须等待对应章节。

## 7. 项目中的应用

本章对应项目中的运行入口层。

直接相关文件：

- `STM32F103RCTX_FLASH.ld`
- `Core/Startup/startup_stm32f103rctx.s`
- `Core/Src/system_stm32f1xx.c`
- `Core/Src/sysmem.c`
- `Core/Src/main.c`

它们之间的关系是：

- `.cproject` 在第02章已经确认链接脚本指向 `STM32F103RCTX_FLASH.ld`。
- 链接脚本定义入口和内存布局，并提供启动文件使用的符号。
- 启动文件中的向量表使用 `_estack` 和 `Reset_Handler`。
- `Reset_Handler` 调用 `SystemInit()`，然后准备 C 运行环境。
- `SystemInit()` 在当前文件中主要保留向量表重定位和外部存储相关条件代码；具体系统时钟配置不在本函数中完成。
- `sysmem.c` 使用 `_end`、`_estack` 和 `_Min_Stack_Size` 为 Newlib 堆增长建立边界。
- `main.c` 中存在 `int main(void)`，启动链路最终进入这里。

从项目主线看，本章位于所有外设初始化和控制逻辑之前。没有本章的启动和内存基础，后续 `HAL_Init()`、系统时钟配置、外设初始化和控制循环都没有运行前提。

## 8. 代码分析

### 8.1 链接脚本入口与内存区域

`STM32F103RCTX_FLASH.ld` 使用 `ENTRY(Reset_Handler)` 指定入口符号。它不是直接跳到 `main()`，而是先进入启动文件中的 `Reset_Handler`。

`MEMORY` 区域定义了两块主要存储：

- RAM：`ORIGIN = 0x20000000`，`LENGTH = 48K`
- FLASH：`ORIGIN = 0x8000000`，`LENGTH = 256K`

这与第01章平台边界一致，也与第02章链接脚本配置一致。

从地址计算看，48K RAM 的末地址为 `0x2000C000`，这正是 `_estack` 的计算结果。256K FLASH 覆盖范围从 `0x08000000` 到 `0x08040000` 之前，向量表放在 FLASH 起点处，使复位后取向量的地址和镜像入口保持一致。

#### 8.1.1 向量表保留与`--gc-sections`边界

当前 Debug 链接命令启用了 `-Wl,--gc-sections`，这意味着未被引用的输入 section 可能被链接器丢弃。对普通业务函数来说，这有助于裁掉未使用代码；但对启动向量表来说，只靠“源码里有 `g_pfnVectors`”还不够，因为向量表是硬件按地址读取的入口，不一定表现为 C 代码里的普通调用引用。

因此链接脚本在 `.isr_vector` 输出段中写了 `KEEP(*(.isr_vector))`。GNU ld 文档对 `KEEP` 的语义是：在启用 section 垃圾回收时，包在 `KEEP()` 中的输入 section 不会被回收。把这条工具语义落到当前项目，就是要求 `startup_stm32f103rctx.o` 中的 `.isr_vector` 即使没有普通 C 调用边，也必须保留在 FLASH 起点。

当前 `Debug/Three-axis_cloud_platformV2.map` 给出闭环证据：`.isr_vector` 位于 `0x08000000`，大小 `0x1e4`，来源为 `./Core/Startup/startup_stm32f103rctx.o`，并在同一位置导出 `g_pfnVectors`。`Debug/Three-axis_cloud_platformV2.list` 的段表也显示 `.isr_vector` 位于 `0x08000000`。这能证明当前 Debug ELF 中向量表没有被 `--gc-sections` 丢弃。

但这仍然不能证明目标板已经从该向量表启动。它只证明“链接脚本保留规则 + 当前 Debug map/list”一致；板上真实取到的初始 SP、复位 PC 和后续执行路径，还需要调试器 PC/SP、断点命中或下载记录【待验证】。

这里还要把 `ENTRY(Reset_Handler)` 和向量表第二项拆开。`ENTRY(Reset_Handler)` 是链接器选择 ELF 入口符号的规则，能影响链接结果中的入口元数据；Cortex-M3 复位时真正装载 PC 的来源，则是地址 `0x08000004` 处的向量表第二个字。本项目把 `.isr_vector` 固定放在 FLASH 起点，并在 `g_pfnVectors` 中依次写入 `_estack` 和 `Reset_Handler`，所以链接入口和硬件复位入口在当前构建中指向同一个复位处理函数，但它们不是同一层证据。

| 证据层 | 本项目证据 | 可以证明 | 不能证明 |
|---|---|---|---|
| 链接入口 | `STM32F103RCTX_FLASH.ld` 中 `ENTRY(Reset_Handler)` | ELF 的入口符号选择为 `Reset_Handler` | CPU 复位后一定已经从该地址取指 |
| 向量表入口 | `startup_stm32f103rctx.s` 中 `g_pfnVectors` 前两项为 `_estack`、`Reset_Handler`，`.map/.list` 显示 `.isr_vector` 位于 `0x08000000` | 当前 Debug 镜像具备硬件复位所需的初始 SP 和复位 PC 布局 | 目标板 Flash 起始地址实际内容一定等于该 ELF |
| 运行现场 | 调试器读 PC/SP、复位断点、下载校验或内存窗口【待验证】 | 板上本次运行确实取用了对应向量表【待验证】 | 长期运行稳定、后续外设初始化和业务逻辑正确 |

### 8.2 栈、堆和段边界符号

链接脚本定义 `_estack`、`_Min_Heap_Size` 和 `_Min_Stack_Size`。其中 `_estack` 被启动向量表使用，堆和栈的预留值则为后续 Newlib 适配和内存使用分析建立前提。

链接脚本还定义：

- `_sidata`：`.data` 初值在 FLASH 中的加载地址。
- `_sdata`、`_edata`：`.data` 在 RAM 中的起止地址。
- `_sbss`、`_ebss`：`.bss` 在 RAM 中的起止地址。

这些符号不是普通变量，而是链接阶段生成给启动代码使用的地址标记。

更细地看，`.data` 的拷贝长度由 `_edata - _sdata` 决定，拷贝源起点是 `_sidata`。如果 `_sdata = 0x20000000`、`_edata = 0x20000370`，则当前构建中 `.data` 运行区大小为 `0x370` 字节；`Debug/Three-axis_cloud_platformV2.list` 中也显示 `.data` 大小为 `00000370`。这类数值可以作为本地构建证据，但它会随源码、库和编译选项变化。

`.bss` 的清零长度由 `_ebss - _sbss` 决定。当前构建产物显示 `.bss` 从 `0x20000370` 开始，大小为 `0x1e58` 字节。它包含 HAL 句柄、控制状态、姿态解算变量、USB 缓冲等运行态对象；这些对象进入 `main()` 前应处于 0 初值状态，后续初始化函数再逐步写入真实配置。

#### 8.2.1 `.bss`清零与运行初始化边界

这一点要和“项目已经初始化完成”分开理解。`STM32F103RCTx_FLASH.ld` 把 `.bss` 放入 RAM，并导出 `_sbss`、`_ebss`；`Core/Startup/startup_stm32f103rctx.s` 的 `Reset_Handler` 在调用 `main()` 前使用这两个边界清零 RAM 区间；`Debug/Three-axis_cloud_platformV2.list` 的段表也显示 `.bss` 为 `00001e58` 字节，运行地址为 `0x20000370`。这些证据能证明当前镜像包含一段会在启动阶段清零的静态存储区。

| 层级 | 当前证据 | 能证明什么 | 不能证明什么 |
|---|---|---|---|
| 链接布局 | `.map` 中 `.bss` 位于 `0x20000370`，大小 `0x1e58`，并列出 `eepromConfig`、`sensors`、`frame_500Hz`、`uwTick` 等符号 | 这些未初始化或零初始化的全局/静态对象进入最终 RAM 布局 | 对象后续是否被写入有效配置、传感器数据是否真实、控制状态是否安全 |
| 启动动作 | `Reset_Handler` 使用 `_sbss` 到 `_ebss` 的范围执行清零，并在之后调用 `main()` | `main()` 入口前该区间按 C 静态存储语义处于 0 初值 | `HAL_Init()`、`MX_*_Init()`、`init_eepromConfig(true)` 已经执行完毕 |
| 项目初始化 | `Core/Src/main.c` 中 `main()` 依次调用 `HAL_Init()`、时钟/外设初始化，并在启动阶段调用 `init_eepromConfig(true)` 与写入 PID 覆盖值 | 清零后的对象会在应用层继续被显式配置或运行时更新 | EEPROM 持久化恢复、硬件传感器数据有效性和闭环稳定性；这些仍属于 `【待验证】` |

因此，本章可以把 `.bss` 清零作为“复位后内存初值”的构建证据，但不能把它写成“配置对象已经拥有默认参数”或“运行状态已经有效”。例如 `eepromConfig` 在 map 中落入 `.bss` 只能证明它在进入 `main()` 前为零；默认 PID、使能位覆盖和后续控制参数来自 `main()` 与 `init_eepromConfig(true)` 的执行路径。`sensors`、`frame_500Hz`、`uwTick` 也同理：它们的零初值是启动事实，运行含义要等 HAL 节拍、定时器中断、I2C 读取和姿态解算路径实际运行后才能讨论。

堆栈边界要再拆成三层证据。第一层是链接脚本的静态占位：`STM32F103RCTX_FLASH.ld` 在 `._user_heap_stack` 中先 `PROVIDE(_end = .)`，再连续增加 `_Min_Heap_Size` 和 `_Min_Stack_Size`。当前 map 文件显示 `_end = 0x200021c8`，随后预留 `0x200` 字节堆检查空间和 `0x400` 字节栈检查空间，链接期检查点到达 `0x200027c8`。这只能说明当前静态段布局后仍能容纳最小保留空间。

第二层是 Newlib 堆增长保护：`Core/Src/sysmem.c` 的 `_sbrk()` 首次调用时把 `__sbrk_heap_end` 设为 `_end`，之后每次申请前计算 `stack_limit = _estack - _Min_Stack_Size`。以当前构建数值代入，`_estack = 0x2000c000`，`_Min_Stack_Size = 0x400`，所以 `_sbrk()` 的最大堆边界是 `0x2000bc00`。如果新的堆顶超过这个边界，函数设置 `errno = ENOMEM` 并返回 `(void *)-1`。这说明项目对 Newlib 堆有防止侵入预留 MSP 栈区的运行时检查。

第三层才是运行期安全结论。即使 `_sbrk()` 没有返回 `ENOMEM`，也只能说明堆没有越过它计算出的边界，不能说明 MSP 栈真实峰值没有超过 `0x400` 字节。中断嵌套、库函数调用、浮点格式化、USB 回调和控制循环调用深度都会影响栈峰值。出版级教材在这里必须把“链接期最小保留”“堆分配边界保护”和“运行期栈水位”分开讲，否则读者会误以为 map 文件正常就等于内存绝对安全。

### 8.3 启动文件中的复位入口

`startup_stm32f103rctx.s` 中的 `Reset_Handler` 执行顺序很明确：

- 调用 `SystemInit()`。
- 拷贝 `.data`。
- 清零 `.bss`。
- 调用 `__libc_init_array`。
- 调用 `main()`。

这解释了为什么全局初始化变量在进入 `main()` 前已经有值，未初始化全局变量在进入 `main()` 前已经为 0。

汇编循环可以进一步拆开：

- `.data` 拷贝阶段，`r0` 装入 `_sdata`，`r1` 装入 `_edata`，`r2` 装入 `_sidata`，`r3` 作为偏移量从 0 开始递增。
- 每轮 `ldr r4, [r2, r3]` 从 FLASH 初值区读取 4 字节，`str r4, [r0, r3]` 写入 RAM 运行区，随后 `adds r3, r3, #4` 前进到下一字。
- 循环条件 `adds r4, r0, r3` 和 `cmp r4, r1` 判断当前 RAM 写入位置是否还小于 `_edata`。小于则继续拷贝。
- `.bss` 清零阶段，`r2` 指向 `_sbss`，`r4` 指向 `_ebss`，`r3` 固定为 0。每轮 `str r3, [r2]` 写入一个 0，再让 `r2` 增加 4 字节，直到到达 `_ebss`。

这段代码默认段边界按 4 字节对齐。链接脚本在 `.data` 和 `.bss` 周围多次使用 `. = ALIGN(4)`，正是为了让启动文件可以按字循环处理，而不需要逐字节处理尾部。

### 8.4 `SystemInit()` 的项目边界

`Core/Src/system_stm32f1xx.c` 的注释说明 `SystemInit()` 在复位后、进入主程序前由启动文件调用。

当前项目中的 `SystemInit()` 函数没有直接配置最终项目时钟树，只保留了条件编译下的外部存储初始化入口和向量表重定位入口。系统时钟的项目配置在 `main.c` 的后续函数中出现，留到第06章系统时钟树分析。

这也解释了一个常见误读：`system_stm32f1xx.c` 文件头部注释会从通用模板角度描述系统时钟和 `SystemCoreClock`，但当前项目的 `SystemInit()` 实际代码只在条件满足时执行 `SystemInit_ExtMemCtl()`，并在定义 `USER_VECT_TAB_ADDRESS` 时写 `SCB->VTOR`。是否启用了这些条件，要看当前宏定义和构建配置，不能只凭模板注释下结论。

### 8.5 `main()` 入口

`Core/Src/main.c` 中存在 `int main(void)`。启动文件调用 `main()` 后，项目才进入 HAL 初始化、系统配置、外设初始化和主循环。第03章只确认入口关系，不展开后续业务执行。

当前 `main()` 中的后续顺序可以作为边界提示：`HAL_Init()`、`SystemClock_Config()`、多个 `MX_*_Init()` 和 `while (1)` 都发生在启动文件调用 `main()` 之后。因此本章证明的是“C 入口之前的最低运行环境”，不是 HAL 时基、中断优先级、USB、I2C、PWM 或控制循环已经正确运行。

### 8.6 本节证据边界

本节只根据当前仓库说明文件、函数、宏、变量和调用关系。运行时频率、外部硬件表现、主机侧现象、传感器方向、电机响应或真实控制效果仍需调试记录、日志或仓库外实测证据；缺少证据时保持【待验证】。

### 8.7 启动链路构建产物证据边界

第03章的启动结论要分成“权威资料层”“配置源”“链接结果”和“反汇编上下文”四层，不能只看到 `main()` 或 `.isr_vector` 就直接推断目标板已经正常启动。

第一层是权威资料层。ST PM0056 和 Arm Cortex-M3 文档可以证明 Cortex-M3 向量表的架构含义：表中包含初始栈指针值和异常入口地址，复位入口来自向量表而不是普通 C 入口。GNU ld 文档可以证明链接脚本用于描述输入段如何映射到输出文件并控制内存布局，也能解释 VMA、LMA、`SECTIONS`、`MEMORY`、`ORIGIN()`、`LENGTH()` 和 `LOADADDR()` 这类链接脚本语义。该层证明“这些概念和工具语义成立”，但不能单独证明本项目的 `_estack`、`.data`、`.bss` 或 `Reset_Handler` 具体地址。

第二层是配置源。`STM32F103RCTX_FLASH.ld` 中的 `ENTRY(Reset_Handler)`、`_estack = ORIGIN(RAM) + LENGTH(RAM)`、`.isr_vector`、`.data >RAM AT> FLASH`、`.bss >RAM` 和 `PROVIDE(_end = .)` 说明链接脚本如何组织镜像；`Core/Startup/startup_stm32f103rctx.s` 中的 `g_pfnVectors`、`Reset_Handler`、`bl SystemInit`、`.data` 拷贝、`.bss` 清零、`bl __libc_init_array` 和 `bl main` 说明启动代码如何消费这些符号。这一层证明项目源码与链接脚本存在完整启动设计。

第三层是 `.map` 链接结果。当前 `Debug/Three-axis_cloud_platformV2.map` 显示 `_estack = 0x2000c000`，`.isr_vector` 位于 `0x08000000` 且来自 `./Core/Startup/startup_stm32f103rctx.o`，`g_pfnVectors` 位于向量表起点，`Reset_Handler` 位于 `0x080025bc`，`SystemInit` 位于 `0x08001d10`，`main` 位于 `0x080014d4`，`__libc_init_array` 位于 `0x08010008`；同时还能看到 `_sidata = 0x08015b70`、`_sdata = 0x20000000`、`_edata = 0x20000370`、`_sbss = 0x20000370`、`_ebss = 0x200021c8` 和 `_end = 0x200021c8`。这能证明当前 Debug ELF 的链接布局与启动文件引用的符号闭合。

第四层是 `.list` 反汇编上下文。当前 `Debug/Three-axis_cloud_platformV2.list` 的段表显示 `.isr_vector`、`.data`、`.bss` 和 `._user_heap_stack` 的地址与大小；`Reset_Handler` 反汇编区域显示先分支到 `SystemInit`，随后加载 `_sdata/_edata/_sidata` 与 `_sbss/_ebss`，再分支到 `__libc_init_array` 和 `main`。这能证明当前 ELF 中确实存在从复位入口到 C 入口的指令级路径。

但是，这四层证据仍然只能证明“Cortex-M3 启动规则成立、GNU ld 语义成立、当前仓库和当前 Debug 构建具备静态启动链路”。它不能证明芯片已经从用户 FLASH 启动、下载的是这个 ELF、MSP 实际等于 `0x2000c000`、`.data` 运行时已经成功拷贝、`.bss` 运行时已经成功清零，或 `main()` 已在目标板上执行。上述结论需要调试器 PC/SP 读数、断点命中记录、内存窗口、下载日志或运行日志；缺少现场证据时保持【待验证】。

## 9. 调试方法

本节按“现象 -> 可能原因 -> 定位方法 -> 验证步骤 -> 解决方案 -> 经验总结”组织。第03章调试的目标是确认程序能从复位入口走到 `main()`，并确认链接脚本、启动文件和运行现场证据处在同一条链路上。

### 9.1 现象与可能原因

- 链接失败并提示找不到启动符号：检查链接脚本入口和启动文件中的 `Reset_Handler`。
- 运行后全局变量初值异常：检查 `.data` 段符号和启动文件拷贝逻辑。
- 未初始化全局变量不是 0：检查 `.bss` 段符号和启动文件清零逻辑。
- 进入异常默认入口后停住：检查是否有处理函数未被项目实现，或启动链路是否跳到了错误入口。
- 栈或堆空间不足：先查看链接脚本中的 RAM 长度、最小堆栈设置和 `._user_heap_stack` 预留。
- `malloc()` 或库函数返回内存不足：继续检查 `_sbrk()` 是否触发 `ENOMEM`，并记录当时 `__sbrk_heap_end`、申请增量和 `stack_limit`。
- `.map` 中 RAM 使用量看似正常但运行仍异常：继续检查真实调用深度、中断嵌套、库函数栈消耗、动态分配路径和异常现场，不能只用链接成功证明运行安全。

### 9.2 定位方法：可观察对象

- `.cproject` 中链接脚本是否仍指向 `STM32F103RCTX_FLASH.ld`。
- `STM32F103RCTX_FLASH.ld` 中是否存在 `ENTRY(Reset_Handler)`。
- 链接脚本中是否定义 `_estack`、`_sidata`、`_sdata`、`_edata`、`_sbss` 和 `_ebss`。
- 启动文件中是否引用这些符号。
- 启动文件中是否存在 `g_pfnVectors`、`Reset_Handler` 和 `Default_Handler`。
- `Core/Src/system_stm32f1xx.c` 中是否存在 `SystemInit()`。
- `Core/Src/main.c` 中是否存在 `int main(void)`。
- `Debug/makefile` 是否使用 `-T".../STM32F103RCTX_FLASH.ld"` 链接当前镜像。
- `Debug/objects.list` 是否包含 `./Core/Startup/startup_stm32f103rctx.o`。
- `Debug/Three-axis_cloud_platformV2.list` 或 `.map` 中 `.isr_vector`、`.data` 和 `.bss` 的地址是否与链接脚本规则一致。
- `Debug/Three-axis_cloud_platformV2.map` 中 `_end`、`_estack`、`_Min_Heap_Size`、`_Min_Stack_Size` 和 `._user_heap_stack` 是否能对应到链接脚本。
- `Core/Src/sysmem.c` 中 `_sbrk()` 是否使用 `_end` 作为堆起点，并使用 `_estack - _Min_Stack_Size` 作为堆增长上限。

### 9.3 验证步骤：调试记录

- 记录链接脚本符号、启动文件引用位置、启动动作和对应结论。
- 对异常入口停住的情况，记录当前 PC、向量表入口、实际处理函数和是否落入 `Default_Handler`。
- 对栈堆问题，只记录链接脚本与调试器观察证据，不把单次停机直接写成内存规划错误。
- 对 `_sbrk()` 问题，分别记录 `_end`、`_estack - _Min_Stack_Size`、当前堆顶、申请大小和返回值；不要把 `_Min_Heap_Size` 误记成运行时最大堆容量。
- 对 `.data` 和 `.bss` 问题，记录 `_sidata/_sdata/_edata/_sbss/_ebss` 的 map 地址，并在调试器中观察进入 `main()` 前后的 RAM 内容变化；没有现场观察时，结论保持【待验证】。

### 9.4 解决方案：先分清静态链路与运行现场

链接脚本、启动文件、`.map` 和 `.list` 能证明静态启动链路；PC、SP、RAM 内容和异常入口记录才能证明某次目标板运行现场。两类证据不一致时，先补齐运行现场记录，再判断是否需要修改链接脚本或启动文件。

### 9.5 解决方案：按段定位

排查时按 `.isr_vector -> .data -> .bss -> ._user_heap_stack -> main()` 的顺序记录。这样能避免把全局变量初始化异常、堆栈预留不足和异常入口停机混成同一个问题。

### 9.6 经验总结：调试边界

当前仓库能证明启动文件、链接脚本和 `main()` 入口之间的静态关系。复位后真实执行路径、栈损坏、异常原因和调试器停机位置需要运行现场证据；缺少证据时保持【待验证】。

## 10. 常见问题

### 1. 为什么不从 `main()` 开始讲？

因为在 STM32 上，复位后 CPU 先读取向量表，装载初始栈指针，再进入复位处理函数。启动文件完成数据搬运、零初始化和系统初始化后，才调用 `main()`。

`main()` 是应用入口，但不是复位后的第一入口。先讲启动链路，可以让读者理解后续为什么链接脚本、栈顶、`.data` 和 `.bss` 会影响程序是否能正常进入应用代码。

### 2. 为什么链接脚本会影响启动？

启动文件要使用 `_estack`、`.data` 和 `.bss` 的边界符号。这些符号由链接脚本生成。如果链接脚本错误，启动文件即使逻辑正确，也可能搬错数据或清错区域。

当前仓库能证明的是启动文件引用这些符号，链接脚本定义这些内存区域。至于某次异常停机是否一定由链接脚本导致，还需要调试器栈、异常入口和内存观察记录支撑，不能只凭现象下结论。

### 3. `SystemInit()` 是不是本项目最终时钟配置？

不是。当前 `SystemInit()` 是启动阶段被调用的系统初始化入口，当前实现主要保留向量表重定位和条件初始化入口。项目具体系统时钟配置留到第06章。

这一点很关键：启动阶段存在 `SystemInit()`，不代表最终时钟已经配置完成。本项目的时钟树证据来自 `main.c` 中后续的 `SystemClock_Config()`、HAL RCC 调用和 `.ioc` 频率记录。

### 4. 向量表里存在很多入口，是否说明项目都用了？

不说明。向量表是芯片启动和异常入口结构的一部分。是否成为项目功能主线，必须看对应源码、配置和后续章节证据。

例如向量表提供了外设中断入口，只能说明芯片和启动文件具备这个入口。真正要判断项目是否使用某个中断，还要看 `.ioc`、NVIC 配置、MSP 初始化、实际中断函数和业务调用。这个边界会在第09章继续展开。

### 5. 为什么要关心堆和栈？

因为链接脚本中明确预留了最小堆和最小栈空间。后续 Newlib 适配、内存分配和运行稳定性都依赖这部分边界。

不过，第03章只能说明仓库中的内存规划和启动边界，不能单独证明运行时堆栈永远足够。更细地说，`._user_heap_stack` 证明链接期保留空间足够，`_sbrk()` 证明 Newlib 堆增长不会越过 `_estack - _Min_Stack_Size`，但二者都不证明真实 MSP 栈峰值安全。若要判断栈溢出、堆分配失败或异常复位，需要结合调试器现场、map 文件、运行路径和栈水位继续验证。

### 6. `_Min_Heap_Size` 是否就是运行时最大堆容量？

不是。`_Min_Heap_Size = 0x200` 参与 `._user_heap_stack` 的链接期空间检查，说明静态段之后至少保留 512 字节堆检查空间。项目的 `_sbrk()` 实现没有用 `_Min_Heap_Size` 计算最大堆容量，而是让堆从 `_end` 开始，最多增长到 `_estack - _Min_Stack_Size` 之前。

因此，读 map 文件时要把两个结论分开：`_Min_Heap_Size` 是链接成功所需的最低保留量，`_sbrk()` 的最大堆边界来自 `_estack` 和 `_Min_Stack_Size`。实际能否安全使用这些空间，还要看运行期栈峰值和动态分配路径。

### 7. `.data` 为什么有两个地址？

因为它同时服务两个需求：固件镜像需要在掉电后保留初值，所以初值放在 FLASH；程序运行时需要修改变量，所以运行地址放在 RAM。链接脚本中的 `AT> FLASH` 描述加载位置，`>RAM` 描述运行位置，`LOADADDR(.data)` 把加载地址导出给启动文件。

如果只放在 FLASH，普通可写全局变量无法像 RAM 变量一样被修改；如果只放在 RAM，复位后初值没有非易失来源。启动阶段的拷贝正是为了解决这个矛盾。

## 11. 实践任务

开始任务前，先回到本章第8节定位链接脚本、启动文件、`SystemInit()` 和 `main()` 的证据；第9节提供启动链路调试顺序。

任务一：整理链接脚本关键符号。

在 `STM32F103RCTX_FLASH.ld` 中找到入口、RAM/FLASH 区域、堆栈预留和 `.data`、`.bss` 符号。
验收依据是符号表包含符号名、所属区域、启动阶段用途和来源行索引。

任务二：画出复位到 `main()` 的顺序。

在 `startup_stm32f103rctx.s` 中画出 `Reset_Handler` 到 `main()` 的执行顺序。
验收依据是启动链路图包含 `SystemInit()`、数据段复制、BSS 清零、C 运行时初始化和 `main()`。

任务三：对应启动文件与链接符号。

对照链接脚本和启动文件，说明 `_sidata`、`_sdata`、`_edata`、`_sbss`、`_ebss` 分别服务哪个启动动作。
验收依据是符号动作表逐项记录链接脚本定义位置、启动文件使用位置和对应动作。

任务四：确认 `SystemInit()` 边界。

在 `system_stm32f1xx.c` 中确认 `SystemInit()` 的当前位置和当前项目边界。
验收依据是边界记录同时列出 `SystemInit()` 位置、`SystemClock_Config()` 位置和差异结论。

任务五：确认最终进入 `main()`。

在 `main.c` 中确认启动链路最终进入 `int main(void)`。
验收依据是入口对照表包含汇编跳转指令、C 入口函数和文件位置。

任务六：用构建产物交叉验证段地址。

在 `Debug/Three-axis_cloud_platformV2.list` 或 `Debug/Three-axis_cloud_platformV2.map` 中找到 `.isr_vector`、`.data` 和 `.bss` 的地址、大小与加载地址。
验收依据是构建产物记录能对应到链接脚本规则，并明确说明这些数值只代表当前 Debug 构建。

任务七：追踪 Newlib 堆边界。

在 `STM32F103RCTX_FLASH.ld`、`Core/Src/sysmem.c` 和 `Debug/Three-axis_cloud_platformV2.map` 中追踪 `_end`、`_estack`、`_Min_Heap_Size`、`_Min_Stack_Size` 和 `_sbrk()`。
验收依据是边界表能分清链接期最小保留、`_sbrk()` 最大堆边界和运行期栈水位三类结论。

实践边界：

当前任务优先形成表格、链路图、搜索记录和计算过程。涉及 IDE 现场、构建日志、断点数值、外部波形、主机侧结果或硬件响应时，若没有截图、日志或仓库外实测证据，结论保持【待验证】。

## 12. 思考题

1. 如果 `_estack` 计算错误，启动阶段最先可能出现什么风险？
2. 为什么 `.data` 既和 FLASH 有关，又和 RAM 有关？
3. 如果 `.bss` 没有清零，项目中的全局状态可能出现什么异常？
4. 为什么当前章只讲 `SystemInit()` 的启动位置，而不展开系统时钟树？
5. 向量表完整存在和项目功能真实使用之间有什么区别？
6. 如果某个中断处理函数在向量表中存在，还需要哪些后续证据才能判断它参与当前业务主线？
7. 如果只有当前仓库的启动文件和链接脚本，没有调试器停机记录，哪些启动异常原因仍不能下结论？
8. 为什么 `.data` 需要加载地址和运行地址，而 `.bss` 不需要在 FLASH 镜像里保存一份全 0 初值？
9. `._user_heap_stack` 的预留空间和运行时真实栈深度之间有什么区别？
10. 为什么 `_Min_Heap_Size` 不能直接解释为 `_sbrk()` 的最大堆容量？

## 13. 本章总结

本章建立了三轴云台项目从复位到 `main()` 的启动路径。

已经确认的结论是：

- 链接脚本定义 `Reset_Handler` 入口、FLASH/RAM 区域、堆栈预留和段布局。
- 启动文件中的向量表使用 `_estack` 和 `Reset_Handler`。
- `Reset_Handler` 调用 `SystemInit()`，再完成 `.data` 拷贝、`.bss` 清零、C 运行时初始化和 `main()` 调用。
- `.data` 的加载地址在 FLASH、运行地址在 RAM，`.bss` 只需要 RAM 区间并在启动阶段清零。
- `_Min_Heap_Size` 属于链接期最小堆保留检查，`_sbrk()` 的运行时堆边界由 `_end` 和 `_estack - _Min_Stack_Size` 决定。
- ST PM0056、Arm Cortex-M3 文档和 GNU ld 文档只能支撑架构与工具语义，不能替代本项目链接脚本、启动文件和构建产物证据。
- 当前 Debug 构建产物可交叉验证 `.isr_vector`、`.data` 和 `.bss` 的实际地址，但不能替代源码和链接脚本作为配置来源。
- 当前 `SystemInit()` 不展开项目最终时钟树，系统时钟配置留到后续章节。
- `main.c` 中的 `int main(void)` 是启动链路进入应用层的入口。

本章待验证分类：

| 类别 | 已由本章证明 | 仍保持【待验证】 |
|---|---|---|
| 启动链路边界 | 启动文件、链接脚本、`.map/.list` 能证明固件从向量表、`Reset_Handler`、`.data/.bss` 初始化和 C 运行时进入 `main()` | 不能据此证明向量表中每个中断都参与当前业务，具体中断启用和业务调用仍需后续章节逐项验证 |
| 后续初始化边界 | 本章记录 `SystemInit()`、`main()`、时钟树入口、外设初始化入口和运行调度入口之间的先后关系 | 时钟树细节、外设初始化行为和运行调度稳定性只保留入口关系，具体行为留到第06章、第07章、第09章和第26章继续验证 |
| 堆栈运行边界 | 链接脚本能证明链接期堆栈保留，`sysmem.c` 能证明 `_sbrk()` 的堆增长边界实现 | 不能证明运行期最大栈深度已经安全，栈水位、堆分配失败和异常复位仍需调试器现场或运行日志验证 |

下一章可以进入 HAL 工程裁剪与构建产物，因为启动路径已经说明了工程镜像如何被放置、如何启动，以及为什么构建结果必须与链接脚本和启动文件一致。

### 章节尾部固定检查

知识链路：

知识点总表
↓
知识依赖图
↓
学习优先级
↓
教学顺序
↓
教材章节

项目证据：

- `STM32F103RCTX_FLASH.ld`
- `Core/Startup/startup_stm32f103rctx.s`
- `Core/Src/system_stm32f1xx.c`
- `Core/Src/sysmem.c`
- `Core/Src/main.c`
- `Debug/makefile`
- `Debug/objects.list`
- `Debug/Three-axis_cloud_platformV2.list`
- `Debug/Three-axis_cloud_platformV2.map`

权威参考资料：

- ST PM0056 `STM32F10xxx/20xxx/21xxx/L1xxxx Cortex-M3 programming manual`: `https://www.st.com/resource/en/programming_manual/pm0056-stm32f10xxx20xxx21xxxl1xxxx-cortexm3-programming-manual-stmicroelectronics.pdf`
- Arm Cortex-M3 Generic User Guide `Vector table`: `https://developer.arm.com/documentation/dui0552/a/the-cortex-m3-processor/exception-model/vector-table`
- GNU ld 官方文档 `Linker Scripts`: `https://sourceware.org/binutils/docs/ld.pdf`
- GNU ld 官方文档 `Builtin Functions`: `https://sourceware.org/binutils/docs/ld/Builtin-Functions.html`
- GNU ld 官方文档 `Input Section and Garbage Collection`: `https://sourceware.org/binutils/docs/ld/Input-Section-Keep.html`

符号与函数证据：

- `ENTRY(Reset_Handler)`
- `_estack`
- `_sidata`
- `_sdata`
- `_edata`
- `_sbss`
- `_ebss`
- `_Min_Heap_Size`
- `_Min_Stack_Size`
- `_end`
- `_sbrk()`
- `g_pfnVectors`
- `Reset_Handler`
- `SystemInit()`
- `__libc_init_array`
- `main()`
- `Default_Handler`
- `.isr_vector`
- `.text.Reset_Handler`
- `.text.SystemInit`
- `.preinit_array`
- `.init_array`
- `.text.main`
- `.data`
- `.bss`
- `._user_heap_stack`

质量自检：

- P0 事实错误：通过
- P1 依赖断层：通过
- P2 逻辑连贯：通过
- P3 项目证据：通过
- P4 原理展开：通过
- P5 调试实践：通过
- P6 表达统一：通过

---
> 导航：上一章：[第02章_STM32CubeIDE工程结构](第02章_STM32CubeIDE工程结构.md) ｜ 下一章：[第04章_HAL工程裁剪与构建产物](第04章_HAL工程裁剪与构建产物.md)
