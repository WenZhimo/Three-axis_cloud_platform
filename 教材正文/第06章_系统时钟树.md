# 第06章 系统时钟树

> 导航：上一章：[第05章_CMSIS与内核访问](第05章_CMSIS与内核访问.md) ｜ 下一章：[第07章_MSP初始化与GPIO复用](第07章_MSP初始化与GPIO复用.md)

## 1. 本章目标

- 看懂本项目系统时钟从 HSE、PLL 到 SYSCLK、HCLK、PCLK1、PCLK2 和 USB 时钟的配置链路。
- 理解为什么 SysTick、TIM、I2C、UART 和 USB 都必须建立在正确的系统时钟前提上。
- 能从 `SystemClock_Config()`、`.ioc` 和 HAL RCC 源码中交叉验证项目实际频率。
- 为后续 MSP、GPIO 复用、中断节拍、定时器、通信和 USB 章节建立共同时钟基础。

本章阅读分层：

| 阅读层次 | 建议范围 | 适合读者 |
|---|---|---|
| 【必须掌握】 | 第1节到第5节，第13节总结 | 需要理解 HSE、PLL、SYSCLK、HCLK、PCLK 和 USB 48MHz 主链路的读者 |
| 【工程深化】 | 第6节到第8.1节，第9节调试方法 | 需要维护 `SystemClock_Config()`、Flash latency、`SystemCoreClock` 和外设时钟计算入口的读者 |
| 【拓展阅读】 | 第5.1节到第5.4节，第6.1节，第10节到第12节 | 需要进一步理解证据层级、RCC状态位、SysTick重装载、CSS边界和 MCO 实测方法的读者 |
| 【证据与验证】 | 第5.1节到第5.4节、第8节、第9节、章节尾部固定检查，以及所有 `【待验证】` 项 | 需要审查时钟源、PLL、AHB/APB分频、Flash等待周期、USB时钟分支、`SystemCoreClock`更新、APB外设计算、源码/构建产物、RCC状态位、寄存器读回、MCO/示波器或 USB 主机侧证据的读者 |

如果只是沿全书时钟基础学习，可以先抓住“8MHz HSE -> PLL x9 -> 72MHz SYSCLK/HCLK -> APB分频 -> USB 48MHz”这条链；判断目标板真实频率或外设现场行为时，再回到证据层级和调试方法小节。

本章速查：

| 查找目标 | 优先阅读 | 避免重复展开 |
|---|---|---|
| HSE、PLL、SYSCLK、HCLK、PCLK 和 USB 48MHz 主链路 | 第4节到第5节、第13节 | CMSIS寄存器访问回到第05章，GPIO/MSP继续到第07章 |
| 配置证据、软件频率计算和实测频率边界 | 第5.1节到第5.4节、第9节 | 不把 `.ioc`、源码或 `SystemCoreClock` 直接写成板上实测频率 |
| `SystemClock_Config()`、Flash latency 和外设时钟计算入口 | 第6节到第8.8节 | SysTick节拍继续到第09章，PWM定时器继续到第12章 |
| USB、I2C、UART、DWT 等后续章节的共同频率前提 | 第7节、第10节、章节尾部固定检查 | 具体外设初始化分别回到对应章节，不在本章重复展开 |

## 2. 前置知识

- STM32F103RCTx芯片平台
- STM32 HAL驱动库

本章只使用第01章确认的平台前提和第04章确认的 HAL 构建前提。第05章已经建立 CMSIS 与寄存器访问边界，本章不会展开 DWT、SysTick 中断、定时器 PWM 或通信外设细节。

## 3. 问题背景

三轴云台项目里，时间不是一个抽象数字。姿态采样、控制循环、电机 PWM、串口输出和 USB 通信都依赖同一套时钟基础。

如果系统时钟理解错误，后续会出现一串连锁问题：

- `SystemCoreClock / 1000` 得不到预期的 1ms 节拍基础。
- 定时器的计数频率和 PWM 频率判断会偏离项目配置。
- I2C、UART 和 USB 的外设时钟来源无法解释。
- DWT 周期数、微秒延时和执行时间统计会失去频率前提。

因此第06章要解决的问题不是“项目是不是 72MHz”这么简单，而是要说明：这个 72MHz 从哪里来，经过哪些分频，如何传递给总线和外设，以及项目文件如何证明这些结论。

## 4. 核心概念

- 系统时钟树：从振荡源、PLL、系统时钟选择、总线分频到外设时钟的层级关系。
- HSE：外部高速时钟源。本项目配置为开启 HSE，并以 HSE 作为 PLL 来源。
- HSI：内部高速时钟源。本项目 `SystemClock_Config()` 中保持 HSI 开启，但系统时钟来源选择的是 PLLCLK。
- PLL：锁相环，用于把输入时钟倍频到目标系统频率。
- 时钟域：由同一个时钟驱动的一组总线、内核或外设范围，例如 SYSCLK 域、AHB/HCLK 域、APB1/PCLK1 域和 APB2/PCLK2 域。
- SYSCLK：系统时钟来源，本项目选择 PLLCLK。
- HCLK：AHB 总线时钟，也是 `SystemCoreClock` 表示的核心时钟频率。
- PCLK1：APB1 总线时钟，本项目配置为 HCLK 二分频。
- PCLK2：APB2 总线时钟，本项目配置为 HCLK 不分频。
- APB定时器时钟：当 APB 分频不为 1 时，对应定时器时钟可能不同于该 APB 总线时钟。本项目 `.ioc` 明确给出 APB1 与 APB2 定时器时钟均为 72MHz。
- USB时钟：USB 外设需要独立的外设时钟选择，本项目选择 PLLCLK 经过 1.5 分频得到 48MHz。
- `SystemCoreClock`：CMSIS/HAL 使用的核心时钟变量，本项目后续用它配置 SysTick 周期。
- Flash等待周期：CPU 访问 Flash 时需要的等待状态，本项目 `HAL_RCC_ClockConfig()` 使用 `FLASH_LATENCY_2`。
- Flash预取缓冲：HAL 配置项 `PREFETCH_ENABLE` 当前为 `1U`，`HAL_Init()` 会启用 Flash prefetch，用来缓解高速取指时的等待影响。
- 时钟源选择：决定某一级时钟从哪里来，例如 SYSCLK 选择 PLLCLK。
- ready标志：硬件确认振荡源或 PLL 已稳定的状态位，例如 `HSERDY` 和 `PLLRDY`。
- SW/SWS状态：`SW` 是软件请求的 SYSCLK 来源，`SWS` 是硬件实际切换后的 SYSCLK 状态；调试时应以后者确认切换是否完成。
- 时钟门控使能：决定某个外设是否获得时钟输入，例如 `__HAL_RCC_TIM2_CLK_ENABLE()`。
- MCO：Microcontroller Clock Output，可把 SYSCLK、HSE、HSI 或 PLL/2 等时钟输出到 PA8 供示波器测量；当前源码未启用 MCO。
- 配置频率：由源码、`.ioc` 和 HAL 计算得到的频率。
- 实测频率：由调试器、MCO 输出、示波器、逻辑分析仪或 USB 主机观察得到的频率。

这些概念都服务于正式知识点 `系统时钟树`，不新增结构外知识点。

## 5. 工作原理

本项目的时钟树可以从输入、倍频、选择、分频和外设分支五步理解。

第一步是输入时钟。

`Core/Inc/stm32f1xx_hal_conf.h` 中 `HSE_VALUE` 为 `8000000U`。

`SystemClock_Config()` 开启 HSE，并设置 HSE 预分频为 1。因此进入 PLL 的 HSE 基础值按项目配置为 8MHz。

当前仓库没有独立硬件设计文件确认实际外部时钟器件，仓库外实测频率仍应标记为【待验证】；教材只确认工程配置中的 HSE 值。

第二步是 PLL 倍频。

`SystemClock_Config()` 设置 PLL 来源为 HSE，倍频为 `RCC_PLL_MUL9`。按工程配置计算：

- PLL 输入：8MHz
- PLL 倍频：9
- PLLCLK：72MHz

`.ioc` 中 `RCC.PLLCLKFreq_Value=72000000` 与这一计算一致。

这一段也可以写成公式：

```text
f_PLLCLK = f_HSE / HSEPrediv * PLLMUL
         = 8 MHz / 1 * 9
         = 72 MHz
```

第三步是系统时钟选择。

`SystemClock_Config()` 设置 `SYSCLKSource = RCC_SYSCLKSOURCE_PLLCLK`。

`.ioc` 中 `RCC.SYSCLKSource=RCC_SYSCLKSOURCE_PLLCLK`。因此项目系统时钟来源是 PLLCLK，而不是 HSE 直连或 HSI。

第四步是总线分频。

项目配置为：

- AHB：`RCC_SYSCLK_DIV1`，所以 HCLK 为 72MHz。
- APB1：`RCC_HCLK_DIV2`，所以 PCLK1 为 36MHz。
- APB2：`RCC_HCLK_DIV1`，所以 PCLK2 为 72MHz。

对应公式是：

```text
f_SYSCLK = f_PLLCLK = 72 MHz
f_HCLK   = f_SYSCLK / 1 = 72 MHz
f_PCLK1  = f_HCLK / 2   = 36 MHz
f_PCLK2  = f_HCLK / 1   = 72 MHz
```

`.ioc` 中 `RCC.AHBFreq_Value=72000000`、`RCC.APB1Freq_Value=36000000`、`RCC.APB2Freq_Value=72000000` 与代码一致。

对后续定时器分析还有一个关键点：APB1 预分频不为 1 时，对应定时器时钟会高于 APB1 总线时钟。

本项目 APB1 为 36MHz，但 `.ioc` 记录 APB1 定时器时钟为 72MHz。

这背后的规则可以先写成最小形式：

```text
如果 APB 预分频 = 1：
    f_TIMx = f_PCLKx
如果 APB 预分频 != 1：
    f_TIMx = 2 * f_PCLKx
```

因此本项目中：

```text
APB1: PCLK1 = 36 MHz, APB1 prescaler = 2, TIM2/TIM3/TIM4/TIM6 时钟 = 72 MHz
APB2: PCLK2 = 72 MHz, APB2 prescaler = 1, TIM8 时钟 = 72 MHz
```

这个差值必须在后续定时器章节使用，而不是在第06章展开到 PSC、ARR 和 CCR。第06章只给出定时器输入时钟，具体 PWM 频率仍要等到第12章和第30章结合 `PSC`、`ARR`、计数模式和通道输出再推导。

第五步是外设分支。

本章只建立时钟前提，不展开外设配置。当前可确认的关键分支是：

- APB1 定时器时钟：`.ioc` 记录为 72MHz。
- APB2 定时器时钟：`.ioc` 记录为 72MHz。
- USB 时钟：`SystemClock_Config()` 选择 `RCC_USBCLKSOURCE_PLL_DIV1_5`，`.ioc` 记录为 48MHz。

这样，项目形成了一条清晰链路：8MHz HSE 进入 PLL，倍频到 72MHz，作为 SYSCLK，再分发到 AHB、APB1、APB2 和 USB 分支。

### 5.1 证据层级

本章的“72MHz、36MHz、48MHz”不是同一种证据。为了避免把工程配置误写成实物测量，教材按四层理解：

```text
源码配置证据
-> HAL 计算证据
-> 运行时寄存器/变量观察
-> 目标板物理测量或主机侧现象
```

- 源码配置证据：`SystemClock_Config()` 和 `.ioc` 说明工程希望如何配置时钟。
- HAL 计算证据：`HAL_RCC_GetSysClockFreq()`、`HAL_RCC_GetPCLK1Freq()` 和 `HAL_RCC_GetPCLK2Freq()` 说明 HAL 如何从寄存器与常量计算频率。
- 运行观察证据：调试器读取 `RCC->CFGR`、`RCC->CR`、`SystemCoreClock`、`SysTick->LOAD` 和 `FLASH->ACR`。
- 物理证据：MCO 输出测量、外部晶振测量、USB 主机枚举和外部波形记录。

当前仓库能充分证明前两层，能给出运行观察的检查对象，但没有现场调试截图、MCO 波形或主机侧日志。因此实测频率、USB 枚举成功和外设实际波形仍保持【待验证】。

#### 5.1.1 构建产物证据边界

当前 Debug 构建产物还能证明时钟配置链路已经进入最终镜像，并给出关键函数的静态资源条目。它们的价值是把“源码里写了配置”推进到“当前构建确实链接了这些入口和调用路径”；但它们仍不能证明 HSE 实物频率、PLL 锁定质量、USB 枚举成功或外设真实波形。

| 链路环节 | 函数或路径 | 静态栈估计 | 圈复杂度 | 证据文件 | 证据边界 |
|---|---|---:|---:|---|---|
| 时钟配置入口 | `main()` 调用 `SystemClock_Config()` | - | - | `Debug/Three-axis_cloud_platformV2.list` | 证明当前构建的启动主线会进入系统时钟配置函数，不能证明配置运行成功。 |
| 项目时钟配置 | `SystemClock_Config()` | 96 字节 | 4 | `Debug/Core/Src/main.su` / `.cyclo` | 覆盖 HSE、PLL、SYSCLK、APB 和 USB 分支配置结构体的函数级资源。 |
| 振荡源与PLL配置 | `HAL_RCC_OscConfig()` | 32 字节 | 60 | `Debug/Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_rcc.su` / `.cyclo` | 说明 HAL 振荡源配置路径包含 ready/timeout 分支，不能替代 `HSERDY`、`PLLRDY` 运行观察。 |
| 总线时钟切换 | `HAL_RCC_ClockConfig()` | 24 字节 | 19 | `Debug/Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_rcc.su` / `.cyclo` | 覆盖 Flash latency、SYSCLK 切换、AHB/APB 分频和 `SystemCoreClock` 更新路径。 |
| SYSCLK软件计算 | `HAL_RCC_GetSysClockFreq()` | 32 字节 | 4 | `Debug/Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_rcc.su` / `.cyclo` | 证明软件频率计算函数进入构建，不能证明真实晶振频率等于 `HSE_VALUE`。 |
| PCLK软件计算 | `HAL_RCC_GetPCLK1Freq()` / `HAL_RCC_GetPCLK2Freq()` | 8 / 8 字节 | 1 / 1 | `Debug/Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_rcc.su` / `.cyclo` | 证明 APB 分频计算入口存在，不能把 PCLK 直接等同于所有外设工作频率。 |
| USB时钟分支 | `HAL_RCCEx_PeriphCLKConfig()` | 32 字节 | 14 | `Debug/Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_rcc_ex.su` / `.cyclo` | 证明 USB 分支配置函数进入构建，不能证明 USB 主机枚举成功。 |
| 外设频率查询 | `HAL_RCCEx_GetPeriphCLKFreq()` | 40 字节 | 16 | `Debug/Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_rcc_ex.su` / `.cyclo` | 证明 HAL 提供外设频率查询路径，不能替代外部测量或主机侧日志。 |

这张表也提醒读者：`.su/.cyclo` 是函数级静态分析结果，不是调用链最大栈水位和真实耗时。特别是 `HAL_RCC_OscConfig()` 圈复杂度为 60，反映 HAL 为 HSE、HSI、PLL、ready 标志、超时和错误状态保留了较多分支；项目当前配置只走其中一部分路径。若要验证“72MHz 已经在板上成立”，仍需要同时观察 `RCC->CR`、`RCC->CFGR`、`FLASH->ACR`、`SystemCoreClock`，并用 MCO 或外部仪器补充物理证据【待验证】。

#### 5.1.2 软件频率计算与实测频率证据边界

时钟树最容易混淆的地方，是把“软件算出来的频率”直接当成“板子上真实存在的频率”。本项目不能这样写。`Three-axis_cloud_platformV2.ioc`、`SystemClock_Config()` 和 HAL RCC 源码共同证明的是配置链路与软件计算链路；它们能把 8MHz HSE、PLL x9、SYSCLK=PLLCLK、HCLK=72MHz、PCLK1=36MHz、PCLK2=72MHz 和 USB=48MHz 串起来，但不能单独证明外部晶振实物、PLL 锁定质量或输出波形。

可以按下面四个层级拆开判断：

| 层级 | 当前仓库证据 | 能证明什么 | 不能证明什么 |
|---|---|---|---|
| 配置意图 | `.ioc` 中 RCC 频率字段；`SystemClock_Config()` 中 HSE、PLL、SYSCLK、APB 和 USB 配置 | CubeMX 与源码希望按 72MHz 系统时钟、36MHz APB1 和 48MHz USB 分支配置工程 | 目标板外部晶振真实频率、焊接质量和时钟稳定性 |
| 软件切换路径 | `HAL_RCC_ClockConfig()` 在 `.list` 中进入最终构建，并等待 `SWS` 匹配请求的 SYSCLK 来源 | 当前 Debug 镜像包含 SYSCLK 切换、Flash latency、AHB/APB 分频和 `SystemCoreClock` 更新路径 | 每次上电都一定切换成功；运行现场仍要看 `HSERDY`、`PLLRDY`、`SWS` 和错误返回 |
| 软件频率计算 | `HAL_RCC_GetSysClockFreq()`、`HAL_RCC_GetPCLK1Freq()`、`HAL_RCC_GetPCLK2Freq()` 读取 `RCC->CFGR` 并结合 `HSE_VALUE`、`APBPrescTable` 计算 | `SystemCoreClock`、PCLK1、PCLK2 的软件值从寄存器位和常量推导而来 | 软件值不是测频仪；若真实晶振不等于 `HSE_VALUE`，计算值会与物理频率偏离 |
| 运行与物理观察 | 调试器读取 `RCC->CR`、`RCC->CFGR`、`FLASH->ACR`、`SysTick->LOAD`，以及 MCO/示波器记录【待验证】 | 可把“代码配置”推进到“目标板现场状态”和“外部可观测频率” | 当前仓库没有这些现场记录，因此不能宣称硬件实测已经成立 |

因此，本章后续出现的 72MHz、36MHz 和 48MHz，默认都表示“仓库内配置与 HAL 软件计算可推出的频率”。只有同时拿到运行寄存器状态和外部测量记录时，才可以把它们升级为“目标板实测频率”。这也是为什么 `SW=PLL` 与 `SWS=PLL` 要分开讲：前者是软件请求，后者才是硬件报告；为什么 `SystemCoreClock=72000000` 也要谨慎使用：它说明 HAL 计算路径自洽，但不是物理时钟探头。

### 5.2 时钟源选择与时钟门控

第06章讲的是时钟来源和分频，不等于每个外设已经获得了工作时钟。

例如：

- `SystemClock_Config()` 决定 TIM2 所在的 APB1 定时器时钟前提。
- `Core/Src/tim.c` 中 `__HAL_RCC_TIM2_CLK_ENABLE()` 才打开 TIM2 外设时钟门。
- `TIM_CLOCKSOURCE_INTERNAL` 说明定时器使用内部时钟，而不是外部触发时钟。

所以后续分析定时器时要分两步：

```text
时钟树给出频率前提
-> MSP/初始化代码使能外设并选择工作模式
```

只有前者，不能证明外设已经运行；只有后者，也不能单独推导频率。

### 5.3 RCC状态位与软件计算路径

时钟树不是一组静态常量，HAL 配置时会经历“请求、等待、切换、确认、计算”几个动作。

可以拆成四类寄存器状态：

- `RCC->CR` 中的 `HSEON`、`HSERDY`：软件打开 HSE 后，硬件用 ready 位表示外部高速时钟已经稳定。
- `RCC->CR` 中的 `PLLON`、`PLLRDY`：软件打开 PLL 后，硬件用 ready 位表示 PLL 已锁定。
- `RCC->CFGR` 中的 `SW`：软件请求 SYSCLK 切到哪个来源。
- `RCC->CFGR` 中的 `SWS`：硬件实际报告当前 SYSCLK 已经来自哪个来源。

因此调试时不能只说“代码写了 PLLCLK”，还要能解释：

```text
HSEON -> HSERDY
PLLON -> PLLRDY
SW = PLL
SWS = PLL
SystemCoreClock = HAL_RCC_GetSysClockFreq() >> AHBPrescTable[HPRE]
PCLKx = HCLK >> APBPrescTable[PPREx]
```

`AHBPrescTable` 和 `APBPrescTable` 不是直接保存分频值，而是保存右移位数。例如 APB1 二分频时，`APBPrescTable[...] = 1`，所以 `HAL_RCC_GetPCLK1Freq()` 用 `HCLK >> 1` 得到 36MHz。这个设计避免了除法，也说明 HAL 的频率计算依赖寄存器位和软件常量，不是频率测量。

#### 5.3.1 SysTick重装载值的证据层级

`main()` 在 `SystemClock_Config()` 和外设初始化之后调用 `HAL_SYSTICK_Config(SystemCoreClock / 1000)`。在本项目 72MHz HCLK 的软件计算前提下，传入值为：

```text
TicksNumb = SystemCoreClock / 1000
          = 72000000 / 1000
          = 72000
```

`HAL_SYSTICK_Config()` 只是把参数继续传给 CMSIS 的 `SysTick_Config()`。`Drivers/CMSIS/Include/core_cm3.h` 中的实现先检查 `ticks - 1UL` 是否超过 `SysTick_LOAD_RELOAD_Msk`，随后写入：

```text
SysTick->LOAD = ticks - 1
SysTick->VAL  = 0
SysTick->CTRL = CLKSOURCE | TICKINT | ENABLE
```

因此，当前工程按软件频率推导得到的重装载寄存器目标值是：

```text
SysTick->LOAD = 72000 - 1 = 71999
```

这个结论要分层使用，不能把它直接写成“已经实测到 1ms”。

| 证据层级 | 当前项目证据 | 能证明什么 | 不能证明什么 |
|---|---|---|---|
| 配置推导值 | `main()` 调用 `HAL_SYSTICK_Config(SystemCoreClock / 1000)`，`SystemCoreClock` 由 HAL RCC 路径更新 | 项目希望按 HCLK 的千分之一配置 SysTick | 真实 HCLK 一定等于 72MHz |
| 寄存器目标值 | `SysTick_Config()` 写 `SysTick->LOAD = ticks - 1UL` | 若传入 72000 且配置成功，重装载目标值应为 71999 | 调试现场一定已经读到该值 |
| 中断入口路径 | `.list/.map` 中存在 `SysTick_Handler()`、`HAL_IncTick()`、`HAL_SYSTICK_Config()` 和 `SysTick_Config()` | 当前 Debug 镜像包含 SysTick 配置与 HAL tick 递增入口 | 中断一定按 1ms 周期持续触发 |
| 真实1ms节拍 | 调试器观察 `uwTick`/`HAL_GetTick()` 随墙钟递增，或用 DWT/GPIO/逻辑分析仪测量【待验证】 | 能把软件配置升级为运行节拍证据 | 当前仓库没有这些现场记录 |

教学上可以把 `71999` 看作一个非常好的边界例子：它由 `SystemCoreClock`、CMSIS SysTick 寄存器语义和当前调用路径共同推出；但它仍然属于“配置与构建可证明”的范围。若外部晶振频率不等于 `HSE_VALUE`，或者时钟切换失败，`71999` 仍可能被写入，但真实中断周期不会等于 1ms。若要把它升级为运行结论，至少还要补充 `RCC->CR`、`RCC->CFGR`、`SysTick->LOAD`、`SysTick->CTRL`、`uwTick` 递增记录或外部波形证据【待验证】。

### 5.4 Flash等待周期与性能边界

72MHz 不是只改 RCC 就结束。CPU 从 Flash 取指或读取常量时，Flash 访问速度必须跟上核心时钟。

本项目有两层相关配置：

- `HAL_RCC_ClockConfig(..., FLASH_LATENCY_2)`：把 Flash latency 配到 2 个等待状态，匹配 48MHz 到 72MHz 的 SYSCLK 区间。
- `Core/Inc/stm32f1xx_hal_conf.h` 中 `PREFETCH_ENABLE = 1U`，`HAL_Init()` 中会启用 Flash prefetch buffer。

工程含义是：项目选择 72MHz 提升控制循环和外设计算余量，同时接受 Flash 等待周期带来的取指代价，并通过 prefetch 降低部分影响。真实执行时间仍要看编译优化、代码位置、Flash 状态、DWT 计数或反汇编证据；不能只用 72MHz 推断每段 C 代码的运行时间。

## 6. STM32实现机制

STM32F1 工程里，系统时钟配置主要由 RCC、FLASH 等机制共同完成。

在本项目中，`SystemClock_Config()` 使用三个结构体：

- `RCC_OscInitTypeDef`：配置 HSE、HSI 和 PLL。
- `RCC_ClkInitTypeDef`：配置 SYSCLK 来源、AHB 分频、APB1 分频和 APB2 分频。
- `RCC_PeriphCLKInitTypeDef`：配置 USB 外设时钟来源。

调用顺序也有工程意义：

1. `HAL_RCC_OscConfig()` 先配置并启动振荡源和 PLL。
2. `HAL_RCC_ClockConfig()` 再切换 SYSCLK、设置总线分频，并使用 `FLASH_LATENCY_2`。
3. `HAL_RCCEx_PeriphCLKConfig()` 最后配置 USB 外设时钟分支。

`Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_rcc.c` 中可以看到 `HAL_RCC_ClockConfig()` 会更新 `SystemCoreClock`。

其值来自 `HAL_RCC_GetSysClockFreq()` 再结合 AHB 分频。

也就是说，本项目后面调用 `HAL_SYSTICK_Config(SystemCoreClock / 1000)` 时，依赖的是 `SystemClock_Config()` 已经完成时钟配置并更新后的核心时钟值。

`Core/Src/system_stm32f1xx.c` 提供 `SystemCoreClock` 初值和 `SystemCoreClockUpdate()` 函数。

当前项目主流程没有直接调用 `SystemCoreClockUpdate()`。

第06章把它作为 CMSIS 系统时钟维护机制的证据，而不是项目主流程中的实际调用。

还需要注意 HAL 的默认时间基准流程：

- `HAL_Init()` 会调用 `HAL_InitTick(TICK_INT_PRIORITY)`，此时默认复位时钟仍以 HSI 为基础。
- `HAL_RCC_ClockConfig()` 完成时钟切换后，会再次调用 `HAL_InitTick(uwTickPrio)` 适配新的 `SystemCoreClock`。
- 项目用户代码随后又显式调用 `HAL_SYSTICK_Config(SystemCoreClock / 1000)`。

这说明 SysTick 配置在启动过程中不是“一次写完永不改变”。调试 SysTick 时要看最后一次配置后的 `SysTick->LOAD`、`SysTick->CTRL` 和优先级设置。

如果 `SystemCoreClock` 为 72MHz，传给 `HAL_SYSTICK_Config()` 的参数为 72000。
CMSIS 的 `SysTick_Config()` 会把 `SysTick->LOAD` 写为 `ticks - 1`。
因此，预期重装载寄存器为 71999。

这个数值是配置推导结果，不等于已经证明 1ms 中断真实按 1kHz 发生。真实节拍还要观察 `HAL_IncTick()`、`HAL_GetTick()`、`SysTick->CTRL` 和调试记录。

此外，当前项目没有启用 Clock Security System。仓库中能看到 `HAL_RCC_EnableCSS()` 和 `RCC_CR_CSSON` 相关定义，但 `SystemClock_Config()` 没有调用它。因此，教材不能写成“HSE 故障会被当前工程自动处理”；只能写成“HAL/RCC 提供 CSS 机制，当前源码未启用，HSE 失效行为需要运行时验证【待验证】”。

### 6.1 工程选型含义

本项目配置为 HSE + PLL + 72MHz SYSCLK。从工程角度看，这种选择有几个直接后果：

- 72MHz 给 500Hz 控制循环、DWT 计时、PWM 输出和串口打印留下更高 CPU 预算。
- APB1 采用 HCLK/2，使 PCLK1 为 36MHz，符合 HAL RCC 源码注释中 STM32F10xxx 的 PCLK1 上限。
- APB2 保持 72MHz，给 GPIO 复用、高级定时器和 USART1 等 APB2 外设保留较高时钟前提。
- USB 选择 PLL/1.5，正好从 72MHz 得到 48MHz USB 时钟。

为什么不是 HSI 直接作为 SYSCLK？
当前项目没有写明设计说明，但从代码和 HAL 注释可判断：HSI 受电压和温度影响。
同时，HSI 无法直接给出当前 USB 48MHz 分支。
项目配置选择 HSE 进入 PLL，更适合作为全系统和 USB 的共同频率基础。
目标板实际晶振准确性仍需【待验证】。

为什么不是更低 SYSCLK？降低主频可以降低功耗和时钟压力，但会减少 500Hz 控制循环、DWT 微秒计时、软件滤波和调试输出的时间预算。本项目当前配置明显优先保证控制与外设时序余量，而不是最低功耗。

为什么不是只把 APB1 也配置为 72MHz？HAL RCC 源码注释给出 STM32F10xxx 的 PCLK1 上限为 36MHz。项目使用 HCLK/2 让 APB1 满足该边界，同时由于 APB1 定时器时钟规则，TIM2/TIM3/TIM4/TIM6 仍能获得 72MHz 定时器输入。这是“总线时钟受限”和“定时器仍需较高分辨率”之间的工程折中。

## 7. 项目中的应用

本章对应项目运行流程中的系统配置层。

直接相关文件：

- `Core/Src/main.c`
- `Three-axis_cloud_platformV2.ioc`
- `Core/Src/system_stm32f1xx.c`
- `Core/Inc/stm32f1xx_hal_conf.h`
- `Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_rcc.c`
- `Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_rcc_ex.c`
- `Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal.c`
- `Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_cortex.c`
- `Core/Src/tim.c`
- `Core/Src/i2c.c`
- `Core/Src/usart.c`
- `USB_DEVICE/Target/usbd_conf.c`

它们之间的关系是：

- `main.c` 中 `SystemClock_Config()` 是项目时钟配置入口。
- `.ioc` 保存 CubeMX 生成的 RCC 频率结果，可用于核对代码中的时钟选择和分频。
- `stm32f1xx_hal_conf.h` 提供 `HSE_VALUE` 与 `HSI_VALUE` 等频率常量。
- `system_stm32f1xx.c` 提供 `SystemCoreClock` 变量和更新函数。
- `stm32f1xx_hal_rcc.c` 提供 `HAL_RCC_OscConfig()`、`HAL_RCC_ClockConfig()` 和系统频率计算逻辑。
- `stm32f1xx_hal_rcc_ex.c` 提供 USB 外设时钟分支配置和 USB 频率计算逻辑。
- `stm32f1xx_hal.c` 和 `stm32f1xx_hal_cortex.c` 说明 HAL tick 与 SysTick 配置如何依赖 `SystemCoreClock`。
- `tim.c` 说明 TIM2、TIM3、TIM4、TIM6 和 TIM8 后续将使用本章建立的定时器时钟前提。
- `i2c.c` 说明 I2C2 配置会进一步依赖 APB1/PCLK1。
- `usart.c` 说明 USART3 波特率寄存器计算会依赖 APB1/PCLK1。
- `usbd_conf.c` 说明 USB 外设时钟门会被使能，但不证明主机枚举成功。

在 `main()` 的执行顺序中，项目先调用 `HAL_Init()`，再调用 `SystemClock_Config()`。

随后代码初始化 GPIO、TIM、I2C、UART、USB 等模块。

最后在用户初始化区域调用 `HAL_SYSTICK_Config(SystemCoreClock / 1000)`，并启用 DWT 周期计数器。

这说明系统时钟位于所有外设和时间基准之前。后续章节分析任何周期、波特率、总线速度或 PWM 频率，都必须先回到本章确认的时钟树。

从后续章节看，本章提供的频率前提会被这些模块消费：

- SysTick：`HAL_SYSTICK_Config(SystemCoreClock / 1000)`。
- DWT 延时：`us * (SystemCoreClock / 1000000)`。
- TIM2/TIM3/TIM4 PWM：`PSC=0`、`ARR=3599` 的频率推导需要 72MHz 定时器时钟。
- TIM6：`PSC=35` 的计数频率推导依赖 APB1 定时器时钟。
- TIM8：`PSC=3`、`ARR=999` 的更新频率推导依赖 APB2 定时器时钟。
- I2C2：HAL I2C 初始化调用 `HAL_RCC_GetPCLK1Freq()` 计算 `CR2`、`CCR` 和 `TRISE`。
- USART3：HAL UART 初始化调用 `HAL_RCC_GetPCLK1Freq()` 计算 `BRR`。
- USB：`RCC_USBCLKSOURCE_PLL_DIV1_5` 给 USB 分支提供 48MHz 配置前提。

## 8. 代码分析

### 8.1 `SystemClock_Config()` 的入口位置

`main.c` 在 `HAL_Init()` 之后调用 `SystemClock_Config()`。这意味着 HAL 基础初始化先建立最低限度运行环境，项目最终时钟树随后由 `SystemClock_Config()` 切换到目标配置。

`SystemClock_Config()` 不接收参数，也不返回频率值。它通过写 RCC 配置结构体并调用 HAL RCC 函数改变芯片时钟状态；如果配置失败，就进入 `Error_Handler()`。

### 8.2 振荡源与 PLL 配置

`RCC_OscInitStruct.OscillatorType = RCC_OSCILLATORTYPE_HSE`，说明本次主要配置 HSE。

`HSEState = RCC_HSE_ON`、`HSEPredivValue = RCC_HSE_PREDIV_DIV1`、`PLLSource = RCC_PLLSOURCE_HSE`、`PLLMUL = RCC_PLL_MUL9` 共同说明一条配置链路。

按当前仓库配置，项目使用 HSE 不分频后送入 PLL，并通过 9 倍频得到 72MHz PLLCLK。

`HSIState = RCC_HSI_ON` 表明 HSI 保持开启，但系统时钟来源仍由后面的 `SYSCLKSource` 决定。本章不能把 HSI 写成项目 SYSCLK 来源。

### 8.3 SYSCLK、AHB 和 APB 分频

`RCC_ClkInitStruct.SYSCLKSource = RCC_SYSCLKSOURCE_PLLCLK`，说明系统时钟选择 PLLCLK。

`AHBCLKDivider = RCC_SYSCLK_DIV1`，所以 HCLK 等于 SYSCLK。项目 `.ioc` 记录 HCLK 为 72MHz。

`APB1CLKDivider = RCC_HCLK_DIV2`，所以 PCLK1 为 36MHz。`APB2CLKDivider = RCC_HCLK_DIV1`，所以 PCLK2 为 72MHz。

`.ioc` 还记录 `RCC.APB1TimFreq_Value=72000000` 和 `RCC.APB2TimFreq_Value=72000000`。这为后续定时器章节建立前提，但本章不展开 PSC、ARR、CCR 和 PWM 模式。

### 8.4 Flash 延迟

`HAL_RCC_ClockConfig()` 的第二个参数是 `FLASH_LATENCY_2`。这表示项目在切换到当前高速时钟配置时，同时设置 Flash 等待周期。

`Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_rcc.c` 的注释中给出 SYSCLK 与 Flash latency 的对应关系。

当 SYSCLK 位于 48MHz 到 72MHz 区间时，对应 2WS。当前项目 SYSCLK 配置为 72MHz，因此 `FLASH_LATENCY_2` 与项目内 HAL 源码依据一致。

更严格的器件级依据仍应回到 STM32F103 数据手册或参考手册复核；本章先用当前仓库中的 HAL 源码和项目配置建立工程内证据链。

如果 Flash latency 设置偏低，CPU 从 Flash 取指或读常量时可能不稳定。仓库源码只能证明项目配置了 `FLASH_LATENCY_2`，不能证明目标板在所有电压、温度和批次条件下都留有裕量。

还要把等待周期和预取缓冲分开看。`FLASH_LATENCY_2` 解决的是高速访问 Flash 时的最低等待状态要求；`PREFETCH_ENABLE = 1U` 让 `HAL_Init()` 启用 prefetch，目的是减少顺序取指时的停顿。二者不是互相替代关系。调试执行时间时，应同时记录 `FLASH->ACR` 的 latency 位和 prefetch 位，而不是只看 SYSCLK。

### 8.5 USB 时钟分支

`PeriphClkInit.PeriphClockSelection = RCC_PERIPHCLK_USB`，说明本项目单独配置 USB 外设时钟。

`UsbClockSelection = RCC_USBCLKSOURCE_PLL_DIV1_5`，配合 72MHz PLLCLK 得到 48MHz USB 时钟。`.ioc` 中 `RCC.USBFreq_Value=48000000` 与此一致。

`stm32f1xx_hal_rcc_ex.c` 中 USB 分支会把该选择写入 RCC 配置，并在频率计算函数中按 `PLLCLK * 2 / 3` 得到 48MHz。

USB 48MHz 是枚举和通信的必要前提之一，但不是充分条件。USB 还需要引脚、上拉、电源、描述符、中断和主机侧枚举共同成立；这些留到 USB 章节验证。

这里仍然只讲时钟来源，不展开 USB Device 初始化、描述符或 CDC 通信。

### 8.6 `SystemCoreClock` 的更新路径

`system_stm32f1xx.c` 中 `SystemCoreClock` 初始值为 8MHz。项目进入 `SystemClock_Config()` 后，`HAL_RCC_ClockConfig()` 会在 HAL RCC 源码中更新该变量。

这解释了为什么 `main.c` 后续可以用 `SystemCoreClock / 1000` 配置 1ms SysTick 基础：它不是直接使用初始 8MHz，而是在时钟配置后使用更新后的核心时钟值。

`HAL_RCC_GetSysClockFreq()` 和 `SystemCoreClockUpdate()` 的注释都提醒：
计算频率基于 `HSE_VALUE`、`HSI_VALUE` 和当前选择的时钟源。
它们不是频率计。
若外部晶振与 `HSE_VALUE` 不一致，软件计算值会随配置自洽，但真实频率会偏离。

### 8.7 APB总线与外设计算入口

HAL RCC 提供 `HAL_RCC_GetPCLK1Freq()` 和 `HAL_RCC_GetPCLK2Freq()`。它们从 `SystemCoreClock` 出发，再根据 `RCC->CFGR` 中 APB 分频位计算 PCLK。

在本项目中：

- I2C2 位于 APB1，HAL I2C 初始化读取 `HAL_RCC_GetPCLK1Freq()`。
- USART3 位于 APB1，HAL UART 初始化读取 `HAL_RCC_GetPCLK1Freq()`。
- TIM2、TIM3、TIM4 和 TIM6 属于 APB1 定时器，`.ioc` 给出的定时器时钟为 72MHz。
- TIM8 属于 APB2 定时器，`.ioc` 给出的定时器时钟为 72MHz。

因此，后续不能把所有外设都简单说成“用 72MHz”。不同外设使用 SYSCLK、HCLK、PCLK 或定时器时钟，入口不同。

这条区分在本项目中尤其重要：

- USART3 和 I2C2 都在 APB1，HAL 初始化计算入口是 `HAL_RCC_GetPCLK1Freq()`，即 36MHz。
- TIM2、TIM3、TIM4 和 TIM6 也在 APB1，但定时器输入时钟按 APB1 定时器规则为 72MHz。
- TIM8 在 APB2，PCLK2 为 72MHz，APB2 定时器时钟也为 72MHz。
- USB 不使用 PCLK1/PCLK2 计算，而是从 PLL 分支得到 48MHz。

所以“同在 APB1”不等于“使用同一个频率口径”。总线寄存器访问、通信外设波特率计算和定时器计数输入必须分别建表。

#### 8.8 `SystemClock_Config()` 的构建产物证据边界

当前 `Debug/Three-axis_cloud_platformV2.map` 中可以看到 `.text.SystemClock_Config`、`.text.HAL_RCC_OscConfig`、`.text.HAL_RCC_ClockConfig`、`.text.HAL_RCCEx_PeriphCLKConfig` 和 `.data.SystemCoreClock` 条目。这说明系统时钟配置函数、HAL RCC 配置函数、USB 外设时钟配置函数和 `SystemCoreClock` 变量都进入了当前 Debug 链接产物。

`Debug/Three-axis_cloud_platformV2.list` 还能给出指令级上下文：`main()` 在 `HAL_Init()` 后调用 `SystemClock_Config()`；`SystemClock_Config()` 内部写入 `RCC_PLL_MUL9`，调用 `HAL_RCC_OscConfig()`，随后以 `FLASH_LATENCY_2` 调用 `HAL_RCC_ClockConfig()`，再调用 `HAL_RCCEx_PeriphCLKConfig()` 配置 USB 时钟；`HAL_RCC_ClockConfig()` 内部调用 `HAL_RCC_GetSysClockFreq()` 并写回 `SystemCoreClock`。

这组证据能证明“当前 Debug 构建包含 HSE->PLL x9、PLLCLK 作为 SYSCLK、APB1/2 分频、Flash latency 和 USB PLL/1.5 的软件配置链”。它不能证明外部晶振真实为 8MHz、PLL 已在目标板稳定锁定、`SystemCoreClock` 等于实测 HCLK、USB 引脚实际输出满足规范，或板上已经下载并运行同一 ELF。上述结论仍需要寄存器读回、MCO/示波器、调试器变量观察、下载记录或 USB 主机侧枚举记录；缺少证据时保持【待验证】。

## 9. 调试方法

本节按“现象 -> 可能原因 -> 定位方法 -> 验证步骤 -> 解决方案 -> 经验总结”组织。第06章调试的目标是确认项目时钟树配置、HAL 软件变量、寄存器状态和实测证据是否一致，而不是直接判断某个外设功能是否正常。

### 9.1 现象与可能原因

- 系统节拍不准：先检查 `SystemClock_Config()` 是否被调用，再检查 `SystemCoreClock` 更新路径。
- `SysTick->LOAD` 不是 71999：检查最后一次 `HAL_SYSTICK_Config()` 的参数和 `SystemCoreClock` 当前值。
- 定时器频率推导不一致：先按“APB 分频为 1 则 f_TIM=f_PCLK，否则 f_TIM=2*f_PCLK”复算，再进入定时器章节分析 PSC 与 ARR。
- USB 时钟异常：先核对 PLLCLK 是否为 72MHz，再核对 USB 分频是否为 1.5。
- `SystemCoreClock` 显示 72MHz 但外部测量不一致：检查 `HSE_VALUE` 与真实晶振、MCO 输出配置、测量点和示波器探头设置。
- 软件写入 PLLCLK 但系统未切换：检查 `HSERDY`、`PLLRDY` 和 `SWS`，不要只看 `SW` 请求位。
- Flash 访问或运行不稳定：检查 `FLASH->ACR` latency 位、prefetch 位、电源电压和温度条件【待验证】。
- HSE 故障后行为异常：确认当前源码没有调用 `HAL_RCC_EnableCSS()`，不能假定 CSS 自动兜底。
- 代码和 `.ioc` 频率不一致：以当前源码和当前 `.ioc` 交叉核对，不能只看其中一个文件。
- 修改 HSE 常量后频率仍异常：检查 `HSE_VALUE`、仓库外实测频率【待验证】和 PLL 配置是否同时一致。
- `HAL_RCC_ClockConfig()` 返回错误：检查 HSE ready、PLL ready、SYSCLK switch、Flash latency 和超时路径。
- I2C 或 UART 配置异常：先确认 PCLK1 计算值，再进入对应通信章节分析寄存器。
- USB 主机不识别：48MHz USB 时钟只是前置条件，还要在 USB 章节检查引脚、描述符、中断和主机日志。

### 9.2 定位方法：可观察对象

- `main.c` 中 `SystemClock_Config()` 是否在外设初始化前调用。
- `SystemClock_Config()` 中 HSE、PLL 来源、PLL 倍频和 SYSCLK 来源是否与 `.ioc` 一致。
- `.ioc` 中 `RCC.SYSCLKFreq_VALUE` 是否为 72MHz。
- `.ioc` 中 `RCC.APB1Freq_Value` 是否为 36MHz，`RCC.APB2Freq_Value` 是否为 72MHz。
- `.ioc` 中 `RCC.APB1TimFreq_Value` 和 `RCC.APB2TimFreq_Value` 是否为 72MHz。
- `.ioc` 中 `RCC.USBFreq_Value` 是否为 48MHz。
- `.ioc` 中 `RCC.MCOFreq_Value` 和 `RCC.PLLMCOFreq_Value` 是否只是 CubeMX 计算值；当前源码是否真的调用 `HAL_RCC_MCOConfig()`。
- `stm32f1xx_hal_conf.h` 中 `HSE_VALUE` 是否为 8MHz。
- `stm32f1xx_hal_conf.h` 中 `PREFETCH_ENABLE` 是否为 `1U`，`HAL_Init()` 是否启用 Flash prefetch。
- `stm32f1xx_hal_rcc.c` 的 Flash latency 注释表是否支持 72MHz SYSCLK 使用 `FLASH_LATENCY_2`。
- `main.c` 中 `HAL_SYSTICK_Config(SystemCoreClock / 1000)` 是否发生在 `SystemClock_Config()` 之后。
- `stm32f1xx_hal.c` 中 `HAL_InitTick()` 是否也会按 `SystemCoreClock` 配置 HAL tick。
- `stm32f1xx_hal_cortex.c` 中 `HAL_SYSTICK_Config()` 是否调用 `SysTick_Config()`。
- `core_cm3.h` 中 `SysTick_Config()` 是否写 `SysTick->LOAD = ticks - 1`。
- `stm32f1xx_hal_rcc_ex.c` 中 USB 频率是否按 PLL 分支计算。
- `tim.c` 中 `__HAL_RCC_TIMx_CLK_ENABLE()` 是否只是外设时钟门控，不是频率来源。
- `i2c.c`、`usart.c` 中外设初始化是否通过 HAL 间接读取 PCLK。
- `RCC->CR` 中 `HSERDY`、`PLLRDY` 是否能作为运行观察点【待验证】。
- `RCC->CFGR` 中 `SW` 与 `SWS` 是否都指向 PLL，而不是只看软件请求位。
- `RCC->CFGR` 中 MCO 位是否保持未配置；若临时启用 MCO，PA8 是否已按复用输出处理。

### 9.3 验证步骤：调试记录

- 记录源码配置值、`.ioc` 频率值、HAL 更新变量和推导结果。
- 记录 `RCC->CR` 中 HSE/PLL ready 位，`RCC->CFGR` 中 SW/SWS、HPRE、PPRE1、PPRE2 和 USBPRE 位。
- 记录 `SystemCoreClock`、`HAL_RCC_GetHCLKFreq()`、`HAL_RCC_GetPCLK1Freq()` 和 `HAL_RCC_GetPCLK2Freq()` 的观察值。
- 记录 `FLASH->ACR` latency 位、prefetch 位和 `SysTick->LOAD`。
- 若通过 MCO 或示波器测量时钟，记录 PA8 是否临时配置为 MCO、MCO 源、分频设置、测量点、仪器截图和误差范围。

### 9.4 解决方案：分清配置、变量和实测

- 对时钟异常，先分清工程配置证据、运行时变量观察和仓库外实测频率。

### 9.5 解决方案：把共同频率前提留在本章

- 对 USB、定时器或 SysTick 的后续问题，只在本章记录共同频率前提，细节转入对应章节。

### 9.6 经验总结

系统时钟树调试不能只看 `.ioc` 或 `SystemCoreClock`。配置、寄存器 ready 位、SWS 实际切换、Flash latency、USB 分频和外部测量证据需要分层记录；缺少现场测量或寄存器读回时，真实频率仍保持【待验证】。

## 10. 常见问题

### 1. 为什么项目不直接从 `main()` 的外设初始化开始分析？

因为外设初始化使用的总线时钟已经由 `SystemClock_Config()` 决定。先跳到 TIM、I2C、UART 或 USB，会失去频率前提。

定时器周期、串口波特率、I2C 时钟、USB 48MHz 和 SysTick 都依赖时钟树。第06章先建立系统频率和总线频率，后续章节才能判断参数是否有意义，而不是只背初始化函数名。

### 2. HSI 开启是否表示系统时钟来自 HSI？

不表示。项目代码中 `HSIState = RCC_HSI_ON` 只说明 HSI 保持开启；系统时钟来源由 `SYSCLKSource = RCC_SYSCLKSOURCE_PLLCLK` 决定。

判断系统时钟来源要看选择字段，而不是只看某个振荡器是否开启。当前仓库内证据指向 HSE 进入 PLL，PLL 输出作为 SYSCLK；HSI 开启只能作为备用或内部机制相关状态记录。

### 3. 为什么 HSE 8MHz 倍频 9 后是 72MHz？

工程配置中 `HSE_VALUE` 为 8MHz，HSE 预分频为 1，PLL 倍频为 9，所以 PLLCLK 为 72MHz。`.ioc` 的 `RCC.PLLCLKFreq_Value=72000000` 与这一结果一致。

这个结论属于仓库内配置与源码交叉验证。实际板卡外部晶振是否真为 8MHz，还需要硬件资料或外部测量支撑；缺少这些证据时，实物频率仍保持【待验证】。

### 4. 为什么 APB1 是 36MHz，但 APB1 定时器时钟是 72MHz？

这是 STM32 时钟树中 APB 分频与定时器时钟关系造成的结果。本项目 `.ioc` 已明确记录 APB1 总线为 36MHz、APB1 定时器时钟为 72MHz。定时器如何使用这个频率，留到后续定时器章节展开。

这也是为什么不能只看 PCLK1 就推导 PWM 频率。定时器章节需要使用“定时器时钟”而不是普通总线时钟作为输入，再结合 PSC、ARR、CCR 计算输出行为。

### 5. `SystemCoreClockUpdate()` 是否在项目主流程中被调用？

当前主流程中没有直接调用它。
项目通过 `HAL_RCC_ClockConfig()` 更新 `SystemCoreClock`。
`SystemCoreClockUpdate()` 在本章中作为 CMSIS 系统时钟维护机制出现，不作为项目主流程调用点。

因此调试时要区分“机制存在”和“当前调用路径”。
如果只搜索到函数定义，不能写成主流程调用。
如果观察 `SystemCoreClock` 数值，也要记录它是在 `HAL_RCC_ClockConfig()` 后被更新，
而不是由未调用函数自动完成。

### 6. 为什么第06章不展开具体外设的配置参数？

因为本章只建立共同时钟前提。外设初始化、引脚复用、中断、PWM、通信和 USB 中间件都有各自章节，提前展开会破坏教学顺序。

本章给后续章节提供频率依据：哪些频率来自 SYSCLK，哪些来自 APB，哪些需要专用 48MHz。具体外设是否工作、波形是否正确、主机是否识别，仍必须在对应章节结合源码和实测证据验证。

### 7. `HAL_Init()` 已经配置 SysTick，为什么后面还要看 `SystemClock_Config()`？

`HAL_Init()` 会在默认复位时钟基础上配置 HAL tick。随后 `SystemClock_Config()` 切换到项目目标时钟，`HAL_RCC_ClockConfig()` 又会调用 `HAL_InitTick()` 适配新频率。

本项目用户代码后续还显式调用 `HAL_SYSTICK_Config(SystemCoreClock / 1000)`。因此调试时应看最后一次配置后的 `SysTick->LOAD`，不能只看 `HAL_Init()` 阶段。

### 8. `SystemCoreClock` 是不是等于示波器测到的频率？

不一定。`SystemCoreClock` 是软件变量，依据寄存器配置和 `HSE_VALUE`、`HSI_VALUE` 计算。

如果外部晶振真实频率、焊接状态或时钟源状态与工程假设不一致，软件变量可能仍然显示 72MHz，但实物频率并不正确。实测需要 MCO、示波器、逻辑分析仪或调试记录。

### 9. 外设时钟使能是否等于外设频率已经正确？

不等于。`__HAL_RCC_TIM2_CLK_ENABLE()` 只是打开 TIM2 时钟门。

TIM2 的频率前提来自系统时钟树和 APB1 定时器时钟；TIM2 是否按预期计数，还要看定时器初始化、计数器启动、中断或 PWM 输出状态。

### 10. 为什么 USB 48MHz 不能直接证明 USB 可用？

USB 48MHz 只是 USB 外设工作的必要频率条件。项目还需要 USB 外设时钟门、GPIO、USB Device 中间件、描述符、端点、中断和主机侧枚举共同成立。

本章只能证明 `RCC_USBCLKSOURCE_PLL_DIV1_5` 配置和 `.ioc` 48MHz 结果。USB 是否枚举成功保持到 USB 章节验证。

### 11. 为什么 APB1 外设有的按 36MHz，有的按 72MHz？

因为“APB1 总线时钟”和“APB1 定时器时钟”不是同一个口径。USART3、I2C2 这类通信外设的 HAL 计算入口使用 `HAL_RCC_GetPCLK1Freq()`，本项目是 36MHz。

TIM2、TIM3、TIM4 和 TIM6 虽然也挂在 APB1，但当 APB1 预分频不为 1 时，定时器时钟按 2 倍 PCLK1 计算，所以本项目为 72MHz。后续计算 PWM、更新频率或计数周期时，必须先确认自己使用的是 PCLK 还是 timer clock。

### 12. `RCC.MCOFreq_Value=72000000` 是否说明当前 PA8 正在输出 72MHz？

不能。`.ioc` 中的 MCO 频率字段是 CubeMX 的时钟计算结果，当前源码没有调用 `HAL_RCC_MCOConfig()`，也没有把 PA8 配成 MCO 输出。

如果要用 MCO 做实测，需要临时配置 PA8、选择 MCO 源、确认不会和现有引脚功能冲突，并用示波器记录波形。缺少这些步骤时，MCO 只能作为可选验证方法，不能作为当前运行证据。

### 13. 当前工程是否启用了 HSE 时钟安全系统 CSS？

没有看到当前源码调用 `HAL_RCC_EnableCSS()`。仓库中存在 CSS 相关 HAL 函数和 `RCC_CR_CSSON` 位定义，只能说明芯片与 HAL 支持该机制。

因此 HSE 失效时系统如何反应不能从当前配置直接推出。若后续要讨论时钟故障保护，需要增加专门配置、异常处理和硬件故障注入或调试证据【待验证】。

## 11. 实践任务

开始任务前，先回到本章第8节定位 `SystemClock_Config()`、`.ioc` 时钟字段和 HAL RCC 证据；第9节提供时钟树核对顺序。

任务一：整理时钟初始化顺序。

在 `Core/Src/main.c` 中标出 `HAL_Init()`、`SystemClock_Config()`、外设初始化和 `HAL_SYSTICK_Config()` 的先后顺序。
验收依据是初始化顺序表按源码顺序列出四类调用，并标明系统时钟配置位置。

任务二：整理 `SystemClock_Config()` 参数。

在 `SystemClock_Config()` 中找出 HSE、PLL 来源、PLL 倍频、SYSCLK 来源、AHB 分频、APB1 分频、APB2 分频和 USB 分频。
验收依据是时钟树参数表包含输入源、PLL、总线分频、USB 分频和来源代码位置。

任务三：核对 `.ioc` 时钟频率。

在 `.ioc` 中核对 SYSCLK、HCLK、PCLK1、PCLK2、APB 定时器时钟和 USB 时钟的频率。
验收依据是 `.ioc` 频率与源码配置推导结果一致，若不一致应记录为待核查项。

任务四：确认外部和内部时钟常量。

在 `stm32f1xx_hal_conf.h` 中确认 `HSE_VALUE` 和 `HSI_VALUE`。
验收依据是常量记录表包含常量名、数值、来源文件和影响的 HAL 频率计算入口。

任务五：确认 Flash 等待周期依据。

在 `stm32f1xx_hal_rcc.c` 中找到 Flash latency 注释表，并说明 72MHz SYSCLK 为什么对应 2WS。
验收依据是 Flash latency 对照表包含 SYSCLK、等待周期、注释依据和项目配置值。

任务六：确认 `SystemCoreClock` 更新位置。

在 `stm32f1xx_hal_rcc.c` 中找到 `HAL_RCC_ClockConfig()` 更新 `SystemCoreClock` 的位置。
验收依据是更新链路记录包含函数名、更新变量、调用位置和后续读取结论。

任务七：区分 SysTick 的多次配置。

在 `stm32f1xx_hal.c`、`stm32f1xx_hal_rcc.c` 和 `main.c` 中分别找到 `HAL_InitTick()`、`HAL_RCC_ClockConfig()` 后的 tick 适配和用户区 `HAL_SYSTICK_Config()`。
验收依据是 SysTick 配置时间线说明最后一次配置应产生 `SysTick->LOAD=71999` 的配置推导值。

任务八：区分外设时钟门控与频率来源。

在 `tim.c`、`i2c.c`、`usart.c` 和 `usbd_conf.c` 中找出外设时钟使能语句。
验收依据是表格区分“外设时钟门打开”和“频率来自 SYSCLK/PCLK/定时器时钟/USB 分支”。

任务九：建立运行时寄存器核对表。

用调试器【待验证】观察 `RCC->CR`、`RCC->CFGR`、`FLASH->ACR`、`SystemCoreClock` 和 `SysTick->LOAD`。
验收依据是记录表明确每个寄存器/变量能证明什么，不能证明什么。

任务十：复算 APB 定时器时钟。

根据 `APB1CLKDivider = RCC_HCLK_DIV2` 和 `APB2CLKDivider = RCC_HCLK_DIV1`，分别计算 PCLK 和 TIMx 输入时钟。
验收依据是表格明确 USART3/I2C2 使用 PCLK1=36MHz，TIM2/TIM3/TIM4/TIM6 使用 72MHz，TIM8 使用 72MHz。

任务十一：区分 MCO 计算值与 MCO 实测。

在 `.ioc` 中找到 `RCC.MCOFreq_Value` 和 `RCC.PLLMCOFreq_Value`，再在源码中搜索是否调用 `HAL_RCC_MCOConfig()`。
验收依据是能说明当前源码未启用 MCO；若要实测，必须另行配置 PA8、MCO 源和示波器记录【待验证】。

任务十二：确认 Flash latency 与 prefetch。

在 `main.c` 中找到 `FLASH_LATENCY_2`，在 `stm32f1xx_hal_conf.h` 中找到 `PREFETCH_ENABLE`，在 `stm32f1xx_hal.c` 中找到 prefetch 启用路径。
验收依据是表格区分“等待周期正确性”“预取缓冲性能影响”和“真实执行时间仍需 DWT/反汇编验证”。

实践边界：

当前任务优先形成表格、链路图、搜索记录和计算过程。涉及 IDE 现场、构建日志、断点数值、外部波形、主机侧结果或硬件响应时，若没有截图、日志或仓库外实测证据，结论保持【待验证】。

## 12. 思考题

1. 如果 HSE 实际频率与 `HSE_VALUE` 不一致，项目中哪些时间相关结论会受到影响？
2. 为什么 `SystemClock_Config()` 必须放在外设初始化之前？
3. 如果 SYSCLK 改为 HSE 直连，哪些 `.ioc` 频率项会首先变化？
4. 为什么 USB 时钟需要从 PLL 分支单独确认？
5. 后续分析定时器 PWM 前，为什么必须先知道 APB 定时器时钟，而不能只看 PCLK1 或 PCLK2？
6. 当前仓库能证明时钟配置代码存在，但哪些频率结论仍需要目标板实测或外部条件确认？
7. 为什么 `HAL_RCC_GetSysClockFreq()` 的注释会强调它计算的不是芯片真实频率？
8. 如果只看到 `__HAL_RCC_TIM2_CLK_ENABLE()`，为什么还不能推导 PWM 频率？
9. 如果 `SysTick->LOAD` 是 71999，是否已经证明 1ms 中断真实稳定发生？还缺少哪些证据？
10. 为什么 `SW=PLL` 和 `SWS=PLL` 在调试结论中含义不同？
11. 为什么 `PREFETCH_ENABLE=1U` 不能替代 `FLASH_LATENCY_2`？
12. 为什么当前 `.ioc` 中有 MCO 频率值，也不能把 MCO 当作已经输出的实测证据？

## 13. 本章总结

本章建立了三轴云台项目的系统时钟树。

已经确认的结论是：

- 工程配置中的 HSE 值为 8MHz。
- 项目使用 HSE 作为 PLL 来源，PLL 倍频为 9，得到 72MHz PLLCLK。
- SYSCLK 来源为 PLLCLK。
- AHB/HCLK 为 72MHz。
- APB1/PCLK1 为 36MHz，APB2/PCLK2 为 72MHz。
- `.ioc` 记录 APB1 与 APB2 定时器时钟均为 72MHz。
- APB1 上 USART3/I2C2 的计算口径是 PCLK1=36MHz，而 TIM2/TIM3/TIM4/TIM6 的定时器口径是 72MHz。
- USB 时钟由 PLLCLK 经过 1.5 分频得到 48MHz。
- `HAL_RCC_ClockConfig()` 会更新 `SystemCoreClock`，后续 SysTick 配置依赖该变量。
- `HAL_Init()`、`HAL_RCC_ClockConfig()` 和用户区 `HAL_SYSTICK_Config()` 都可能参与 SysTick 配置。
- 按 72MHz HCLK 推导，最终 1ms SysTick 配置对应 `SysTick->LOAD=71999`。
- 当前工程使用 `FLASH_LATENCY_2`，并通过 `PREFETCH_ENABLE=1U` 让 `HAL_Init()` 启用 Flash prefetch。
- `.map/.list/.su/.cyclo` 的构建产物结论统一回到第5.1节和第8.1节判断：它们能证明 `SystemClock_Config()`、RCC/HAL 时钟配置函数、`main()` 调用路径、`SystemCoreClock`、静态栈和圈复杂度条目进入当前 Debug 构建，但不能替代 HSE 实物频率、PLL 锁定质量、运行寄存器状态、MCO 波形或 USB 主机侧证据。
- 当前源码未启用 MCO，也未启用 CSS；它们只能作为可选验证/保护机制记录，不能当作运行事实。
- I2C2 和 USART3 的初始化分别依赖 APB1/PCLK1 计算寄存器。
- TIM2/TIM3/TIM4/TIM6/TIM8 的周期推导必须使用 `.ioc` 给出的定时器时钟前提。

本章待验证分类：

| 类别 | 已由本章证明 | 仍保持【待验证】 |
|---|---|---|
| 构建验证 | `.map/.list/.su/.cyclo` 能证明 `SystemClock_Config()`、RCC/HAL 时钟配置函数、`main()` 调用路径、`SystemCoreClock`、静态栈和圈复杂度条目进入当前 Debug 构建。 | 构建产物不能替代 HSE 实物频率、PLL 锁定质量、运行寄存器状态、MCO 波形或 USB 主机侧证据。 |
| 软件验证 | `.ioc`、`SystemClock_Config()`、`HAL_RCC_ClockConfig()`、`SystemCoreClock`、PCLK 和 USB 频率函数能证明当前固件的软件配置与计算口径。 | 软件频率不能等同于物理测量值；运行寄存器状态、外设实际时钟和 USB 主机枚举仍需额外记录。 |
| 参数验证 | HSE=8MHz、PLL x9、SYSCLK/HCLK=72MHz、PCLK1=36MHz、PCLK2=72MHz、USB=48MHz、`FLASH_LATENCY_2`、`PREFETCH_ENABLE=1U` 和 `SysTick->LOAD=71999` 已按配置与推导列清。 | 1ms 真实节拍、控制循环抖动、Flash 真实性能、TIM/I2C/USART/USB 的实际时钟边界仍需后续章节或实测验证。 |
| 硬件验证 | RCC 外设时钟门控语句能证明 USART、I2C、TIM、USB 等外设获得时钟输入条件。 | 不证明外设功能已经正确运行、承担业务主线、输出外部波形，或目标板时钟稳定性已经满足硬件要求。 |
| 官方资料待确认 | CMSIS `ticks - 1` 规则、HAL 时钟配置流程、Flash latency/prefetch、MCO 和 CSS 可作为本章时钟分析的资料边界。 | MCO/CSS 是否作为最终验证或保护机制、Flash latency 是否覆盖目标频率和电压条件，仍需结合官方资料与项目配置确认。 |
| 实验待完成 | 本章已经把配置口径、软件频率、SysTick 推导、外设门控和可选验证机制拆成可观测对象。 | 后续需记录 HSE/PLL 稳定性、MCO 波形、1ms SysTick、控制循环抖动、USB 主机侧证据和关键外设实际波形。 |

下一章可以进入 MSP 初始化与 GPIO 复用，因为系统时钟已经建立，后续才能解释外设时钟使能、引脚复用和外设初始化之间的关系。

### 章节尾部固定检查

知识链路：

知识点总表
↓
知识依赖图
↓
学习优先级
↓
教学顺序
↓
教材章节

项目证据：

- `Core/Src/main.c`
- `Three-axis_cloud_platformV2.ioc`
- `Core/Src/system_stm32f1xx.c`
- `Core/Inc/stm32f1xx_hal_conf.h`
- `Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_rcc.c`
- `Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_rcc_ex.c`
- `Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal.c`
- `Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_cortex.c`
- `Drivers/CMSIS/Include/core_cm3.h`
- `Core/Src/tim.c`
- `Core/Src/i2c.c`
- `Core/Src/usart.c`
- `USB_DEVICE/Target/usbd_conf.c`
- `Debug/Three-axis_cloud_platformV2.map`
- `Debug/Three-axis_cloud_platformV2.list`
- `Debug/Core/Src/main.su`
- `Debug/Core/Src/main.cyclo`
- `Debug/Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_rcc.su`
- `Debug/Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_rcc.cyclo`
- `Debug/Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_rcc_ex.su`
- `Debug/Drivers/STM32F1xx_HAL_Driver/Src/stm32f1xx_hal_rcc_ex.cyclo`

外部权威资料：

- ST RM0008 Reference manual: `https://www.st.com/resource/en/reference_manual/rm0008-stm32f101xx-stm32f102xx-stm32f103xx-stm32f105xx-and-stm32f107xx-advanced-armbased-32bit-mcus-stmicroelectronics.pdf`
- ST DS5792 Datasheet for STM32F103xC/xD/xE: `https://www.st.com/resource/en/datasheet/stm32f103rc.pdf`
- ST STM32F103 documentation page: `https://www.st.com/en/microcontrollers-microprocessors/stm32f103/documentation.html`

符号、函数与配置项证据：

- `SystemClock_Config()`
- `HAL_Init()`
- `HAL_RCC_OscConfig()`
- `HAL_RCC_ClockConfig()`
- `HAL_RCCEx_PeriphCLKConfig()`
- `HAL_RCC_GetSysClockFreq()`
- `HAL_RCC_GetHCLKFreq()`
- `HAL_RCC_GetPCLK1Freq()`
- `HAL_RCC_GetPCLK2Freq()`
- `HAL_RCCEx_GetPeriphCLKFreq()`
- `SystemCoreClock`
- `SystemCoreClockUpdate()`
- `HAL_InitTick()`
- `PREFETCH_ENABLE`
- `__HAL_FLASH_PREFETCH_BUFFER_ENABLE()`
- `HAL_SYSTICK_Config(SystemCoreClock / 1000)`
- `SysTick_Config()`
- `SysTick->LOAD`
- `FLASH->ACR`
- `RCC->CR`
- `RCC->CFGR`
- `HSERDY`
- `PLLRDY`
- `RCC_CFGR_SW_PLL`
- `RCC_CFGR_SWS_PLL`
- `AHBPrescTable`
- `APBPrescTable`
- `HSE_VALUE`
- `HSI_VALUE`
- `RCC_OSCILLATORTYPE_HSE`
- `RCC_HSE_ON`
- `RCC_HSE_PREDIV_DIV1`
- `RCC_PLLSOURCE_HSE`
- `RCC_PLL_MUL9`
- `RCC_SYSCLKSOURCE_PLLCLK`
- `RCC_SYSCLK_DIV1`
- `RCC_HCLK_DIV2`
- `RCC_HCLK_DIV1`
- `FLASH_LATENCY_2`
- `RCC_USBCLKSOURCE_PLL_DIV1_5`
- `RCC.MCOFreq_Value=72000000`
- `RCC.PLLMCOFreq_Value=36000000`
- `HAL_RCC_MCOConfig()`
- `RCC_CR_CSSON`
- `HAL_RCC_EnableCSS()`
- `RCC.SYSCLKFreq_VALUE=72000000`
- `RCC.AHBFreq_Value=72000000`
- `RCC.APB1Freq_Value=36000000`
- `RCC.APB2Freq_Value=72000000`
- `RCC.APB1TimFreq_Value=72000000`
- `RCC.APB2TimFreq_Value=72000000`
- `RCC.USBFreq_Value=48000000`
- `__HAL_RCC_TIM2_CLK_ENABLE()`
- `__HAL_RCC_TIM3_CLK_ENABLE()`
- `__HAL_RCC_TIM4_CLK_ENABLE()`
- `__HAL_RCC_TIM6_CLK_ENABLE()`
- `__HAL_RCC_TIM8_CLK_ENABLE()`
- `__HAL_RCC_I2C2_CLK_ENABLE()`
- `__HAL_RCC_USART3_CLK_ENABLE()`
- `__HAL_RCC_USB_CLK_ENABLE()`

质量自检：

- P0 事实错误：通过
- P1 依赖断层：通过
- P2 逻辑连贯：通过
- P3 项目证据：通过
- P4 原理展开：通过
- P5 调试实践：通过
- P6 表达统一：通过

---
> 导航：上一章：[第05章_CMSIS与内核访问](第05章_CMSIS与内核访问.md) ｜ 下一章：[第07章_MSP初始化与GPIO复用](第07章_MSP初始化与GPIO复用.md)
